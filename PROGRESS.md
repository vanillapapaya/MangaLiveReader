# 진행 상황과 다음 작업

최종 갱신: 2026-07-27. 설계 명세는 `DESIGN.md`(v3)이고, 이 문서는 **거기서 갈라진
사실과 남은 일**만 적는다. 설계 내용을 여기에 복제하지 말 것.

**사용자 판단이 필요한 것은 `REVIEW.md` 에 모아 두었다.**

`DESIGN.md` 는 v4 로 갱신했다 — 측정으로 뒤집힌 전제 4개(§3.1 `is_bubble`, §8.2 LLM
재배열, §8.4 모델 계층, §10 지연 예산)를 사실로 고치고 변경 이력을 머리말에 남겼다.
v3 원본은 `/mnt/c/tmp/DESIGN.v3.bak`.

---

## 현재 상태: M2 골격 완료 (번역 + 캐시 + SSE 동작)

`POST /read` 가 `text/event-stream` 으로 `cached → ocr → translation* → done` 을
흘린다. 실측 종단 (yanmaga_002528, 9 region):

```
1회차  cached 35ms · ocr 343ms · 첫 translation 4,819ms · done 7,486ms
       (이후 말풍선이 300-700ms 간격으로 하나씩)
2회차  전부 7ms (캐시 적중)
```

**원문이 343ms, 첫 번역이 4.8초에 나간다.** 번역 스트리밍 전에는 첫 번역이 9.8초였다.
클라이언트(M3)는 아직 없다.

```
scripts/fetch_models.py      가중치 다운로드 (sha256 검증). 실행 완료
scripts/crop_viewer.py       전체 페이지 스크린샷 → 만화 영역만 크롭
scripts/debug_page.py        M1 검증 도구. 박스·순번·판정을 PNG 로 출력
                             기본으로 §5.4 정규화를 적용한다 (--raw 로 끔)
scripts/check_order.py       읽기 순서 회귀 확인. GPU·모델 없이 돈다
scripts/check_merge.py       박스 병합 회귀 확인. GPU·모델 없이 돈다
scripts/check_keys.py        API 키가 서비스 프로세스에서 보이는지 (값은 안 찍음)
scripts/ab_translate.py      번역 모델 A/B. GPU 없이 돈다
scripts/send_page.py         돌고 있는 서비스에 페이지를 보내고 SSE 를 눈으로 본다
tests/                       pytest. 위 두 스크립트의 단언판 + RLE 왕복
src/mtl_service/
  config.py                  service.toml 로더. 모르는 키는 예외
  ctd/                       comic-text-detector 벤더링본. NOTICE.md 먼저 읽을 것
  detect.py                  검출 싱글톤, 노이즈 폐기, 박스 병합
  mask.py                    RLE, fill_rgb, is_bubble, vertical, fill_confidence
  order.py                   재귀 XY-cut 정렬 (§8.2 컬럼 밴드는 폐기. 아래 §6)
  ocr.py                     manga-ocr 배치 추론 + 버킷 예열
  translate.py               번역. Anthropic/Gemini 중립. 순서 교정은 폐기(§10)
  cache.py                   SQLite 페이지 캐시. OCR·번역 분리 저장
  jsonstream.py              스트리밍 JSON 에서 완성된 배열 원소만 증분 추출
  env.py                     API 키 파일 로더
  metrics.py                 JSONL 계측
  app.py                     FastAPI. GPU 작업은 상주 단일 워커에서
  __main__.py                바인딩 결정 (0.0.0.0 폴백 없음)
```

검증 완료: `/health`, `/warmup`, `/read`(정상 · `include_sfx=false` · 잘못된 이미지 400).

```
uv run mtl-service                                   # 127.0.0.1:8788
uv run python scripts/debug_page.py <이미지...>       # HTTP 없이 파이프라인 직접
```

실물 검증 픽스처:

```
test/pages/     원본. 브라우저 "전체 페이지 캡처" 52장 (6사이트)
                §12 의 세 사이트 + sunday-webry / ichicomi / comic-fuz.
                뒤 셋은 프로필이 없다 — 계속 볼 사이트면 `[profiles.*]` 를 추가할 것
test/cropped/   crop_viewer.py 산출물. 파이프라인에 넣는 것은 이쪽
test/debug/     debug_page.py 산출물 (.boxes.png / .mask.png)
test/fixtures/  order_cases.json — 검출 결과 + 손으로 매긴 정답 읽기 순서
                merge_cases.json — 병합 **전** yolo 박스 + 정답 병합 결과
```

```
uv run pytest                                   # 전부. 3.5초, GPU 불필요
uv run python scripts/check_order.py            # 읽기 순서 상세 보고서
uv run python scripts/check_merge.py            # 박스 병합 상세 보고서
```

두 픽스처 모두 CPU 검출로 다시 뜰 수 있다 — 52장에 14초다. GPU 가 다른 워크로드에
잡혀 있어도 검출 단계 작업은 막히지 않는다.

`crop_viewer.py` 는 뷰어 밴드 하단을 자동으로 찾는다. 새 사이트를 넣었는데 크롭에
사이트 UI 글자가 남으면 `VIEWER_BOTTOM_OVERRIDE` 에 한 줄 추가할 것 (comic-fuz 가
그런 경우 — 만화와 본문 사이 간격이 20px 뿐이라 자동 탐지가 지나친다).

---

## 명세와 달라진 점 (이어받기 전에 반드시 읽을 것)

### 1. `is_bubble` 은 검출기가 주지 않는다

`DESIGN.md` §3.1 의 "comic-text-detector 가 마스크 + bbox + is_bubble 동시 산출"은
**사실이 아니다.** 그 모델의 yolo 클래스는 말풍선 여부가 아니라 **언어**다
(체크포인트 cfg `nc=2`, 원본 `utils/textblock.py` 의 `LANG_LIST`).

`mask.py` 가 글자 획 주위 링의 균일도·명도·채도로 직접 판정한다. §7.4 가
`fill_confidence` 용으로 정의한 신호와 같은 것이다. 검은 말풍선에 흰 글자는
효과음으로 분류되며, 그 region 은 L2 로 그려지므로 안전한 실패다.

### 2. DBNet 헤드(`text_det`)는 벤더링하지 않았다

텍스트 라인 폴리곤을 M1 응답에 쓰지 않는다. 박스 병합(§8.1)과 읽기 순서(§8.2)는
직접 구현했다. 그래서 `service.toml` 의 `box_threshold` 를 `nms_threshold` 로 바꿨다
(전자는 DB 라인 전용 값이었다).

### 3. 라이선스

`src/mtl_service/ctd/` 는 comic-text-detector + YOLOv5 발췌라 **GPL-3.0** 이다.
배포하지 않는 개인용이라 그대로 뒀다. 배포하게 되면 이것부터 정리할 것.

### 4. 이 GPU 는 다른 워크로드와 공유된다

측정 시점에 uv 관리 python 두 개가 RTX 5080 을 utilization 98% / 313W 로 상시
점유하고 있었다. 같은 코드·같은 입력의 OCR 지연이 **53ms ↔ 680ms 로 13배** 흔들린다.

2026-07-27 기준값은 유휴 상태(2% / 47W)에서 다시 잡았다 — §A 지연 표. 그래도
측정하기 전에는 매번 확인할 것:

```
nvidia-smi --query-gpu=utilization.gpu,power.draw --format=csv
```

WSL 에서는 `--query-compute-apps` 가 항상 비어 나온다. Windows 프로세스가 안 보이는
것뿐이니 이걸로 판단하지 말고 utilization/power 를 볼 것. VRAM 이 잡혀 있어도
utilization 이 낮으면 경합은 없다.

`DESIGN.md` §9.3 은 ComfyUI 공존을 VRAM 문제로 다루지만 이건 사실이 아니다.
그림 생성이 유휴일 때 VRAM 은 7.8/16.3GB 가 잡혀 있는데도 우리 지연은 정상이었다.
실제 비용은 **VRAM 이 아니라 SM 경합에 따른 지연**이다.

### 5. YOLO 헤드가 놓친 것을 마스크에서 줍는다 (`detect._promote`)

**해결됨.** 실물 8장에서 효과음(「ダッ!!」「ジャキ」「ダン ダン ダン」…)이 **하나도**
검출되지 않았다. `detect_threshold` 를 0.35 → 0.12 로 낮춰도 검출 수가 6→6, 9→9,
2→3 으로 그대로였다 — 임계값 문제가 아니라 **YOLO 블록 헤드가 그런 글자를 제안하지
않는 것**이었다. 반면 세그멘테이션 헤드가 낸 마스크에는 전부 선명히 찍혀 있었다.

그래서 `detect.py` 가 마스크 쪽에서도 줍는다. 팽창으로 획을 덩어리로 잇고, YOLO
박스가 덮은 곳과 노이즈를 걸러 남은 것을 region 으로 올린다. 자세한 건 `_promote`
주석에 있다. 걸러내는 신호가 **이진화 전 마스크 원본값**이라는 게 요점이다 —
실측하면 진짜 글자 193-221, 그림 오검출 85-142 로 뚜렷이 갈리는데
`mask_threshold`(0.3)로 이진화하면 그 차이가 사라진다.

되살아난 것 (§A 표의 누락 10건 중 9건):

- `shonenjumpplus_002421` 수상작 표기 · 「小雨詠 作」 · 「うれしいです」 → 8/8
- `sunday-webry_002556` 리드문 · 화수 표기 · 작가명 → 7/7
- `shonenjumpplus_002430` 「おはよー」 + 효과음 「ダン」×3 · 「ダッ!!」 · 「ジャキ」
- `sunday-webry_002601` 「あらすじ」 본문 · 「畔野町」 간판 · 효과음 「ぴょ」
- `comic-walker` 매미 효과음 「ジ…ジ…」, 「この湯で、あなたと 第1話」 제목

**남은 한계 — 세그멘테이션 헤드 자체가 못 보는 효과음이 있다.**
`yanmaga_002528` 의 「ピンポーン」(외곽선 처리된 흰 글자)과 「ガチャ」(보라색)는
마스크에 **아예 없다.** 승격으로는 못 건진다. 흑백 효과음은 마스크에 나온다.
yanmaga 프로필은 `include_sfx = false` 라 당장은 문제가 아니다.

효과음 OCR 은 자주 틀린다 — 「ダッ!!」→「でも、」, 「ジャキ」→「イメージ」.
region 은 제대로 잡혔으니 검출이 아니라 manga-ocr 의 한계다. 번역 프롬프트가
효과음을 그렇게 다루도록 M2 에서 감안할 것.

### 6. §8.2 컬럼 밴드 정렬은 폐기했다 → 재귀 XY-cut

**해결됨.** 자세한 근거는 `order.py` 모듈 주석에 있다. 요약만 적는다.

컬럼 밴드는 x 중심으로만 묶어서 **칸(panel) 행 개념이 없었다.** `yanmaga_002528`
에서 상단 「蜻蛉高校」(x=1113)와 하단 「ん？」(x=1103)이 같은 컬럼으로 묶여 1·2번이
되고, 그 사이에 와야 할 상단 행 나머지가 5·7번으로 밀렸다. `column_band_ratio`
어떤 값으로도 못 고친다 — x 차이가 실제로 10px 이다.

§8.2 가 예고한 "복잡한 페이지에서 10-15% 실패"는 실물에서 **51개 중 26개(51%)
실패**로 나왔다. 쉬운 페이지에서도 틀렸다. M2 의 LLM 재배열로 덮을 수준이 아니었다.

재귀 XY-cut 으로 갈아엎었다. 펼침면을 페이지로 먼저 가르고, 페이지 안에서는
가로 빈 띠(칸 행)를 세로 빈 띠보다 **먼저** 적용한다. 방향을 "가장 넓은 틈"으로
고르면 옛 실패로 돌아간다 — 한 행 안 좌우 말풍선 간격이 행 사이 간격보다 넓기
때문이다. 간격 크기가 아니라 구조의 위계로 정한다.

| | 위치 정확도 | 쌍 정확도 |
|---|---|---|
| 컬럼 밴드 (폐기, 8장) | 25/51 = 49% | — |
| 재귀 XY-cut (8장) | 46/51 = 90% | 162/166 = 98% |
| 재귀 XY-cut (52장 중 검증 4장) | **36/45 = 80%** | **229/237 = 97%** |

**남은 실패 유형은 하나다 — 칸 경계를 모른다.** `comic-fuz_030111` 에서 상단 우측
칸의 캡션 「船橋あやめ 文芸部」가 좌측 칸 뒤로 밀렸다. 우측 칸 안에서 말풍선과 캡션
사이에 세로로 빈 띠가 있는데, XY-cut 은 그것을 칸 행 경계로 착각해 페이지를 가로로
자른다. 하단에서도 같은 이유로 두 개가 밀렸다.

칸 테두리를 모르고 **글자 배치의 빈 띠만** 보기 때문이다. 고치려면 칸 검출이
필요하고, 그건 지금 벤더링하지 않은 모델 기능이다(§2). 쌍 정확도 97% 가 말해주듯
국소적으로는 거의 맞으니, M2 의 LLM 재배열 교정이 덮을 수 있는 범위로 보인다.

`service.toml [order]` 는 `column_band_ratio` → `min_gap_ratio` 로 바뀌었고,
`sort_regions()` 는 `page_size` 를 추가로 받는다.

### 7. 박스 병합 허용치에 절대 상한이 없었다

**해결.** 두 가지가 겹쳐 있었다.

**(1) 잘못된 축.** `_should_merge` 가 나란한 세로 열을 붙일 때 허용 간격을 **박스
높이**에서 뽑았다. 세로쓰기 박스는 길쭉해서 높이 450px 이면 가로로 270px 까지 손을
뻗는다. 간격이 가로로 벌어졌으면 허용치도 가로 크기에서 나와야 한다. 대칭으로 고쳤다.

**(2) 비례항만 있고 상한이 없었다.** 축을 고쳐도 큰 박스는 여전히 멀리 뻗는다.
52장 실측에서 간격 병합 30건 중 **24건이 30px 를 넘었고, 눈으로 확인한 것은 전부
오류였다** — `ichicomi_030024` 는 서로 다른 세 칸의 외침 말풍선을 페이지 높이만큼
한 박스로 묶었고, `comic-walker_025837` 은 위아래 다른 칸의 말풍선을 붙였다.

비례항을 조이는 것으로는 못 고친다. 0.3 까지 내리면 큰 오탐은 죽지만 정당한 줄 병합도
같이 죽는다 (`ichicomi_030013` 의 가로 2줄은 간격 12px 인데 박스 높이가 34px 뿐이라
허용치도 같이 작아진다). 한 말풍선 안 줄·열 간격은 박스 크기가 아니라 **페이지
해상도**에 묶이는 값이다. 그래서 `merge_gap_max_ratio`(0.025, 1200px 페이지에서 30px)
로 절대 상한을 씌웠다.

52장 결과 — 박스 451 → 431, 병합 19건:

| | 건수 | 판정 |
|---|---|---|
| 겹침 병합 (서로 포개진 중복 검출) | 13 | 명백히 옳음 |
| 간격 병합 (간격 2-29px) | 6 | 줄·열 병합. 검수함 |
| 간격 30px 초과 | **0** | 24건 전부 제거됨 |

펼침면 경계를 넘는 병합 0건. `yanmaga_002535` 의 읽기 순서도 이걸로 풀린다 —
경계를 넘던 박스가 사라져 `sort_regions` 가 페이지를 가를 수 있게 됐고, 9개가
오른쪽 상단 3 → 오른쪽 하단 4 → 왼쪽 2 로 정확히 나온다.

**앞선 "정당한 병합 0건" 기록은 틀렸다.** 8장에서만 보고 내린 결론이었다.
52장에서는 겹침 병합 13건이 전부 정당하다 — 검출기가 같은 덩어리에 박스를 겹쳐 내는
경우가 꾸준히 있다. 병합 단계를 빼자는 검토 항목은 철회한다.

### 8. 깨알 소문자 뭉치는 생성 상한으로 잡는다 (`ocr.max_text_length`)

**부분 해결.** 원래 `generate(max_length=300)` 이었다. 자기회귀라 배치에서 가장 긴
출력이 배치 전체를 붙잡으므로, 깨알 소문자 뭉치 하나가 페이지 지연을 혼자 정했다.

찾으려던 신호는 못 찾았다. **마스크에서 글자 크기를 잴 수 없다** — 획 두께를 재보면
쓰레기 6.9/5.8/5.4, 정상 5.5/5.2/5.1 로 평평하다. 마스크가 검출기 해상도(1024)의
소프트 세그멘테이션이라 원본 글자 크기가 뭉개진다. 면적 대비 글자 밀도도 안 갈린다
(쓰레기 0.15-2.19, 정상 0.77-0.84 로 뒤섞임). OCR 전에 가려낼 방법을 못 찾았다.

대신 **상한에 부딪힌 것 자체를 신호로 쓴다.** 모델이 32자를 다 쏟고도 끝을 못 찾았고
그 region 이 말풍선 밖이면 십중팔구 약관·홍보문·줄거리다. §8.3 의 `min_text_length`
와 대칭이다.

**말풍선 안이면 버리지 않고 자르기만 한다.** 진짜 긴 대사를 통째로 잃는 것이 잘린
원문을 남기는 것보다 나쁘다. 실측 정상 대사 최장 26자라 상한 32 에 여유가 있다.

8장에서 버려진 것 3개, 전부 진짜 쓰레기였고 정상 원문 손실 0 (75 → 72 region):

| 버린 것 | 말풍선 | 길이 |
|---|---|---|
| 「今日は朝食のお風呂についても素晴らしく…」 | False | 61자 |
| 「自分の子供は学校で学校の学校を始めて…」 | False | 66자 |
| 「●松木君平●時野町文畔野平学校に通学…」 | False | 37자 |

Apple Books 배너(48자)는 말풍선으로 분류돼 있어 버리지 않고 32자에서 잘렸다.

**지연은 예산 안으로 돌아왔고, 승격 이전보다도 낮다** (유휴 GPU 재측정 — §A 표).
OCR 최악 554 → **286ms**(예산 350), 평균 249 → **189ms**. region 이 51개에서 72개로
41% 늘었는데도 기준선(184ms)과 같다. 상한이 없을 때는 쓰레기 하나가 배치 전체를
붙잡았기 때문이다 — `comic-walker_002635` 는 61자 쓰레기가 사라지면서 332 → 164ms 로
절반이 됐다.

### 9. LLM 읽기 순서 교정(§8.2)은 폐기했다 — 역효과였다

`DESIGN.md` §8.2 는 XY-cut 의 실패를 LLM 재배열로 덮는다고 했다. **실측에서 반대였다.**

| | 순서 정확도 (4장, 45 region) |
|---|---|
| XY-cut (현재 코드) | **36/45** |
| gemini-3.6-flash | 33/45 |
| claude-sonnet-5 | 26/45 |

`yanmaga_002535` 는 XY-cut 이 10/10 맞힌 것을 Sonnet 이 2/10 으로 망가뜨렸다.
정작 고쳐야 할 `comic-fuz_030111`(4/13)은 아무도 못 고쳤다.

**두 방향 모두 실패했다.** 좌표를 안 주면 판단 근거가 없어 입력 순서를 그대로 베끼고,
좌표와 기하 규칙을 주면 XY-cut 을 LLM 더러 다시 구현하라는 요구가 되어 결정론적
알고리즘보다 못한다. LLM 의 강점은 기하가 아니라 대사 흐름인데, 그걸 쓰려면 좌표가
필요하고 좌표를 주면 기하로 풀려 든다.

순서는 `order.py` 것을 그대로 쓴다. 칸 경계 미인식(§6)은 칸 검출로 풀 문제다.

### 10. 번역 지연은 7-9초다 — §10 예산(1초)과 맞지 않는다

페이지당 1회 호출에 7-9초다. `effort` 를 low/medium 으로 바꿔도 거의 안 변한다 —
thinking 이 아니라 **출력 토큰 450개를 생성하는 시간**이라 그렇다.

스트리밍으로 체감은 크게 줄였다:

| | 원문 | 첫 번역 | 전체 |
|---|---|---|---|
| 스트리밍 전 | 490ms | 9,790ms | 9,801ms |
| **스트리밍 후** | **343ms** | **4,819ms** | 7,486ms |

`DESIGN.md` §10 의 예산표는 이 수치로 다시 써야 한다.

### 11. OCR 은 배치 크기마다 예열이 필요하다

최초 배치 `generate` 가 8-24초 걸린다. 배치를 버킷(1/2/4/8/16)으로 올림 패딩하고
기동 시 전부 예열한다. 빼면 첫 페이지가 24초다. `ocr.py` 상단 주석 참조.

---

## 다음 작업

### A. M1 실물 검증 — 1회차 완료

```
uv run python scripts/crop_viewer.py test/pages/*.png --out test/cropped
uv run python scripts/debug_page.py test/cropped/*.png --out test/debug
```

**검출 재현율 51/61 = 84%** (효과음 제외, 말풍선·나레이션·페이지 부속 텍스트 기준).
§1 목표 90% 에 조금 못 미친다.

| 페이지 | 있음 | 검출 | 놓친 것 |
|---|---|---|---|
| comic-walker_002629 | 5 | 4 | 세로 홍보문이 말풍선 박스에 병합돼 원문 유실 |
| comic-walker_002635 | 6 | 6 | — |
| shonenjumpplus_002421 | 8 | 5 | 상단 띠 수상작 표기, 「小雨詠 作」, 작은 말풍선 1 |
| shonenjumpplus_002430 | 7 | 6 | 「おはよー」 |
| sunday-webry_002556 | 5 | 2 | 세로 리드문, 화수 표기, 작가명 |
| sunday-webry_002601 | 12 | 11 | 「や」 |
| yanmaga_002528 | 9 | 9 | — |
| yanmaga_002535 | 9 | 8 | 표지 태그라인 |

놓친 10개 중 6개가 **말풍선 밖 여백에 놓인 글자**(제목·화수·작가명·홍보문)다.
칸 안의 말풍선은 거의 다 잡는다 — yanmaga 두 장은 9/9, 8/9.

세부는 위 §5(효과음)·§6(읽기 순서). 그 둘이 이번 검증의 본론이다.

확인된 것:

- **합성 페이지의 가로쓰기 미검출은 OOD 였다.** 실물 가로쓰기(「休み時間」
  「おお元気や元気や！」「原作・南部くまこ…」)는 정상 검출된다. 이 건은 닫는다
- **`is_bubble` 분류는 8장에서는 정확했다.** 말풍선은 전부 True, 사이드바(「あらすじ」
  「登場人物」「●松木香平●…」)와 표지 홍보문은 전부 False.

  **2026-07-27 브라우저 확장 실측에서 뒤집혔다.** sunday-webry 의 어떤 페이지에서
  「あーあ…ドジすぎ痛いやつ。」「い、家で録ったやつ…」 등 **명백한 말풍선 5개가
  False 로 나왔다.** 공통점은 **스크린톤(점무늬) 배경 위**라는 것이다 — `mask.py` 가
  글자 획 주위 링의 균일도로 판정하는데 점무늬가 링에 섞이면 "그림 위" 로 오판한다.

  `include_sfx = false` 로 필터하면 이 5개가 통째로 사라진다 (17개 vs 22개).
  §7.4 에 따르면 `is_bubble = false` 는 L3(말풍선 채우기)를 절대 하지 않으므로,
  오분류는 멀쩡한 말풍선을 계속 L2 로 강등시킨다.

  **네 가지를 재봤고 전부 실패했다** (`DEVLOG.md` 에 수치 있음): 블러 후 표준편차 /
  중앙값 이탈 비율 / IQR / 링 폭 스윕(3·5·8·12px) / flood fill 갇힘 정도. 어느
  통계로도 **오분류된 말풍선과 진짜 효과음이 안 갈린다** — 진짜 효과음의 값 범위가
  오분류 범위 안에 들어와서 어떤 문턱을 잡아도 한쪽이 깨진다. 둘 다 "가까이는 균일,
  멀어지면 튄다" 는 같은 모양이라 그렇다.

  **결론: 문제를 잘못 잡았다.** `is_bubble` 하나가 두 질문에 답하려 한다 —
  "말풍선인가 효과음인가"(`include_sfx` 필터)와 "칠해도 안전한가"(L3). 배경 통계는
  두 번째만 답할 수 있다.

  **해결: 필터를 번역 단계로 옮겼다.** 응답에 `kind` 를 추가했다 (`dialogue` /
  `sfx` / `narration` / `extra`). 번역 모델이 어차피 원문을 읽으니 추가 호출이 없다.
  실물 4장 52 region 에서 **`kind` 52/52 · `is_bubble` 39/52.** `is_bubble` 은
  양방향으로 틀린다 — 스크린톤 말풍선을 효과음으로(4건), 나레이션 상자를 말풍선으로(9건).

  `is_bubble` 은 **모델 입력에서도 뺐다.** 남겨 두면 틀린 힌트를 모델이 그대로
  따라간다 (명백한 대사 4개가 `bubble: false` 하나 때문에 오분류). 입력 토큰 -16%.

  `fill_confidence` 의 `is_bubble` 하드 게이트는 M5 에서 재검토한다 (같은 증거를
  두 번 세고 있다)
- **OCR 품질은 말풍선에서 우수**하고, 깨알 같은 약관성 소문자 뭉치에서는 무너진다
  (`comic-walker_002635` id 5, `sunday-webry_002556` id 1). manga-ocr 설계점 밖이라 예상된 것

지연 (경합 없는 GPU. 2%/47W 유휴 상태에서 8장 × 3회, 1회차 버리고 평균):

| | detect+mask | ocr | total | §10 예산 |
|---|---|---|---|---|
| 평균 | 19ms | 184ms | 204ms | 검출 165 + OCR 350 |
| 최악 | 23ms | 332ms | 354ms | |

**검출은 예산의 1/7 밖에 안 쓴다.** OCR 최악값이 예산에 5% 여유로 붙는데, 원인은
region 개수가 아니라 **생성 길이**다 — manga-ocr 은 자기회귀라 가장 긴 출력이
배치 전체를 붙잡는다:

| 페이지 | region | 최장 출력 | ocr |
|---|---|---|---|
| shonenjumpplus_002430 | 6 | 10자 | 78ms |
| sunday-webry_002601 | 11 | 36자 | 305ms |
| sunday-webry_002556 | **2** | 46자 | **166ms** |
| comic-walker_002635 | 6 | 60자 | 332ms |

region 2개짜리가 11개짜리보다 느린 이유가 이것이다. 그리고 그 긴 출력들은 전부
깨알 같은 약관성 소문자 뭉치에서 나온 **쓰레기 결과**다(§A 확인된 것 3번째).
즉 그걸 걸러내면 품질과 최악 지연이 같이 좋아진다 — `[ocr] min_text_length` 의
반대쪽, 상한이나 region 면적 대비 글자 밀도 조건을 검토할 것.

§5 마스크 승격과 §8 생성 상한을 차례로 켠 뒤 (전부 유휴 GPU 에서 같은 조건):

| | 기준 | §5 승격 후 | §8 상한 후 | 52장 최종 | §10 예산 |
|---|---|---|---|---|---|
| detect+mask 평균 | 19 | 26 | 26 | 28ms | 165 |
| detect+mask 최악 | 23 | 31 | 30 | 38ms | |
| ocr 평균 | 184 | 249 | **189** | 228ms | 350 |
| ocr 최악 | 332 | **554** | **286** | 391ms | |
| total 평균 | 204 | 276 | 216 | 257ms | |
| total 최악 | 354 | 584 | 314 | 421ms | |

마지막 열은 §7 병합 수정까지 반영한 52장 전체(552 region, 유휴 GPU, 1회 통과)다.
앞 세 열은 같은 8장 3회 반복이라 직접 비교는 조심할 것 — 52장 쪽이 표본이 넓고
1회 측정이라 노이즈가 크다. **OCR 예산 350ms 를 넘긴 페이지는 52장 중 2장**이고
최악이 391ms 다. 초과 폭이 12% 라 당장 손댈 일은 아니지만, region 이 늘면 다시 볼 것.

승격이 OCR 최악값을 554ms 로 밀어 예산을 넘겼고, 생성 상한이 286ms 로 되돌렸다.
**예산 이내이고 승격 이전 기준선(332ms)보다도 낮다.** region 이 51 → 72개로 41%
늘었는데 평균이 기준선과 같다는 게 요점이다 — 지연을 정하는 것은 region 개수가
아니라 생성 길이다.

검출은 예산의 1/5 밖에 안 쓴다. 여유는 여전히 검출 쪽에 있다.

### A-2. 다음 회차에 할 것

1. ~~읽기 순서 재설계~~ — **완료.** §6. 49% → 90%
2. ~~마스크 연결요소를 region 으로 승격~~ — **완료.** §5. 누락 10건 중 9건 회수
3. ~~깨알 소문자 뭉치 걸러내기~~ — **완료.** §8. 쓰레기 3건 제거·손실 0,
   OCR 최악 554 → 286ms 로 예산 복귀
4. ~~박스 병합 과다~~ — **완료.** §7. 52장 기준 간격 30px 초과 병합 0건,
   펼침면 경계를 넘는 병합 0건
5. ~~유휴 GPU 에서 지연 재측정~~ — **완료.** §A 지연 표

**A 회차 완료.** 남은 것은 픽스처 검증 범위를 넓히는 일뿐이다:

6. ~~`order_cases.json` 갱신~~ — **완료.** 52장 중 칸 구조가 복잡한 13장(184 region)을
   골라 고정했다. 그중 4장은 칸 구조를 눈으로 보고 정답을 매겼고(verified),
   9장은 회귀 기준선 스냅샷이다
7. ~~픽스처 장수 늘리기~~ — **완료.** 8 → 52장, 4 → 6사이트. §7 병합 판단이
   이걸로 결정됐다 (병합 단계 유지)
8. **검증 페이지를 늘릴 것.** 지금 4장뿐이라 80% 라는 수치의 신뢰구간이 넓다.
   `test/debug52/<page>.boxes.png` 를 보고 `expected_order` 를 고쳐 `verified` 를
   켜면 된다.

   다만 **실물의 상당수는 정답을 기계적으로 정할 수 없다.** 늘리려다 확인한 것:
   `comic-walker_025825` 왼쪽 페이지는 칸 격자 없는 전면 일러스트에 말풍선이 떠 있고,
   `sunday-webry_025651` 은 게임 UI 화면이 겹친 개그 페이지다. 둘 다 "우→좌 위→아래"를
   적용할 칸 구조 자체가 없다. 검증 후보는 **칸 격자가 뚜렷한 단일 페이지**로 고를 것.
   이런 페이지에서는 애초에 순서 정확도라는 지표가 성립하지 않으므로, 스냅샷으로 두고
   정확도 합계에서 빼는 지금 구조가 맞다

### B. tests/ — 완료

`uv add --dev pytest` 로 추가했다 (pytest 9.1.1, lockfile 변경됨).
`pyproject.toml [tool.pytest.ini_options]` 에 `pythonpath = ["src"]` 가 필요하다 —
src 레이아웃이라 명시하지 않으면 `mtl_service` 를 못 찾는다.

```
uv run pytest            # 96 통과 / 31 스킵 / 1 xfail, 3.5초. GPU·모델 불필요
```

- `tests/test_mask_rle.py` — `encode_rle`/`decode_rle` 왕복. 난수 50회 포함.
  이 RLE 가 오버레이 L3 로 그대로 나가므로 한 픽셀만 어긋나도 글자 가장자리가 남는다
- `tests/test_fixtures.py` — `order_cases.json` / `merge_cases.json` 회귀.
  검증 페이지와 스냅샷을 나눠 돌리고, 펼침면 경계를 넘는 병합이 없는지 따로 본다

**xfail 이 하나 있다** — `comic-fuz_030111` 의 읽기 순서. §6 에 적은 칸 경계 미인식이라
알려진 실패다. 고치면 xfail 이 xpass 로 바뀌어 바로 드러난다.

`scripts/check_order.py` / `check_merge.py` 는 그대로 둔다. 테스트는 CI 가 잡을 수 있게
단언만 하고, 스크립트는 **어디가 어떻게 틀렸는지** 사람이 읽는 보고서를 낸다. 회귀가
나면 테스트가 먼저 알려주고 스크립트로 들여다보는 순서다.

GPU 나 모델이 필요한 것은 테스트에 넣지 않았다. 파이프라인 전체 검증은
`scripts/debug_page.py` 몫이다.

### C. M2 — 번역 + 캐시 + SSE (골격 완료)

**완료된 것**

- `translate.py` — 프로바이더 중립(Anthropic/Gemini). 페이지당 1회 호출, JSON 스키마
  강제, 시스템 프롬프트 캐싱, 직전 페이지 원문 문맥(§8.4)
- `cache.py` — SQLite. **OCR 과 번역 분리 저장**(§8.5), 해밍 거리 근사 매칭,
  90일 정리. 테스트 11개
- `app.py` — `/read` 를 SSE 로 전환(§4.1). `cached → ocr → translation* → done`.
  GPU 워커와 번역 스레드 풀을 분리해 번역 9초 동안 GPU 가 놀지 않는다. 테스트 6개
- `scripts/ab_translate.py` — 모델 A/B. `scripts/check_keys.py` — 키 확인

**모델 선택 (실측 6장)**

| | 지연 중앙값 | 입력 | 출력 | 200쪽 1권 |
|---|---|---|---|---|
| **claude-sonnet-5** | **8.7초** | 516 | **444** | **$1.64** |
| gemini-3.6-flash | 11.0초 | 752 | 1,477 | $2.44 |

Gemini 는 단가가 절반인데 사고 토큰이 3.3배라 **실제로 더 비싸고 더 느리다.**
`service.toml [api]` 를 Sonnet 5 / `effort=low` 로 두었다.

**남은 것**

1. ~~번역 스트리밍~~ — **완료.** 첫 번역 9.8초 → 4.8초. `jsonstream.py` 가
   스키마 강제를 유지한 채 완성된 원소만 뽑는다
2. `service.toml [server]` M1 개발용 설정 뒤집기:
   `auth_disabled = false`, `dev_bind_loopback = false`, `auth_token` 실제 값
3. 캐시 정리 태스크를 어딘가에서 주기 실행 (`purge_expired`)
4. `[profiles.*]` 가 세 사이트뿐이다. ichicomi / comic-fuz / sunday-webry 추가

### D. M3 이후

`DESIGN.md` §11 그대로. `mtl_client/` 와 `mtl_shared/{layout,coords}.py` 는
아직 빈 상태다.

---

## 알아두면 좋은 것

- 모델 가중치: `models/comictextdetector.pt` (76MB, sha256 검증됨).
  manga-ocr 은 HuggingFace 캐시에 있다
- `logs/metrics.jsonl` 에 요청마다 단계별 ms 가 쌓인다
- 서비스는 지금 루프백 전용 + 인증 꺼짐(M1 개발 설정). 기동 시 경고를 찍는다


---

## 원격 클라이언트 (2026-07-27)

맥북에서 붙어 보려고 M2 로 미뤄 뒀던 바인딩을 앞당겼다.

- `service.toml [server].dev_bind_loopback = false` — 이제 Tailscale 주소(100.66.125.121)에만 바인딩
- 윈도우 인바운드 규칙 `mtl-service (Tailscale)` 생성 — Tailscale 인터페이스 · Private ·
  TCP 8788 · 이 venv 의 python.exe 로만 좁힘. 기본 정책이 차단이라 이게 없으면 조용히 막힌다
- 확장 서비스 주소를 `chrome.storage.sync` 로 뺐다 (옵션 화면 추가). 하드코딩이면
  머신마다 파일을 고쳐야 한다
- `manifest.json host_permissions` 에 Tailscale 주소 추가

**아직 안 한 것:** `auth_disabled = true` 그대로다. tailnet 안이라 외부에는 안 열리지만
같은 tailnet 의 기기는 전부 붙는다. 확장 쪽 `X-Auth-Token` 전송은 이미 구현돼 있어
`service.toml` 과 옵션 화면만 채우면 된다.

**검증 대기:** Retina(dpr=2). 좌표 계산은 dpr 을 타고 들어가 있지만 2배 화면에서
돌려 본 적이 없다.


---

## WSL 실행 환경 (2026-07-27)

`./run-service.sh` 로 WSL 에서 서비스를 띄운다. 윈도우 `py -X utf8 -m mtl_service` 와
같은 것이지만 환경이 둘로 갈린다.

| | 경로 | 쓰는 곳 |
|---|---|---|
| 윈도우 | `.venv/Scripts/python.exe` | cmd |
| WSL | `~/.venvs/mlr/bin/python` | `run-service.sh` |

**리눅스 venv 는 `/mnt/c` 에 두지 않는다.** drvfs 라 `import torch` 가 35.9초 걸린다
(ext4 는 2.0초). 자세한 것은 `DEVLOG.md`.

- uv 0.11.32 설치 (`~/.local/bin`)
- torch 2.11.0+cu128, CUDA 정상 (`Using CUDA`, RTX 5080)
- 테스트 138 통과 — 윈도우와 같다
- 지연도 손해 없음: detect+mask 34ms · ocr 185ms

**지금은 루프백 바인딩이다** (`dev_bind_loopback = true`). WSL2 가 localhost 를
윈도우로 전달하니 윈도우 브라우저는 그대로 붙지만 **맥북은 못 붙는다.** 맥북
테스트를 할 때는 윈도우에서 서비스를 띄우고 이 값을 `false` 로 되돌릴 것
(방화벽 규칙 `mtl-service (Tailscale)` 는 이미 있다).


---

## 드래그 영역 선택 (2026-07-27)

`Alt+Shift+D` 로 읽을 영역을 직접 고른다. 자동 뷰어 탐지가 틀리는 사이트를 위한
탈출구다 — 없으면 그런 사이트는 통째로 못 쓴다.

- `manifest.json` 에 `select-region` 명령 추가
- `content.js` — `startSelection()` / `cancelSelection()`, 덮개가 포인터 이벤트를 먹는다
- `background.js` — `run(tab, override)` 로 probe 를 건너뛴다. 콘텐츠 스크립트가
  `read-rect` 로 되쏘는 구조 (MV3 워커가 응답을 오래 못 기다린다)
- `MAX_LONG_SIDE = 2600` 추가 — 가늘고 긴 선택에서 확대가 터지는 것을 막는다

서버는 손대지 않았다. 드래그 크기 크롭 3종으로 확인했고 자세한 것은 `DEVLOG.md`.

**미검증:** 실제 브라우저에서의 드래그 조작감. 코드 경로는 문법 검사만 했다.


---

## 조작 패널 · 자동 감지 (2026-07-27)

`DESIGN.md` §5.1 "페이지 전환 자동 감지" 를 확장 경로로 구현했다. M2 미완 항목이었다.

- **패널** — 오버레이 오른쪽 위 버튼 다섯 (읽기/영역/자동/라벨/효과음).
  단축키를 몰라도 마우스로 다 된다. 켜짐 상태는 주황색으로 보인다
- **자동 감지** — 입력(클릭·키·휠)과 DOM 변화를 신호로 받아 0.7초 뒤 캡처,
  해시가 달라졌을 때만 읽는다. 탭별 상태, 기본 꺼짐
- `run()` 에서 `prepare()` 분리 — 판정에 쓴 캡처를 읽기에 재사용한다

**미검증:** 실제 브라우저 동작. 코드 경로는 문법 검사와 선언 순서 확인만 했다.
특히 자동 감지가 사이트별로 얼마나 잘 무는지는 직접 넘겨 봐야 안다.


---

## 실사용 피드백 반영 (2026-07-27 저녁)

맥북에서 실제로 읽으며 나온 문제 넷을 고쳤다. 수치는 `DEVLOG.md`.

| 증상 | 원인 | 조치 |
|---|---|---|
| 영역을 골라도 번역이 안 됨 | 글자가 화면을 크게 차지하면 검출기가 분포 밖 (선택이 폭의 40%↑ 이면 검출 0) | 3배 넓게 보내고 결과를 고른 범위로 클리핑. 대사 회수 35% → **94%** |
| 같은 페이지가 계속 다시 번역됨 | **캐시 키가 sha256** 이라 서버 퍼지 매칭이 원리적으로 불가 | 지각 해시로 교체 (`fuzzy_hamming` 3 → 20). 같은 페이지 재읽기 비용 0 |
| 6줄짜리 대사가 반만 번역됨 | `max_text_length = 32` 가 자름 (실측 최장 60자) | 64 로 상향 |
| 초록 박스가 툭 사라짐 | 효과음 판정 시 `display: none` | 0.25초 페이드 |

**UI 추가:** 패널 세로 배치 + 유휴 시 반투명 · 「상태」 토글 · 박스 우클릭 메뉴
(다시 읽기 / 지우기 / 걸러진 것 보기).

**미검증:** 위 넷 모두 브라우저 실사용은 아직. 서버 쪽(클리핑·상한·해시)은 실측했다.


---

## 긴 대사 처리 (2026-07-27 밤)

"2~3줄 이상은 번역이 되다가 만다" 제보에서 세 층의 원인이 나왔다. 수치는 `DEVLOG.md`.

1. **라벨 CSS 가 `…` 로 잘라 보여 주고 있었다** — 번역은 다 왔는데 안 보여줬다. 줄바꿈 허용
2. **`service.toml` 이 `config.py` 기본값을 덮고 있었다** — 재시작해도 32 그대로
3. **상한은 지연을 묶는 값이 아니었다** — 64 위로 평평. 256 으로 상향

그 위에 **manga-ocr 의 구조적 한계**가 있었다. 긴 다열 블록에서 자기회귀가
드리프트해 후반이 붕괴한다. 확대로는 안 고쳐진다.

**세로쓰기 블록을 열로 쪼개 OCR 한다** (`ocr.py` 의 `split_columns`).
후리가나 열은 폭으로 걸러내고, 우→좌로 이어 붙인다. 3열 미만은 쪼개지 않는다.
지연 변화 없음 (484 → 493ms). 테스트 9개 추가.


---

## 박스 단위 손보기 (2026-07-27 밤)

검출·OCR·번역 중 하나만 틀려도 박스 하나가 통째로 못 쓰게 된다. 우클릭 메뉴로
그 박스만 손볼 수 있게 했다: 원문 보기 / 다시 읽기 / 크기 조정 / 지우기 /
걸러진 것 보기.

- **크기 조정** — 손잡이 8개. 놓으면 바로 다시 읽는다 (조정만 하고 끝내면
  박스만 커지고 원문은 그대로라 소용이 없다). 손잡이는 고른 박스 하나에만 붙인다
- **원문 보기** — 번역이 라벨을 덮어쓰므로 `dataset.ja` 에 원문을 보관해 둔다
- **`no_cache`** — 손으로 고른 읽기(영역·다시 읽기)는 서버 캐시를 건너뛴다.
  이게 없으면 "고쳤는데 여전하다" 가 난다 (실제로 났다)


---

## 캐시 관리 · 음성 (2026-07-28)

**캐시 지우기 두 갈래.** `POST /cache/purge` 로 `{all:true}` 또는 `{phash, profile}`.
페이지 단위는 조회와 **같은 퍼지 기준**으로 지운다 — 정확 일치만 지우면 캡처가 매번
몇 비트씩 달라서 정작 보고 있는 페이지가 안 지워진다.

- 패널 「갱신」 = 그 페이지만 지우고 다시 읽기 (`refresh` 플래그, 새 결과는 저장)
- 옵션 화면 「캐시 전부 지우기」 = 전부 (되돌릴 수 없어 한 번 묻는다)

`no_cache`(부분 크롭: 읽지도 쓰지도 않음)와 `refresh`(전체 페이지: 지우고 다시 읽고
저장)를 나눠 뒀다. 쓰임이 반대다.

**`fuzzy_hamming` 20 → 10.** 실사용 캐시에서 **다른 두 페이지가 거리 24** 로 붙어
있었다. 픽스처 8장은 전부 다른 작품이라 서로 100 이상 떨어졌지만, 같은 작품의
연속된 두 장은 칸 배치가 닮는다. 같은 페이지는 6 이하로 측정됐으므로 10 이면
적중은 그대로고 오적중 여유가 4 → 14 로 는다.

**윈도우 TTS.** 일본어 음성이 기본으로 없다 (실측: Heami/ko, Zira·David/en).
음성이 원문을 읽으므로 그대로면 무음이라, **일본어 음성이 없으면 번역문을 읽도록**
대체 경로를 뒀다.
