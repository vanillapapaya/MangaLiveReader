# 검토 요청 — 2026-07-27 야간 작업

자는 동안 M2(번역 + 캐시 + SSE) 골격까지 진행했다. **판단이 필요한 것만** 여기 모았다.
경과와 근거는 `PROGRESS.md`, 코드 의도는 각 모듈 주석에 있다.

---

## 1. 노출된 Gemini 키 — 폐기했는지 확인 필요 ⚠️

작업 중 내가 만든 파싱 에러 메시지가 키 파일의 줄 내용을 그대로 출력해서 **Gemini API
키 하나가 대화 기록에 노출됐다.** `AIza...` 로 시작하던 키다.

폐기하셨다고 이해했고 이후 새 키로 정상 동작을 확인했다. 혹시 아직이면
[AI Studio](https://aistudio.google.com/apikey)에서 삭제할 것.

에러 메시지에서 줄 내용을 빼는 수정은 했다 (`env.py`). 같은 실수는 재발하지 않는다.

---

## 2. DESIGN.md — v4 로 고쳤다 ✅

측정으로 뒤집힌 전제 **4개**를 사실로 교체하고, 머리말에 v3→v4 변경 이력 표를 넣었다.
원본은 `/mnt/c/tmp/DESIGN.v3.bak` 에 있다. 아래는 무엇을 왜 고쳤는지의 요약이다.

### §3.1 `is_bubble` — 검출기가 주지 않는다

yolo 클래스는 말풍선 여부가 아니라 **언어**다. `mask.py` 가 직접 판정한다.

### §8.2 LLM 읽기 순서 교정 → 삭제

XY-cut 36/45 인데 LLM 을 태우면 26~33/45 로 **내려간다.** 이미 맞은 페이지를 망가뜨린다.
좌표를 주든 안 주든 실패했다 (`PROGRESS.md` §9에 이유). 코드에서 걷어냈다.

### §10 지연 예산 → 원문/번역 분리로 재작성

번역이 페이지당 7-9초다. `effort` 를 낮춰도 안 줄어든다 — thinking 이 아니라 출력
450 토큰을 생성하는 시간이다. 스트리밍 후 실측: 원문 343ms, 첫 번역 4.8초, 전체 7.5초.

"1초 이내" 를 유지하려면 페이지당 1회 호출을 포기해야 하는데, 그러면 문맥이 끊겨
§8.4 의 전제가 무너진다. 지연을 받아들이고 **원문 표시(1초)와 번역 도착(5초)을 분리한
예산**으로 다시 썼다.

### §8.4 2계층 모델 → Sonnet 5 단일

Haiku 4.5 는 (1) 캐시 최소 길이 4096 토큰이라 번역 프롬프트가 **조용히 캐시되지 않고**
(2) `effort`/adaptive thinking 을 400 으로 거부한다. 2계층을 정당화할 측정도 없다.
지금은 둘 다 Sonnet 5 로 두었다.

---

## 3. 모델 확정 — Sonnet 5 로 두었다

6장 A/B 결과:

| | 지연 중앙값 | 출력 토큰 | 200쪽 1권 |
|---|---|---|---|
| **claude-sonnet-5** | **8.7초** | **444** | **$1.64** |
| gemini-3.6-flash | 11.0초 | 1,477 | $2.44 |

Gemini 는 단가가 절반인데 사고 토큰이 3.3배라 실제로 더 비싸고 느리다.

**번역 품질은 아직 사람이 안 봤다.** `test/ab/report_natural.md` 에 6장이 원문과 나란히
있다. 훑어보시고 Gemini 쪽이 뚜렷하게 나으면 말씀해 달라 — 되돌리는 건 설정 한 줄이다.
내가 본 범위에서는 둘 다 의미 오류가 없고 문체 성향만 달랐다 (Sonnet 반말, Gemini 정중체).

---

## 4. 번역 스트리밍 — 완료 ✅

"클로드로 해" 하신 뒤 이어서 붙였다. 실측:

| | 원문 | 첫 번역 | 전체 |
|---|---|---|---|
| 전 | 490ms | 9,790ms | 9,801ms |
| **후** | **343ms** | **4,819ms** | 7,486ms |

첫 번역이 **절반**으로 줄었고, 이후 말풍선이 300-700ms 간격으로 하나씩 채워진다.

**적어둔 트레이드오프는 피했다.** JSONL 로 바꿔 스키마 강제를 포기할 필요가 없었다 —
스트리밍 JSON 에서 중괄호를 세어 완성된 원소만 뽑는 파서(`jsonstream.py`)를 쓰면
스키마를 유지한 채로 증분 처리가 된다. 1바이트부터 64바이트까지 어떤 청크 경계에서도
같은 결과가 나오는 걸 테스트로 고정했다 (16개).

---

## 5. ~~`is_bubble` 오분류 (스크린톤)~~ — 해결됨

브라우저 확장으로 실물을 돌려 보니 **스크린톤(점무늬) 배경 위의 말풍선이
`is_bubble = false` 로 오판된다.** sunday-webry 한 페이지에서 5개.

`mask.py` 가 글자 획 주위 링의 균일도로 판정하는데 점무늬가 링에 섞여 "그림 위" 로
읽힌다. `include_sfx = false` 로 필터하면 그 대사들이 통째로 사라지고(17 vs 22),
§7.4 에 따라 L3 채우기도 영구히 못 한다.

**근본 수정을 네 번 시도했고 전부 실패했다** (`DEVLOG.md` 에 수치). 배경 통계로는
"스크린톤 위 작은 말풍선" 과 "그림 위 캡션" 이 구별되지 않는다 — 둘 다 같은 모양이다.
말풍선 경계를 실제로 분할해야 하는데 검출기는 글자 획 마스크만 준다.

**3안(번역 단계에서 LLM 이 분류)으로 해결했다.** 응답에 `kind` 를 추가했다 —
`dialogue` / `sfx` / `narration` / `extra`. 확장은 `sfx` 와 `extra` 만 숨긴다
(`narration` 은 이야기의 일부라 숨기면 내용이 끊긴다). Alt+Shift+S 로 되돌린다.

실물 4장 52 region 에서 **`kind` 52/52, `is_bubble` 39/52.** 추가 비용은 출력 토큰
몇 개뿐이고 지연은 그대로다. 오히려 `is_bubble` 을 모델 입력에서 뺐더니 입력 토큰이
16% 줄었다.

**당신이 확인할 것:** 실제로 읽어 보고 숨김 판정이 거슬리는지. 나레이션과 광고의
경계가 애매한 경우가 있다 (comic-walker 홍보 문구 「湯が繋ぐ、生き方と恋の物語ーー」
를 `narration` 으로 봤다 — 안 숨긴다). 거슬리면 프롬프트의 경계 설명을 조이면 된다.

## 6. 그 밖에 결정이 필요한 것

- **`[profiles.*]` 가 세 사이트뿐이다.** 픽스처는 6사이트(ichicomi, comic-fuz,
  sunday-webry 추가)인데 프로필이 없다. `capture_region` 은 화면 배치에 달려 있어
  내가 정할 수 없다
- **`service.toml [server]` 가 아직 M1 개발 설정이다** (`auth_disabled = true`,
  `dev_bind_loopback = true`). M3 클라이언트 붙이기 전에 뒤집어야 한다
- **캐시 정리(`purge_expired`)를 아무도 안 부른다.** 작업 스케줄러에 걸지, 기동 시
  한 번 부를지 정해야 한다

---

## 오늘 작업 요약

| | |
|---|---|
| 테스트 | **131 통과** / 31 스킵 / 1 xfail (4.6초, GPU 불필요) |
| 새 모듈 | `translate.py` `cache.py` `env.py` `jsonstream.py` |
| 새 스크립트 | `ab_translate.py` `check_keys.py` |
| 종단 검증 | 실제 페이지 SSE — 원문 343ms, 첫 번역 4.8초, 캐시 적중 7ms |

되돌리고 싶은 게 있으면 말씀해 달라. 특히 §8.2 폐기와 모델 선택은 되돌리기 쉽다.
