# 개발일지

실험과 그 결과를 시간순으로 적는다. **측정값과 그것이 뒤집은 판단**을 남기는 것이
목적이다 — 같은 실험을 두 번 하지 않기 위해서다.

- `DESIGN.md` — 무엇을 왜 만드는가 (명세)
- `PROGRESS.md` — 지금 어디까지 왔고 무엇이 남았는가
- `REVIEW.md` — 사용자 판단이 필요한 것
- **`DEVLOG.md`(이 문서)** — 무엇을 재봤고 무엇이 나왔는가

실패한 실험도 적는다. "이 방향은 이미 재봤고 안 됐다"가 이 문서의 절반이다.

---

## 2026-07-27

### M1 실물 검증 — 픽스처 구축

브라우저 "전체 페이지 캡처" 52장(6사이트). 뷰어 영역만 잘라내는 `crop_viewer.py` 작성.

**실패 1 — 뷰어 폭 전체를 남겼더니 검출이 무너졌다.** comic-walker 는 만화가 좌측
900px 에만 있고 나머지는 빈 페이지인데, 검출기가 입력을 1024px 로 줄이므로 만화가
400px 로 쪼그라들어 글자를 놓쳤다. 내용 영역까지 좁히도록 수정.

**실패 2 — 뷰어 밴드 하단 자동 탐지.** 국소 분산 연결요소로 잡으려 했으나 사이트
본문의 썸네일·광고와 이어져 실패. 행 표준편차의 첫 빈 띠로 바꾸니 손으로 확인한 네
사이트 값을 5-43px 오차로 재현. comic-fuz 만 만화와 본문 간격이 20px 뿐이라 예외 표에 둠.

### 검출 재현율

**51/61 = 84%** (효과음 제외). 놓친 10개 중 6개가 말풍선 밖 여백 글자.

**효과음은 0개 검출.** `detect_threshold` 0.35 → 0.12 로 낮춰도 6→6, 9→9, 2→3 으로
변화 없음 — 임계값 문제가 아니라 **YOLO 블록 헤드가 그런 글자를 제안하지 않는 것**.
반면 세그멘테이션 마스크에는 선명히 찍혀 있었다. → 마스크 승격 구현(§5).

승격 후 누락 10건 중 9건 회수. 노이즈를 가르는 신호는 **이진화 전 마스크 원본값**
(진짜 글자 193-221, 그림 오검출 85-142). 이진화하면 이 차이가 사라진다.

**남은 한계:** `yanmaga_002528` 의 「ピンポーン」(외곽선 흰 글자)과 「ガチャ」(보라색)는
마스크에 **아예 없다.** 승격으로 못 건진다.

### 읽기 순서 — 컬럼 밴드 폐기

`DESIGN.md` §8.2 의 컬럼 밴드 휴리스틱이 실물에서 **51개 중 26개(51%) 실패.**
"복잡한 페이지에서 10-15%" 예상보다 훨씬 나빴고 쉬운 페이지에서도 틀렸다.

원인: x 중심으로만 묶어 **칸 행 개념이 없다.** `yanmaga_002528` 에서 상단 「蜻蛉高校」
(x=1113)와 하단 「ん？」(x=1103)이 같은 컬럼으로 묶였다 — x 차이가 10px 이라 어떤
밴드 폭으로도 못 고친다.

재귀 XY-cut 으로 교체. **방향은 간격 크기가 아니라 구조의 위계로 정한다** — "가장 넓은
틈"으로 고르면 한 행 안 좌우 말풍선 간격(900px)이 행 사이 간격(230px)보다 넓어 행을
가로지른다.

| | 위치 정확도 | 쌍 정확도 |
|---|---|---|
| 컬럼 밴드 | 25/51 = 49% | — |
| 재귀 XY-cut | **46/51 = 90%** | 162/166 = 98% |

### 박스 병합 — 절대 상한이 없었다

52장에서 **간격 병합 30건 중 24건이 30px 초과였고 눈으로 확인한 것은 전부 오류.**
`ichicomi_030024` 는 서로 다른 세 칸의 외침 말풍선을 페이지 높이만큼 한 박스로 묶었다.

두 단계로 고침:

1. **축이 틀렸다** — 세로 열을 붙일 때 허용 간격을 박스 *높이*에서 뽑았다. 세로쓰기
   박스는 길쭉해서 높이 450px 이면 가로로 270px 까지 손을 뻗는다. 벌어진 방향의
   크기에서 뽑도록 대칭으로 수정
2. **비례항만으로는 부족하다** — 0.3 까지 조이면 큰 오탐은 죽지만 정당한 줄 병합도
   같이 죽는다(`ichicomi_030013` 은 간격 12px 인데 박스 높이가 34px). 한 말풍선 안
   줄·열 간격은 박스 크기가 아니라 **페이지 해상도**에 묶이는 값이라 절대 상한을 씌움

결과: 박스 451 → 431, 병합 19건(겹침 13 + 간격 6, 전부 30px 이하). 펼침면 경계를
넘는 병합 0건.

**정정:** 8장만 보고 "정당한 병합 0건"이라 기록했으나 52장에서는 겹침 병합 13건이
전부 정당했다. 검출기가 같은 덩어리에 박스를 겹쳐 내는 경우가 꾸준히 있다.

### 지연 측정 — GPU 경합에 두 번 속았다

같은 코드·같은 입력의 OCR 지연이 **53ms ↔ 680ms 로 13배** 흔들렸다. 원인은 그림
생성 워크로드와의 **SM 경합**이다.

`DESIGN.md` §9.3 은 이걸 VRAM 문제로 다루는데 **틀렸다.** 그림 생성이 유휴일 때
VRAM 은 7.8/16.3GB 가 잡혀 있어도 우리 지연은 정상이었다. `ttl_seconds` 로 모델을
내려도 해결되지 않는다.

WSL 에서는 `nvidia-smi --query-compute-apps` 가 항상 비어 나온다 — Windows 프로세스가
안 보이는 것뿐이니 utilization/power 를 볼 것.

유휴 기준값(52장): detect+mask 28ms · ocr 228ms · total 257ms.

### OCR 지연은 region 개수가 아니라 **생성 길이**가 정한다

| 페이지 | region | 최장 출력 | ocr |
|---|---|---|---|
| shonenjumpplus_002430 | 6 | 10자 | 78ms |
| sunday-webry_002601 | 11 | 36자 | 305ms |
| sunday-webry_002556 | **2** | 46자 | **166ms** |
| comic-walker_002635 | 6 | 60자 | 332ms |

region 2개짜리가 11개짜리보다 2배 느리다. manga-ocr 은 자기회귀라 배치에서 가장 긴
출력이 전체를 붙잡는다. 긴 출력은 전부 깨알 소문자 뭉치에서 나온 **쓰레기**였다.

**OCR 전에 가려낼 방법을 못 찾았다.** 마스크에서 글자 크기를 잴 수 없다 — 획 두께가
쓰레기 6.9/5.8/5.4, 정상 5.5/5.2/5.1 로 평평하다(마스크가 검출기 해상도의 소프트
세그멘테이션이라 원본 글자 크기가 뭉개진다). 면적 대비 글자 밀도도 안 갈린다.

대신 **생성 상한(32자)에 부딪힌 것 자체를 신호로** 썼다. 상한을 다 쓰고도 끝을 못
찾았고 말풍선 밖이면 버린다. 말풍선 안이면 자르기만 한다(진짜 긴 대사를 통째로 잃는
것이 잘린 원문보다 나쁘다. 실측 정상 대사 최장 26자).

버려진 3개 전부 진짜 쓰레기, 정상 원문 손실 0.

---

## 번역

### 모델 A/B — Gemini 가 단가는 절반인데 더 비싸다

같은 6장을 두 모델에 태웠다 (`scripts/ab_translate.py`).

| | 지연 중앙값 | 입력 | 출력 | 200쪽 1권 |
|---|---|---|---|---|
| **claude-sonnet-5** | **8.7초** | 516 | **444** | **$1.64** |
| gemini-3.6-flash | 11.0초 | 752 | 1,477 | $2.44 |

출력 토큰이 3.3배다 — 대부분 `total_thought_tokens`. 번역문 길이는 비슷하니 순수하게
생각을 더 한다. Anthropic 은 thinking 을 `output_tokens` 에 포함해 주므로, 비교하려면
Gemini 쪽에 사고 토큰을 더해야 한다.

**`effort` 는 지연에 거의 영향이 없다.** low ↔ medium 이 8-9초로 같다. thinking 이
아니라 출력 450토큰을 생성하는 시간이라 그렇다.

### LLM 읽기 순서 교정 — 역효과, 폐기

`DESIGN.md` §8.2 의 완화책. **두 방향 모두 실패했다.**

| | 순서 정확도 (4장 45 region) |
|---|---|
| XY-cut | **36/45** |
| gemini-3.6-flash | 33/45 |
| claude-sonnet-5 | 26/45 |

`yanmaga_002535` 는 XY-cut 이 10/10 맞힌 것을 Sonnet 이 2/10 으로 망가뜨렸다.
정작 고쳐야 할 `comic-fuz_030111`(4/13)은 아무도 못 고쳤다.

- **좌표를 안 주면** 판단 근거가 없어 입력 순서를 그대로 베낀다
- **좌표와 기하 규칙을 주면** XY-cut 을 LLM 더러 다시 구현하라는 요구가 되어
  결정론적 알고리즘보다 못한다

LLM 의 강점은 기하가 아니라 대사 흐름인데, 그걸 쓰려면 좌표가 필요하고 좌표를 주면
기하로 풀려 든다. 응답 스키마에서 `reading_order` 제거.

### 번역 스트리밍 — 예고한 트레이드오프를 피했다

첫 번역 9,790ms → **4,819ms**.

JSONL 로 바꿔 JSON 스키마 강제를 포기해야 하나 고민했으나 필요 없었다. 스트리밍 JSON
에서 중괄호를 세어 완성된 배열 원소만 뽑는 파서(`jsonstream.py`)면 스키마를 유지한 채
증분 처리가 된다. 1바이트~64바이트 어떤 청크 경계에서도 같은 결과가 나오는 것을
테스트로 고정(16개).

---

## 브라우저 확장 스파이크

`DESIGN.md` 부록 A 는 이 경로를 **미검증** 상태로 기각했다. 근거 둘 다 약했다:

1. "Vivaldi 내장 캡처가 모든 페이지에서 실패" — 그건 Vivaldi 의 스크린샷 기능이지
   확장의 `chrome.tabs.captureVisibleTab` 이 아니다
2. "만화 읽을 때만 크롬을 강제할 위험" — Vivaldi 는 Chromium 기반이라 크롬 확장이
   그대로 돈다

### 결과: 경로가 검증됐다

| | |
|---|---|
| `captureVisibleTab` | ✅ canvas 뷰어에서도 픽셀이 나온다 |
| 서비스 왕복 | ✅ 원문·번역 도착 |
| **좌표 정확도** | ✅ **번역이 해당 말풍선에 정확히 얹힘** |

3번이 핵심이다. `DESIGN.md` §6 이 ±4px 예산을 통째로 쓰는 좌표 역환산이 **불필요**
하다는 실증. DOM 이 뷰어 사각형을 알려주고 `scale`·`dpr` 을 우리가 정하니 추측이 없다.

### 뷰어 요소 탐지 — 두 번 틀렸다

**실패 1 — "같은 부모의 형제" 로 묶기.** 펼침면은 각 페이지가 별도 div 로 감싸여
있어 형제가 아니다. DOM 구조는 사이트마다 다르므로 기대면 안 된다.

**실패 2 — 면적 최대를 씨앗으로.** 세로 스크롤 뷰어는 펼침면을 위아래로 쌓기도 한다.
뷰포트 중앙 최근접으로 바꿈.

실측(sunday-webry): 페이지가 837×1316 canvas 로 **가로 캐러셀**에 늘어서 있다.
보이는 것은 x=42 와 x=879 둘, 나머지는 x 가 음수이거나 1758(화면 밖).
화면 밖을 걸러내고 둘을 합치면 x 42..1716, 폭 1674 — 펼침면 하나가 정확히 나온다.

---

## `is_bubble` 오분류 (스크린톤) — 우회로 해결

브라우저 확장으로 실물을 돌려서 발견. 8장 픽스처에서는 "오분류 없음"이었는데
뒤집혔다 — 그 픽스처에 스크린톤 위 말풍선이 없었다.

sunday-webry 한 페이지에서 **명백한 말풍선 5개가 `is_bubble = false`.**
`include_sfx = false` 로 필터하면 통째로 사라진다 (17개 vs 22개).

### 어느 조건에서 탈락하는가

셋 중 `stddev` 하나에서만 탈락한다. 밝기(246-255)·채도(0-0.09)는 전부 통과.

```
오분류   41.0  54.6  44.0  48.5     ← 문턱 40
정상     13.2  15.3  10.9  31.0
```

### 시도 1 — 블러 후 표준편차 (실패)

스크린톤은 고주파니 흐리면 균일해질 것이라 봤다. σ=3 가우시안 후:
오분류 21.8~30.9 vs 정상 8.6~17.9. 갈리긴 하나 **여유가 4뿐**이라 위태롭다.

### 시도 2 — 진짜 효과음까지 넣고 비교 (실패, 결정적)

반대 방향 오류(그림 위 텍스트를 말풍선으로 오판 → L3 가 그림을 지움)가 훨씬
위험하므로 C 집단(진짜 효과음, False 가 맞는 것)을 같이 놓아야 한다.

| | A(말풍선인데 False) | C(효과음, False 맞음) |
|---|---|---|
| `sd` | 41.0~54.6 | **28.0**~63.9 |
| `blur_sd` | 21.8~30.9 | **21.8**~54.6 |
| outlier 비율 | 0.05~0.18 | 0.11~0.54 |
| IQR | 0~17 | 1~114 |

**C 의 최솟값이 A 범위 안에 들어온다.** 어떤 문턱을 잡아도 한쪽이 깨진다.

### 시도 3 — 링 폭 스윕 (실패)

가설: 링이 12px 라 말풍선 테두리를 넘어 바깥 스크린톤을 물고 있다. 좁히면 갈릴 것.

```
             ring3  ring5  ring8  ring12
A 말풍선       1.5    1.2   11.2   44.0
C 효과음       2.5    2.2    1.9   62.4
```

**둘 다 같은 모양**이다 — 가까이는 균일, 멀어지면 튄다. 작은 말풍선이 스크린톤 위에
있는 것과 캡션 박스가 그림 위에 있는 것이 링 통계로는 구별되지 않는다.

### 시도 4 — flood fill 로 '닫힌 영역' 찾기 (실패)

통계 대신 위상을 보기로 했다. 글자 획 바로 바깥에서 flood fill 을 시작해, 채워진
영역이 패치 테두리에 얼마나 닿는지(`touch`) 잰다. 말풍선이면 테두리에 막혀 갇히고,
그림 위면 새어 나갈 것이라 봤다.

| 집단 | touch |
|---|---|
| A (말풍선인데 False) | 0.000 · 0.000 · 0.000 · 0.000 |
| B (말풍선, True 맞음) | 0.000 · 0.111 · 0.000 · 0.000 |
| C (효과음, False 맞음) | 0.081 · 0.217 · (측정불가) · **0.000** |
| D (말풍선, True 맞음) | 대부분 0.000, 그러나 **0.459 · 0.894** |

A 는 전부 0 으로 깔끔한데 C 에도 0 이 있고(표지 로고 — 제 모양에 갇힌다) D 에는
0.459·0.894 가 있다(큰 말풍선이라 패치를 넘어간다). **역시 안 갈린다.**

### 결론: 이 신호로는 못 고친다 — 문제를 잘못 잡았다

네 번 재봤고 전부 실패했다. 배경 통계로는 **"스크린톤 위 작은 말풍선"과 "그림 위
캡션"이 구별되지 않는다.** 그럴 만하다 — 둘 다 "글자 주변은 균일하고 조금 벗어나면
다른 것이 있다" 는 같은 모양이다. 말풍선 경계를 실제로 분할하지 않는 한 답이 없고,
검출기는 글자 획 마스크만 준다.

**진짜 문제는 `is_bubble` 하나가 두 가지 질문에 답하려 한다는 것이다.**

| 쓰는 곳 | 실제 질문 | 틀렸을 때 |
|---|---|---|
| `include_sfx` 필터 (§8.1) | 이게 말풍선인가 효과음인가 | **대사가 사라진다** |
| L3 안전도 (§7.4) | 여기를 칠해도 그림이 안 지워지나 | 회색 박스가 그림을 덮는다 |

두 번째는 배경 통계로 **답할 수 있다** (균일하면 칠해도 안전). 첫 번째는 **답할 수
없다.** 그런데 지금은 하나의 불리언이 둘 다 하고, 첫 번째의 실패가 두 번째까지
망가뜨린다 — `fill_confidence` 가 `is_bubble` 이 False 면 무조건 0 을 낸다.

**조치**

1. `include_sfx` 를 켜 둔다 (확장에서 적용). 효과음이 섞이는 것보다 대사가 빠지는
   쪽이 훨씬 나쁘다
2. **`is_bubble` 을 하드 필터로 쓰지 않는다.** 힌트로만 취급할 것
3. `fill_confidence` 의 `is_bubble` 하드 게이트는 재검토 대상이다. 같은 증거(채도·
   표준편차)를 두 번 세고 있어서, 스크린톤 말풍선이 0.5 쯤 받아야 할 자리에 0 을
   받는다. L3 는 M5 라 지금 급하지 않지만 그때 반드시 볼 것


---

## `kind` 분류 — 위 문제의 해결

`is_bubble` 을 못 고치니 **필터를 다른 곳으로 옮겼다.** 번역 모델이 어차피 원문을
읽으니, 거기서 `dialogue`/`sfx`/`narration`/`extra` 를 같이 내게 한다. 추가 호출 없음.

### 실패 — `bubble` 힌트를 같이 주면 모델이 그걸 따라간다

처음엔 입력에 `bubble` 을 남겨 두고 프롬프트에 "참고만 하고 원문을 보고 판단하라" 고
적었다. **안 먹혔다.** sunday-webry 22 region 에서:

| id | 원문 | bubble | kind |
|---|---|---|---|
| 0 | あーあ…ドジすぎ痛いやつ。 | false | **caption** ❌ |
| 3 | …でもビミョーに… | false | **caption** ❌ |
| 8 | い、家で録ったやつ…曲も自作で… | false | **caption** ❌ |
| 18 | そ、そうだったありがー | false | **caption** ❌ |

전부 명백한 대사다 — 8번은 더듬는 말투(「い、家で…」)까지 있다. 틀린 힌트 하나가
원문의 증거를 이겼다.

### 성공 — 힌트를 빼면 원문만 보고 판단한다

`build_input` 에서 `bubble` 을 제거하니 넷 다 `dialogue` 로 돌아왔다.
입력 토큰도 940 → 786 (-16%).

**교훈: 틀리는 신호는 "참고용" 으로도 주면 안 된다.** 모델은 싼 신호를 먼저 쓴다.

### 실패 — `caption` 하나로는 부족했다

3분류(`dialogue`/`sfx`/`caption`)로 픽스처를 돌리니 `caption` 에 성격이 다른 둘이
섞였다.

- **이야기**: 「一日は銃声から始まる」「彼女は小さな秘湯の一人娘だった」「休み時間」
- **잡문**: 「＞＞48 2017年2月」(사이트 UI) 「フラウキンド…ショップの」(광고)

`caption` 을 숨기는 기본값이면 **나레이션이 사라진다.** `narration` / `extra` 로 쪼갰다.
숨기는 것은 `sfx` + `extra` 뿐.

### 결과 — 4장 52 region

| | 정확도 |
|---|---|
| `kind` (번역 모델) | **52/52** |
| `is_bubble` (배경 통계) | 39/52 |

`is_bubble` 은 **양방향으로** 틀린다: 스크린톤 말풍선을 효과음으로(4건),
나레이션 상자를 말풍선으로(9건). `kind` 는 두 방향 다 맞혔다.

비용은 출력 토큰 몇 개뿐이다 (945 vs 946 — 오차 범위). 지연 변화 없음.


---

## WSL 에서 서비스 띄우기

윈도우 cmd 대신 WSL 에서 돌리려고 리눅스 환경을 따로 만들었다. GPU 는 문제가
아니었고 (WSL2 가 `/usr/lib/wsl/lib/libcuda.so` 로 넘겨준다) 걸린 것은 셋이다.

### venv 를 `/mnt/c` 에 두면 안 된다 — import 가 18배 느리다

같은 venv 를 두 곳에 만들고 `import torch` 를 재 봤다.

| 위치 | real | user+sys |
|---|---|---|
| `/mnt/c/.../.venv-linux` (drvfs) | **35.9초** | 5.8초 |
| `~/.venvs/mlr` (ext4) | **2.0초** | 2.7초 |

user+sys 는 양쪽이 같다 — **33초가 전부 I/O 대기**다. venv 는 파일이 수만 개이고
drvfs 는 파일 하나하나가 비싸다. 소스는 `/mnt/c` 에 둬도 된다 (파일이 몇십 개뿐).

`uv` 캐시가 ext4 에 있어서 옮겨 만드는 데 재다운로드는 없었다.

### Python 3.14 로는 안 된다

WSL 기본이 3.14.4 인데 `requires-python = ">=3.11,<3.12"` 다. `uv sync --python 3.11`
로 uv 가 직접 받게 했다.

### 키 파일이 윈도우 프로필에 있다

`env.py` 의 기본 경로는 `~/.config/mangalivereader/env` 인데 WSL 의 `~` 는
`/home/<사용자>` 라 비어 있다. 실제 파일은
`/mnt/c/Users/<사용자>/.config/mangalivereader/env` 다.

복사하면 키가 두 군데가 되어 나중에 하나만 갈게 된다. `run-service.sh` 가
`MTL_ENV_FILE` 로 윈도우 쪽 파일을 가리키게 했다.

### 네트워크 — WSL 은 윈도우의 Tailscale 을 못 본다

WSL 은 NAT 뒤(172.30.44.194)라 `find_tailscale_ip()` 가 None 을 내고 기동을 거부한다
(`bind_tailscale_only = true`). 셋 중 하나를 골라야 한다:

| | 맥북 접속 | 비용 |
|---|---|---|
| **루프백** (택함) | ✗ | 없음. WSL2 가 localhost 를 윈도우로 전달하니 윈도우 브라우저는 그대로 붙는다 |
| WSL 에 Tailscale 설치 | ✓ | sudo 필요. tailnet 노드가 하나 는다 |
| 미러드 네트워킹 | ✓ | `wsl --shutdown` 필요 — 원격 세션이 끊긴다 |

맥북 테스트는 윈도우에서 서비스를 띄우는 쪽으로 한다 (`dev_bind_loopback = false`).

### 실측 — WSL 이 윈도우보다 느리지 않다

`shonenjumpplus_002430` 한 장:

| | detect+mask | ocr | translate |
|---|---|---|---|
| 윈도우 기준선 | 28ms | 228ms | — |
| **WSL** | **34ms** | **185ms** | 6,845ms |

GPU 경로에 손해가 없다. 테스트도 138 통과로 같다.


---

## 드래그로 영역 고르기 (`Alt+Shift+D`)

`probeViewer()` 는 `img`/`canvas` 중 200px 이상인 것을 기하로 묶는 휴리스틱이다.
반드시 틀리는 사이트가 나온다 — 타일로 쪼개진 뷰어, 배경 이미지로 그린 만화,
만화보다 큰 광고. 손으로 집을 수단이 없으면 그 사이트는 통째로 못 쓴다.

좌표계는 `probeViewer()` 와 같은 뷰포트 CSS 픽셀이라 `toCss()` 가 그대로 돈다.
`run(tab, override)` 로 probe 를 건너뛰는 것 말고는 파이프라인이 같다.

### 붙이면서 걸린 것

**덮개가 포인터 이벤트를 먹어야 한다.** `#mlr-overlay` 는 `pointer-events: none` 로
클릭을 통과시키는데(뷰어를 계속 조작해야 하니까) 선택 덮개는 반대다. canvas 뷰어는
자기 드래그 핸들러(페이지 넘김)를 갖고 있어서 통과시키면 그쪽이 먼저 먹는다.

**덮개를 지우고 두 프레임 기다린 뒤 캡처를 요청한다.** 안 그러면 반투명 회색 층이
캡처에 찍혀 검출기가 그 너머의 글자를 보게 된다.

**MV3 서비스 워커에 응답을 기다리게 하면 안 된다.** 드래그는 몇 초가 걸리는데
워커는 30초 유휴로 죽는다. `sendMessage` 로 명령만 보내고, 다 고르면 콘텐츠
스크립트가 `read-rect` 로 되쏘는 구조로 했다.

### 긴 변 상한이 필요했다

정규화가 **짧은 변**만 보고 배율을 정한다. 뷰어 전체는 이미 1200 이상이라 안 걸리지만
드래그 선택은 늘 걸린다 — 말풍선 한 줄(1600×160)을 고르면 배율 7.5 로 12000×1200 이
된다. 업로드도 크고 검출기는 어차피 1024 로 줄여 본다. `MAX_LONG_SIDE = 2600` 추가.

| 선택 | 원래 | 결과 |
|---|---|---|
| 자동 탐지 펼침면 | 1674×1316 | 그대로 (scale 1.00) |
| 가는 가로 띠 | 1600×160 | 2600×260 (7.50 → **1.63**) |
| 말풍선 하나 | 300×200 | 1800×1200 (6.00) |

### 실측 — 드래그 크기 크롭 3종

| 선택 | 결과 | total |
|---|---|---|
| 가는 가로 띠 | 「一日は銃声から始まる」 정확 | 2,195ms |
| 말풍선 근처 | 「．．．．．．二ミリズレたか」 (원문 「ここミリズレたか」) | 3,142ms |
| **그림만 있는 칸** | 「ロスロネスロの」「★田」 **헛것** | 16,136ms |

셋째는 버그가 아니다. 크롭을 열어 보니 대사가 없고 양식화된 큰 검은 획(효과음 로고)
뿐인데, 검출기가 그걸 글자로 보고 manga-ocr 이 읽으려 든 것이다. 번역이 15.6초 걸린
것도 이미 아는 패턴이다 — 쓰레기 입력이 긴 출력을 만든다.

**"글자가 있는 영역을 골라라" 가 사용 지침이다.** 자동으로 막을 방법은 없다 —
OCR 전에 글자다움을 재는 방법을 이미 찾다 실패했다 (위 "OCR 지연" 절).


---

## 조작 패널과 페이지 전환 자동 감지

### 패널

단축키만 있으면 외워야 쓴다. 오버레이 오른쪽 위에 버튼 다섯을 뒀다 —
읽기 / 영역 / 자동 / 라벨 / 효과음.

**패널은 `#mlr-overlay` 안에 둔다.** 캡처 직전 `hide-overlay` 로 같이 숨어야
스크린샷에 버튼이 안 찍힌다. 밖에 두면 검출기가 버튼 글자를 대사로 읽는다.
대신 오버레이는 `pointer-events: none` (뷰어를 계속 조작해야 하니까) 이라
패널만 `auto` 로 되돌린다.

**상태가 있는 기능은 상태가 보여야 한다.** 자동·라벨·효과음은 켜지면 주황색이 된다.
자동 감지처럼 뒤에서 도는 것은 켜져 있는지 모르면 무섭다.

### 자동 감지 — DOM 만 봐서는 안 된다

canvas 뷰어는 페이지를 넘겨도 **DOM 이 안 바뀐다.** 다시 그릴 뿐이다. 반대로 SPA
뷰어는 입력 없이 DOM 만 바뀐다. 그래서 신호를 둘 다 받는다:

- 클릭·키(방향키·Space·PageUp/Down 등)·휠 — 사람이 넘기는 거의 모든 방법
- MutationObserver — 뷰어가 스스로 바꾸는 경우

**신호는 "바뀌었을 수 있다" 일 뿐이다.** 진짜 바뀌었는지는 background 가 캡처해서
해시로 판정한다. 안 그러면 스크롤 한 번에도 같은 페이지를 다시 읽는다.

주기적 폴링은 안 쓴다 — `captureVisibleTab` 은 비싸고 호출 제한이 있으며, 확인할
때마다 오버레이를 숨겼다 되살려야 해서 화면이 깜빡인다.

### 붙이면서 걸린 것

**MutationObserver 가 자기 자신을 물어 무한 루프가 된다.** 우리가 박스를 그리는 것도
DOM 변화다. `isOurs()` 로 오버레이·선택 덮개 안의 변화를 걸러낸다.

같은 이유로 **`mlr-show-extra` 클래스를 `<html>` 에서 오버레이로 옮겼다.** 효과음
토글이 `<html>` 의 class 를 건드리면 그게 "페이지가 바뀌었나" 신호로 잡힌다.

**MV3 서비스 워커가 죽으면 자동 감지가 조용히 멈춘다.** `watching` 이 메모리에만
있어서다. 버튼은 켜진 채인데 아무 일도 안 일어나는 최악의 실패다. 콘텐츠 스크립트는
워커가 죽어도 계속 신호를 보내므로, **그 신호를 근거로 상태를 되살린다.**

**캡처를 두 번 찍지 않는다.** `run()` 에서 `prepare()` 를 떼어내, 자동 감지가 판정에
쓴 캡처를 그대로 읽기에 넘긴다.

**수동으로 읽어도 기준 해시를 갱신한다.** 안 그러면 자동 감지가 다음 신호에서
"바뀌었다" 로 보고 방금 읽은 페이지를 또 읽는다.

### 남은 거친 곳

자동 감지가 확인할 때마다 오버레이가 잠깐 사라졌다 돌아온다 (캡처에 찍히면 안 되니까).
페이지를 넘기는 순간에는 어차피 박스가 새로 그려지므로 티가 안 나지만, **스크롤만 하고
페이지는 안 넘긴 경우에는 헛깜빡임이 보인다.**


---

## 영역 선택이 "아무것도 못 찾는" 이유 — 글자가 화면을 채우면 분포 밖

드래그로 고른 영역에서 번역이 안 나오는 경우가 있다는 제보. 재봤더니 **검출이 0** 이었다.

말풍선 하나(221×243)를 여백만 바꿔 잘라 보낸 결과:

| 여백 | 보낸 이미지 | 검출 |
|---|---|---|
| 0px | 1200×1319 | **0개** |
| 10px | 1200×1310 | **0개** ← 「다시 읽기」가 쓰던 값 |
| 30px | 1200×1294 | **0개** |
| 60px | 1229×1200 | **0개** |
| 120px | 1408×1200 | 1개 |
| 240px | 1640×1200 | 4개 |
| (페이지 전체) | 1668×1211 | 15개 |

**확대 배율 탓이 아니다.** 같은 창을 확대해서/안 해서 각각 넣어 봤는데 결과가 같았다.
작은 글자(77×18)는 300px 창에서도 잘 찾는다. 문제는 절대 크기가 아니라 **글자가
차지하는 비율**이다 — 검출기가 만화 페이지 전체로 학습돼서, 글자가 화면을 크게
차지하면 본 적 없는 그림이 된다.

### 배율 상수

| 넓힌 배율 | 선택이 차지하는 폭 | 검출 |
|---|---|---|
| 1.0배 | 100% | 0개 |
| 1.5배 | 67% | 0개 |
| 2.0배 | 50% | 0개 |
| 2.5배 | 40% | 2개 |
| **3.0배** | **33%** | **3개** |

40% 아래로 내려가야 살아난다. **3배**를 택했다.

### 고친 방식 — 넓게 보내고 좁게 남긴다

사용자가 고른 범위를 바꾸지 않는다. **검출기에만 문맥을 주고**, 돌아온 region 중
고른 범위 밖(중심 기준)의 것을 버린다. 경계에 걸친 박스까지 버리면 정작 고른
말풍선이 사라지므로 중심으로 판정한다.

**실측 (3장, 대사 17개):**

| | 되찾음 |
|---|---|
| 넓히지 않음 | 6/17 = **35%** |
| 3배로 넓힘 | 16/17 = **94%** |

효과음·양식화 글자는 여전히 불안정하지만 그건 manga-ocr 의 원래 한계다
(위 "검출 재현율" 절 참조).

---

## 크레딧이 새던 진짜 구멍 — 캐시 키가 암호학적 해시였다

"중복 번역이 계속되면 크레딧 낭비" 제보. 자동 감지 오판을 의심했는데 **더 아래에
구멍이 있었다.**

서버는 `phash` 의 해밍 거리로 캐시를 맞춘다 (`cache.py` 의 `hamming()`,
`fuzzy_hamming = 3`). 그런데 확장은 **sha256** 을 보내고 있었다. 암호학적 해시는
1픽셀만 달라도 비트의 절반이 바뀐다 — 거리가 늘 ~128 이라 **퍼지 매칭이 원리적으로
절대 안 걸린다.** 바이트까지 똑같은 캡처가 아니면 매번 번역 API 를 새로 쳤다.

지각 해시(16×16 평균 해시, 256비트)를 16진수로 보내도록 바꿨다. 문턱은 실측으로 잡았다:

| | 해밍 거리 |
|---|---|
| 같은 페이지 (JPEG 재압축·1px 이동·밝기 ±4) | **최대 6** |
| 다른 페이지 (8장 28쌍) | **최소 100** |
| 문턱 | 20 |

이제 같은 페이지를 몇 번 읽어도 번역 비용이 0 이다. **자동 감지 판정이 틀려도 돈이
안 나간다** — 이게 근본 방어선이고, 아래 둘은 그 위에 얹은 것이다.

- 자동 감지의 "같은 화면인가" 판정도 지각 해시로 (광고 애니메이션에 안 속는다)
- `AUTO_MIN_INTERVAL_MS = 4000` — 자동으로는 4초에 한 번 이상 안 읽는다

## 생성 상한 32 가 긴 대사를 자르고 있었다

"6줄쯤 되는 건 번역이 제대로 안 되는 것 같다" 는 제보. 맞았다.

`max_text_length = 32` 를 정할 때 "8장 실측 정상 대사 최장 26자" 를 근거로 삼았는데
**그 표본에 긴 대사가 없었다.** 같은 8장을 64 로 다시 읽으니 32 에 걸려 잘린 region 이
3개, 그중 둘이 멀쩡한 대사다:

```
現実はどうだ！運動部やエリート共だけがモテる狩猟採集社会並の階級構造じゃねえか！   (40자)
俺はオダクとしてオタクのエリートになってみせる！なり可愛い彼女を作りこの陰の状態から脱出  (44자)
```

실측 최장 60자. 상한을 64 로 올렸다.

**교훈: "실측 최장" 은 표본이 그 현상을 담고 있을 때만 상한의 근거가 된다.**

## 효과음 박스가 툭 사라지는 것은 버그가 아니었다

검출 직후에는 초록으로 뜨고, 번역이 도착해야 `kind` 를 알 수 있다. 그때
`display: none` 이라 눈앞에서 사라져 버그처럼 보였다. 0.25초 페이드로 바꿨다.


---

## 긴 대사가 "번역되다 마는" 것 — 원인이 셋이었다

맥북 실사용 제보. 하나씩 벗겨 보니 서로 다른 층에 셋이 겹쳐 있었다.

### 1. 화면에서 잘렸다 (가장 큰 원인)

라벨 CSS 가 `white-space: nowrap` + `overflow: hidden` + `text-overflow: ellipsis`
였다. **번역은 멀쩡히 다 왔는데 `…` 로 잘라 보여 주고 있었다.** 줄바꿈을 허용했다.

### 2. `service.toml` 이 기본값을 덮고 있었다

`config.py` 의 기본값만 32 → 64 로 올리고 `service.toml` 의 `max_text_length = 32`
를 안 봤다. 재시작해도 그대로 32 였다.

**설정이 두 곳에 있으면 한쪽만 고치게 된다.** 다음에 값을 바꿀 때는 둘 다 볼 것.

### 3. 상한은 지연을 묶는 값이 아니었다

32 로 정할 때 "지연을 묶는 값" 이라 믿었는데 틀렸다. 8장으로 재보니:

| 상한 | ocr 평균 | ocr 최악 | 실제 길이 상위 |
|---|---|---|---|
| 32 | 337ms | 491ms | [32, 32, 32, 32, 30] ← 전부 상한에 걸림 |
| 64 | 478ms | 898ms | [60, 52, 51, 40, 30] |
| 128 | 473ms | 846ms | 변화 없음 |
| 256 | 484ms | 968ms | 변화 없음 |

**64 위로는 평평하다.** 상한은 모델이 실제로 그만큼 생성할 때만 비용이 되는데
실측 최장이 60자라 아무도 안 닿는다. 대사가 빽빽한 작품을 위해 256 으로 잡았다 — 공짜다.

---

## manga-ocr 은 긴 다열 블록에서 무너진다 — 열로 쪼개면 살아난다

상한을 올렸는데도 7열 90자짜리 말풍선이 제대로 안 나왔다. 상한 문제가 아니었다.

```
정답  ほっ本日は打合せにお誘いいただきありがとうございますッ！アカデミー一年B組の仲町あられと…
통짜  ほら本日は打合せにお誘惑いただきありがとうございますッ！神田へ馬アカテミー…
      …何卒よろしくおねが何をおね！！！        ← 후반이 반복·붕괴
```

앞 30자는 거의 정확한데 뒤로 갈수록 무너진다. **확대해도 안 고쳐진다** (1배·2배·3배
모두 같게 무너졌다) — 해상도가 아니라 길이가 원인이다. manga-ocr 은 말풍선 하나
분량의 짧은 블록으로 학습됐다.

### 열로 쪼개기

세로쓰기라 열 사이가 확실히 비어 있다. 잉크의 세로 투영에서 빈 띠로 가른다.

```
열 8개 검출
  ほっ本日は打合せにお誘いいただき          ← 완벽
  かんだしろやま                          ← 후리가나 (버린다)
  ありがとうございますッ！神田氏八馬        ← 白→氏 하나
  なかまち                                ← 후리가나 (버린다)
  アカデミー一年度組の仲町あられと          ← B→度 하나
  申します／色々ご迷惑おかけすることも      ← ！→／ 하나
  あるかと思いますが何卒よろしく           ← 완벽
  おねがいしままま走まひゅっ！！           ← ま→走 하나
```

**후리가나 열을 버려야 한다.** 남기면 「かんだしろやま」 같은 읽기가 본문 사이에
끼어들어 번역기가 문장으로 착각한다. 본문 30-33px vs 루비 16-17px 로 폭이 확연히
갈려서, 중앙값의 65% 를 문턱으로 잡았다.

**세 열 미만은 쪼개지 않는다.** 짧은 말풍선은 통짜가 더 정확하고 빠르다.

### 비용은 없었다

| | ocr 평균 | ocr 최악 |
|---|---|---|
| 열 분할 전 | 484ms | 968ms |
| 열 분할 후 | **493ms** | **1035ms** |

크롭 수는 늘지만 각각이 짧아서 배치가 오히려 빨리 끝난다. 자기회귀는 **배치에서
가장 긴 출력**이 전체를 붙잡으므로, 100자 하나보다 16자 여덟 개가 싸다.


---

## 2026-07-28 — 실사용에서 나온 것들

맥북으로 실제 만화를 읽으며 나온 문제들. 대부분 **혼자 보면 안 보이고 남이 써야
보이는** 종류였다.

### 캐시를 고쳤더니 낡은 결과가 살아났다

열 분할로 OCR 을 고쳤는데 "여전한데?" 가 돌아왔다. 원인은 **내가 고친 캐시**였다 —
지각 해시로 바꿔 캐시가 실제로 걸리기 시작하니, 고치기 **전**에 저장된 붕괴된 원문을
충실히 돌려주고 있었다.

```
캐시에 남아 있던 것:
ほっ本当は打合せにお誘いいただきありがとうございます。／神田へ馬アカテミー年を組…
```

**캐시가 안 걸릴 때는 안 나던 문제가, 걸리기 시작하니 났다.** 고친 뒤 캐시를 비우는
것까지가 수정이었다.

그래서 「다시 읽기」·「영역」에 `no_cache` 를 붙였다 — 사용자가 굳이 다시 시키는 것은
지난 결과가 마음에 안 들어서다.

### 부분 크롭은 캐시에 넣으면 안 된다

캐시를 들여다보니 **다른 두 부분 영역이 해밍 거리 14** 로 붙어 있었다 (문턱 20).
부분 크롭은 대부분 흰 바탕이라 지각 해시가 밋밋해서 서로 다른 말풍선끼리도 가까워진다.
페이지 전체는 서로 100 이상 벌어지지만 조각은 아니다.

`no_cache` 를 **읽기·쓰기 양쪽 다 안 함**으로 바꿨다. 같은 크롭을 다시 요청할 일도
없으니 쓰면 위험만 늘고 이득이 없다.

### 문턱 20 은 표본이 편향돼 있었다

픽스처 8장으로 "다른 페이지는 최소 100" 을 재고 문턱을 20 으로 잡았는데,
**그 8장은 전부 다른 작품**이었다. 실사용 캐시에서 **같은 작품의 다른 두 장이 거리 24**
로 붙어 있었다 — 여유가 4밖에 없었다.

같은 페이지는 6 이하로 측정됐으므로 10 으로 내렸다. 적중은 그대로고 오적중 여유는
4 → 14 로 늘었다.

**교훈: "다른 것끼리는 멀다" 를 잴 때 표본이 충분히 닮았는지 봐야 한다.**

### 「영역」이 기존 박스를 통째로 지우고 있었다

`begin` 이 `merge` 유무로 갈랐는데 드래그 선택에는 `merge` 가 없어서 `reset()` 으로
떨어졌다. **부분 읽기인가 전체 읽기인가**로 갈라야 맞다.

이 버그에 가려져 있던 것: 드래그 선택 박스에 접두사가 없어 **전체 읽기 박스와 id 가
부딪힐 뻔했다.** 매번 전부 지워져서 드러나지 않던 것이다.

### 손으로 한 작업이 페이지를 떠나면 사라졌다

「영역」으로 더한 박스와 「지우기」로 없앤 것은 화면에만 있었다. 돌아오면 전체 읽기는
캐시로 되살아나는데 손으로 한 것만 없으니 "처음 것으로 되돌아간다" 로 보인다.

페이지별로 저장했다. **좌표는 뷰어 사각형 기준 비율로** — 화면 픽셀로 저장하면 창
크기나 확대 배율이 바뀌는 순간 전부 어긋난다. 키는 전체 읽기의 지각 해시다.

되살릴 때 서버를 다시 부르지 않는다 (번역까지 들고 있다).

### 라벨이 화면에서 잘리고 있었다

"2~3줄 이상은 번역이 되다가 만다" 의 가장 큰 원인은 OCR 도 번역도 아니고 **CSS** 였다.

```css
white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
```

번역은 멀쩡히 다 왔는데 `…` 로 잘라 보여 주고 있었다. 줄바꿈을 허용했다.

**"안 나온다" 를 들었을 때 파이프라인 끝(표시)부터 의심할 것.**

### MV3 서비스 워커는 30초마다 죽는다

"클릭할 때마다 번역이 바뀐다" — 워커가 죽으면 `watching` 이 날아가 `lastHash` 가
null 이 되고, 되살아나면 무조건 "바뀌었다" 로 판정해 통째로 다시 읽었다.
`chrome.storage.session` 에 상태를 남겼다.

같은 이유로 `Extension context invalidated` 도 잦다. 확장을 새로고침하면 페이지에
남은 콘텐츠 스크립트가 유령이 된다. 죽은 것을 알아채고 **스스로 멈춘 뒤 새로고침하라고
알린다** — 조용히 망가지면 사용자는 왜 반응이 없는지 알 길이 없다.

### 윈도우에는 일본어 TTS 가 없었다

음성이 원문(일본어)을 읽게 해놨는데 윈도우에선 통째로 무음이었다. 확인해 보니
Heami(ko)·Zira·David(en) 뿐이다.

**내레이터의 "자연스러운 음성" 은 Chrome 이 못 본다** — Narrator 전용 스택이다.
Chrome 이 쓰는 것은 SAPI5/OneCore 라 `Language.TextToSpeech~~~ja-JP` 를 깔아야
Haruka·Ayumi·Sayaka·Ichiro 가 들어온다.

그동안 무음이 되지 않게 **일본어 음성이 없으면 번역문을 읽는** 대체 경로를 뒀다.

### Web Speech API 의 함정

- **utterance 를 전역에 붙들어야 한다.** 지역 변수로만 두면 말하는 도중 GC 가
  물어간다. `genai.Client()` 를 변수에 안 담아 터졌던 것과 같은 부류다
- `cancel()` 직후 `speak()` 는 큐를 망가뜨린다 — 실제로 말하는 중일 때만 부른다
- `speak()` 앞의 긴 `await` 는 사용자 제스처 문맥을 끊는다 — 음성 목록을 미리 받아 둔다
- 성별 필드가 없다. **이름 목록으로 고르는 수밖에 없다**

### 검출기는 글자가 화면을 채우면 못 본다 (재확인)

빽빽한 펼침면에서 5개만 잡히는 것처럼 보였는데, 같은 캡처를 서버에 통째로 돌리니
**21개 검출 → 19개 OCR → dialogue 15 · sfx 4** 로 멀쩡했다. 검출기 입력 크기를
1024 → 2560 으로 올려도 21개로 같았다.

화면에 5개만 남은 것은 위의 「영역」 버그였다. **서버를 의심하기 전에 클라이언트를
확인할 것.**


---

## 뷰어 메뉴바가 "페이지가 바뀌었다" 로 읽혔다

만화 뷰어는 클릭하면 위에 제목 줄, 아래에 페이지 이동 줄이 뜬다. 그림은 그대로인데
픽셀이 바뀌어서 자동 감지가 새 페이지로 보고 통째로 다시 읽었다.

얼마나 바뀌는지 재봤다 (픽스처 8장, 위 9%·아래 11% 를 어둡게 덮어 메뉴바를 흉내):

| 해시를 볼 범위 | 메뉴바로 인한 차이 | 다른 페이지 최소 거리 |
|---|---|---|
| 1.00 × 1.00 (전체) | **최대 98** | 100 |
| 1.00 × 0.78 | 0 | 96 |
| **0.92 × 0.74** | **0** | **100** |
| 0.85 × 0.70 | 0 | 101 |

**메뉴바 하나가 다른 페이지(100)와 맞먹는 98 을 만든다.** 문턱을 아무리 조정해도
구별할 수 없다.

위아래를 13% 씩 덜어내면 **메뉴바가 해시를 아예 안 건드리면서** 페이지 구별력은
그대로다 (0 vs 100). 좌우 4% 는 옆에 붙는 UI 몫이다.

캐시 키도 같은 해시를 쓰므로 **메뉴바가 떠 있든 없든 같은 캐시에 맞는다** — 예전에는
메뉴바 상태가 다르면 캐시가 빗나가 번역을 새로 쳤다.

**교훈: 화면을 통째로 해싱하면 화면에 뜨는 모든 것이 신원의 일부가 된다.**
정말 신원인 부분만 봐야 한다.

---

## 음성 목록을 캐시하면 안 된다

윈도우에 일본어 TTS 를 깔았는데도 계속 한국어로 읽혔다. OS 에는 Haruka·Ayumi·
Sayaka·Ichiro 가 멀쩡히 등록돼 있었다.

원인은 내 코드였다:

```js
let voicesReady = null;
function loadVoices() {
  if (voicesReady) return voicesReady;   // ← 결과 배열을 영구히 캐시
  ...
  setTimeout(() => resolve(speechSynthesis.getVoices()), 1000);
}
```

콘텐츠 스크립트가 뜨자마자 목록을 미리 받아 두는데, **그 시점에 목록이 아직 안
채워졌거나 일부만 들어 있으면 그게 그대로 굳는다.** 나중에 일본어가 들어와도
캐시된 옛 목록만 본다.

`getVoices()` 는 싼 호출이라 캐시할 이유가 없었다. 매번 새로 읽게 고쳤다.

그리고 **왜 못 읽는지 화면에 띄웠다** — `있는 언어: en ko` 처럼. "안 나온다" 만으로는
브라우저 문제인지 내 선택 로직 문제인지 가릴 수 없다.


---

## 같은 페이지를 70초에 8번 다시 번역하고 있었다

"안 눌렀는데 번역이 막 새로고침된다" 는 제보. 서비스 메트릭(`logs/metrics.jsonl`)을
봤더니 그대로였다.

```
시각        간격   캐시    region   업로드    직전과 해시거리
16:22:13    7s   False      5    377KB    109
16:22:27   14s   False      6    323KB     21
16:22:30    3s   False      8    329KB     13
16:22:49   19s   False     11    357KB    126
16:22:54    5s   False      7    405KB    127
16:23:02    8s   False      5    405KB     26
16:23:09    8s   False      6    395KB     15
16:23:22   13s   False      5    324KB     18
```

전부 300-400KB 라 **전체 페이지 읽기**다. 전부 캐시 미스라 매번 번역 API 를 새로 쳤다.

**결정적 단서는 region 수다: 4·5·6·7·8·11 로 널뛴다.** 같은 페이지를 보고 있는데
검출 개수가 이렇게 다를 수 없다 — **잘라 보내는 영역 자체가 매번 달랐다.**

`probeViewer()` 는 호출할 때마다 기하 휴리스틱을 새로 돌린다. 캐러셀이 조금
움직이거나 타일이 늦게 뜨면 **고르는 요소가 달라지고**, 그러면 crop 이 달라져
해시가 흔들리고, 흔들린 해시는 "새 페이지" 로 읽혀 통째로 다시 번역된다.

해시 거리 13·15·18·21·26 이 그 증거다 — 문턱(10)을 조금씩 넘는다. 문턱을 올려
막을 수는 없다. 실측상 **다른 페이지의 최소 거리가 24** 라 겹쳐 버린다.

### 고친 방식

자동 감지 전용 경로 `probe-stable` 을 뒀다. **뷰어를 다시 찾지 않고 지난 읽기에서
본 요소를 그대로 쓴다.** 요소가 사라졌을 때만 새로 찾는다.

같은 요소의 사각형을 쓰므로 연속 캡처가 픽셀 수준에서 같아지고, 해시가 안 흔들리고,
캐시가 걸린다.

**확인 시각도 매번 남긴다.** 예전에는 "바뀌었을 때만" 남겨서, 안 바뀌는 동안은
`AUTO_MIN_INTERVAL_MS` 가 안 걸려 확인이 몰렸다 — 확인마다 캡처하느라 깜빡였다.

**교훈: 같은 것을 비교하려면 같은 방식으로 잘라야 한다.** 자동 감지는 "무엇이
뷰어인가" 를 매번 다시 판단할 이유가 없다 — 그건 사람이 읽기를 시작할 때 한 번
정하면 되는 것이다.


---

## yanmaga(SpeedBinb): 한 페이지가 띠 세 장이라 3분의 1만 보내고 있었다

"yanmaga 에서 주로 그런 것 같다 · 타일 렌더링이라 품질이 나쁜 건가" 는 제보.
**절반만 맞았다** — 타일이 원인이지만 망가진 곳은 OCR 품질이 아니라 뷰어 탐지였다.

캡처를 서버에 직접 돌리니 **14개 검출 → 14개 OCR** 로 원문도 정확했다.
`captureVisibleTab` 이 받는 것은 이미 **합성된 픽셀**이라 타일이든 아니든 상관없다.

### DOM 을 보니 답이 나왔다

```
IMG 888x426 @0,42      ← 현재 페이지가 띠 3장으로 세로로 쌓여 있다
IMG 888x426 @0,465
IMG 888x428 @0,888
IMG 888x426 @-936,42   ← 이전 페이지들 (x 음수 = 화면 밖)
IMG 888x426 @-1822,42
```

`probeViewer()` 의 그룹 규칙은 `overlap >= 0.6 * min(height)` — **세로로 겹치는
것끼리** 묶는다(펼침면용). 위아래로 쌓인 띠는 세로 겹침이 0 이라 안 묶인다.

실제 수치로 재현:

| | 묶인 개수 | 잘라 보내는 영역 |
|---|---|---|
| 같은 행만 | 1개 | 888×426 (페이지의 1/3) |
| **세로까지 이어 붙이면** | **3개** | **888×1274 (페이지 전체)** |

스크롤하면 뷰포트 중앙에 가까운 띠가 바뀌어 씨앗도 바뀐다 — 잘라 보내는 영역이
널뛰고, 해시가 흔들리고, 같은 페이지를 계속 다시 번역한다. 메트릭에서 같은 페이지의
region 수가 **4·5·6·7·8·11** 로 흔들린 것이 이 흔적이었다.

### 별개 페이지와 구별하기

세로 스크롤 뷰어는 **다른 페이지**도 위아래로 쌓는다. 그건 묶으면 안 된다
(`order.py` 의 펼침면 분할이 2페이지를 가정하고, §8.4 는 페이지당 1회 호출을 전제한다).

한 페이지의 띠는 이렇다:
- 좌우 범위가 거의 같다 (폭·왼쪽 좌표가 10% 안쪽)
- 세로로 맞닿아 있다 (틈 24px 이하 — 실측에서는 **3px 겹쳐** 있었다)

별개 페이지는 틈이 벌어지거나 좌우가 어긋난다. 화면 밖 페이지는 x 가 음수라 애초에
걸러진다.

**교훈: "같은 것" 을 묶는 규칙은 한 가지 배치만 보고 만들면 안 된다.** 펼침면
(좌우)만 생각하고 만든 규칙이 띠(위아래) 앞에서 통째로 실패했다.


---

## 깜빡임의 정체 — 확인할 때마다 오버레이를 숨겼다 되살렸다

"계속 깜빡거리는 건 도대체 뭐지" · "박스를 클릭하면 깜빡이고 뭔가 바뀌는 것 같다".
둘 다 같은 뿌리였다.

자동 감지는 신호(클릭·키·DOM 변화)를 받으면 0.7초 뒤 **캡처해서** 바뀌었는지 본다.
캡처 전에는 오버레이를 숨긴다 — 박스가 스크린샷에 찍히면 검출기가 그걸 글자로 읽기
때문이다. **그 숨겼다 되살리는 것이 곧 깜빡임이다.** 확인은 4초에 한 번까지 일어나니
쉼 없이 깜빡인다.

### 확인용 캡처는 숨길 필요가 없다

비교하는 두 캡처에 **같은 오버레이가 똑같이** 찍히므로, 아래 만화가 바뀌어야만 해시가
달라진다. 판정에 지장이 없다.

진짜로 읽을 때는 당연히 숨긴다. 그때는 어차피 번역에 몇 초를 쓰므로 캡처를
재사용하지 않고 새로 한 번 더 찍는다 — 확인용 캡처에는 오버레이가 찍혀 있어 OCR 에
못 쓴다.

**읽은 직후에는 기준을 새로 잡아야 한다.** 오버레이가 새 박스로 바뀌었으니 다음
비교에서 "바뀌었다" 로 읽힌다. `needsBaseline` 을 세워 다음 확인에서 비교 없이
기준만 갱신한다.

### 박스 클릭도 신호였다

우리 오버레이 위 클릭까지 "페이지가 바뀌었을 수 있다" 로 잡고 있었다. 박스를 누른
것은 페이지를 넘긴 것이 아닌데, 0.7초 뒤 확인 캡처가 돌아 깜빡였다. `isOurs()` 로
거른다 (DOM 변화 신호에서 쓰던 것을 그대로 쓴다).

**교훈: 자기가 만든 변화를 자기 신호로 되먹이지 않았는지 늘 볼 것.** MutationObserver
에서 한 번 겪고도(무한 루프) 클릭에서 또 놓쳤다.


---

## 상태 메시지가 번역되고 있었다

"상태 메시지 이것도 번역해버리는데?" — 오버레이를 숨기고 캡처하는데도 상태줄 글자가
검출기에 들어갔다. 원인이 **둘** 있었다.

### 1. `overlay()` 가 부를 때마다 다시 켰다

```js
function overlay() {
  ...
  el.style.visibility = "visible";   // ← 부를 때마다
  return el;
}
```

`status()` 도 `overlay()` 를 부른다. 그래서 **숨겨 놓은 사이에 상태 메시지가 한 번이라도
나가면 오버레이가 도로 켜지고**, 그 상태로 캡처된다.

편의로 넣은 한 줄이 "숨김" 이라는 상태를 조용히 뒤집고 있었다. 켜는 것은
`showOverlay()` 로 분리해 켜려는 곳에서만 부른다.

### 2. 숨긴 직후 캡처하면 아직 안 그려졌다

`hide-overlay` 응답을 스타일만 바꾸고 바로 보냈다. 브라우저가 다시 그리기 전이라
`captureVisibleTab` 이 **오버레이가 있는 프레임**을 찍는다. 드래그 선택에서는 이미
두 프레임을 기다리고 있었는데(그때 겪었다) 정작 본 경로에는 없었다.

`requestAnimationFrame` 두 번 뒤에 응답한다. 탭이 뒤로 가면 rAF 가 아예 안 도니
120ms 시간 제한을 같이 건다 — 응답이 없으면 background 가 그 자리에서 멎는다.

**교훈: 같은 함정을 한 곳에서 고쳤다고 끝난 게 아니다.** 드래그 선택에만 넣고
본 경로를 안 봤다.

---

## 버튼 툴팁 — 브라우저 기본 `title` 은 못 쓴다

뜨는 데 1초 넘게 걸리고 위치도 정할 수 없다. "마우스를 올린 순간 단축키를 알려 준다"
는 목적에 안 맞는다. 직접 그렸다.

버튼 정의를 `PANEL_BUTTONS`/`PANEL_MORE` 배열 **한 곳으로 모았다.** 예전에는 이름·
설명·단축키가 HTML 문자열 안에 흩어져 있어 단축키를 바꿀 때 놓치기 쉬웠다.


---

## 페이지를 넘겨도 자동 번역이 안 걸리던 세 가지

"페이지 넘어갈 때 자동으로 번역 안 되는 일이 허다해." 원인이 셋 다 **신호를 조용히
버리는 것**이었다.

### 1. 내가 방금 넣은 `needsBaseline` 이 넘김을 삼켰다

깜빡임을 없애면서 확인용 캡처에 오버레이를 남겼다. 그러면 읽은 직후 오버레이가
바뀌어 있으니 다음 비교의 기준이 못 된다 — "다음 확인은 기준만 잡고 넘어가기" 로
처리했다.

**그 한 번의 확인 동안 페이지를 넘기면 그 넘김이 통째로 사라진다.** 기준이 새 페이지로
잡히고 "안 바뀜" 이 되기 때문이다. 만화는 넘기고 바로 또 넘기는 일이 잦아 자주 걸렸다.

읽기가 끝난 뒤 400ms 있다가 기준을 **직접 한 번 찍는 것**으로 바꿨다
(`refreshBaseline`). 오버레이를 안 숨기므로 깜빡이지 않는다.

### 2. 간격 제한에 걸린 신호를 버렸다

```js
if (Date.now() - st.lastAt < AUTO_MIN_INTERVAL_MS) return;   // 그냥 버림
```

4초 안에 두 번 넘기면 두 번째가 사라진다. 남은 시간만큼 **미뤘다가 다시 보게** 고쳤다.

### 3. 디바운스가 영영 안 끝났다

`schedule()` 은 신호가 올 때마다 `clearTimeout` 으로 확인을 700ms 뒤로 민다. 광고나
애니메이션이 도는 뷰어는 DOM 변화가 끊이지 않아 **확인이 영영 안 돈다.**

첫 신호로부터 2.2초가 지나면 더 미루지 않는다 (`SETTLE_MAX_MS`).

**교훈: "지금은 처리 안 함" 과 "버림" 은 다르다.** 세 곳 다 미뤘어야 할 것을 버리고
있었다.

---

## 라벨이 다른 박스 밑으로 들어가던 것

"박스 선보다 다른 번역 내용이 아래에 있는 경우가 있는데" — 라벨에 `z-index` 가 없어
DOM 순서상 **뒤에 오는 박스의 테두리·배경이 앞 박스의 라벨을 덮었다.**

`.mlr-box` 는 `position:absolute` 지만 `z-index:auto` 라 **쌓임 맥락을 만들지 않는다.**
그래서 자식인 라벨에 `z-index: 5` 를 주면 형제 박스들 위로 올라간다.

곁가지로 하나 더 나왔다: 걸러진 효과음을 보이게 할 때 `opacity: 0.55` 를 썼는데
**`opacity < 1` 은 쌓임 맥락을 만든다** — 그 박스의 라벨은 다시 갇힌다. 테두리·배경
색을 직접 옅게 해서 같은 인상을 냈다.

그 김에 "보이는 박스인가" 판정도 `getComputedStyle(b).opacity` 에서 클래스 확인으로
바꿨다. CSS 를 색으로 바꾸는 순간 그 판정이 조용히 어긋난다.


---

## 넘길 때 덜컹이는 이유 — 왕복이 두 번이었다

"화면 넘어가자마자 박스 사라지는 게 별로인 건지, 해시 받는 게 오래 걸려서인지"
— **후자다.** 넘김 뒤 타임라인이 이랬다:

```
넘김 → 대기 700ms → 캡처 150ms → 해시 비교 → **또 캡처** 150ms → 서버 300ms
       └────────────── 이 0.9-1.3초 내내 옛 번역이 새 그림 위에 얹혀 있다 ──────────┘
```

캡처가 두 번인 이유: 확인용 캡처는 깜빡임을 없애려고 **오버레이를 켠 채** 찍는데,
거기엔 박스가 찍혀 있어 OCR 에 못 쓴다. 그래서 읽을 때 다시 찍었다.

### 신호의 성격에 따라 나눴다

| 신호 | 오버레이 | 이유 |
|---|---|---|
| DOM 변화 | **켠 채** 확인 | 광고·애니메이션이라 넘김이 아닐 때가 대부분. 숨기면 가만히 있는데 깜빡인다 |
| 클릭·방향키 | **숨기고** 확인 | 넘김이 거의 확실하다. 깜빡임은 넘김 동작에 묻힌다 |

사람이 넘긴 경우에는 그 캡처를 **그대로 읽기에 넘긴다.** 캡처가 한 번으로 줄고
(150-200ms 절약), 화면에서는 확인하는 그 순간에 옛 박스가 사라져 새 그림만 남는다.
대기도 700 → 380ms 로 줄였다 (넘김 애니메이션은 보통 300ms 안에 끝난다).

```
넘김 → 대기 380ms → 캡처 150ms(이때 박스 사라짐) → 서버 300ms → 새 박스
```

### 흐리게, 그리고 스며들게

DOM 변화로 감지된 경우에는 여전히 다시 찍어야 하므로 그동안 **옛 박스를 흐리게**
해 둔다(`.mlr-stale`). 지우지 않는 이유: 곧바로 비우면 화면이 텅 비었다가 다시 차서
오히려 덜컹인다. 새 박스는 0.18초에 걸쳐 스며들게 했다.

### 막다른 길 — 키를 누르는 순간 흐리게 하기

방향키를 누르자마자 박스를 흐리게 해 보려 했는데 **안 된다.** 흐리게 하는 것도
화면 픽셀을 바꾸는 것이라 확인용 캡처의 해시가 달라진다 — 페이지가 안 바뀌었어도
"바뀌었다" 가 되어 매번 다시 읽는다. 오버레이를 숨기고 확인하는 위 방식이 같은
효과를 내면서 이 문제가 없다.

---

## 가타카나가 히라가나로 읽히는가 — 재봤지만 재현되지 않았다

"효과음 쪽에서 가타카나를 히라가나로 인식하는 느낌" 이라는 제보.

가설은 **디코더의 언어 사전확률**이었다. manga-ocr 은 자기회귀 디코더라, 학습
데이터인 만화 대사에 히라가나가 압도적이면 시각 증거가 약할 때 히라가나로 기울 수
있다. 그렇다면 **글자를 흐리게 할수록 가타카나가 무너져야** 한다.

확실한 가타카나 효과음 크롭을 흐리게/옅게 만들어 봤다:

| | 그대로 | 흐림 σ2 | 흐림 σ4 | 대비 35% |
|---|---|---|---|---|
| 가타카나 비율 | 64% | 50% | 57% | 50% |
| 「ダン」 | ダツ | **ダン** | **ダン** | ダツ |

**기울지 않는다.** 흐리게 해도 가타카나는 가타카나로 남는다. 가설 기각.

실제로 보인 것은 **같은 문자종 안의 혼동**이었다:

- `ン → ツ` (획 방향)
- `ぷ → ぴ` (반탁점)
- `ダーン → ダイン` — 효과음 둘레의 **속도선을 글자로 착각**

그리고 하나 더: 「ぷるぷる」는 **원본이 실제로 히라가나**였다. 만화 효과음은 부드러운
소리를 히라가나로 적는 일이 흔하다 — 오인식이 아닌 경우가 섞여 있다.

**남은 가능성:** 제 픽스처 8장에 그 현상이 없을 뿐일 수 있다. 실제로 틀린 화면을
`test/mac/` 에 넣어 주면 그걸로 다시 본다.

덧붙여 — 히라가나와 가타카나는 **소리가 같다.** 「プルプル」를 「ぷるぷる」로 읽어도
번역은 "부들부들" 로 같게 나온다. 실제 손해는 탁점·반탁점을 놓칠 때(`ド→と`)다.
