# 따라만 하면 되는 설치

**30분이다.** 대부분은 3GB 를 받는 시간이고, 손으로 하는 것은 명령 여섯 줄과 크롬
설정 하나다. 위에서부터 순서대로 한다.

왜 그렇게 하는지는 [`README.md`](README.md) 에 있다. 이 문서는 **되게 만드는 것**만
적는다.

---

## 0. 있어야 하는 것

| | |
|---|---|
| 윈도우 PC | 맥·리눅스도 되지만 이 문서는 윈도우 기준이다 |
| **NVIDIA 그래픽카드** | 없으면 OCR 이 10배 느려 실사용이 안 된다 |
| 크롬 | 엣지·웨일도 된다 (크로미움 계열) |
| 번역 API 키 | 3번에서 만든다. **돈이 든다** — 한 권(200쪽)에 약 2,000원 |

이 도구는 화면을 그림으로 찍어 글자를 읽는다. 정식 구독했거나 무료로 공개된 회차를
**자기가 읽을 때** 쓰는 것을 전제로 한다.

## 1. uv

파이썬을 따로 깔 필요가 없다. 이것 하나가 다 챙긴다.

**시작 → `powershell` → 열기.** 붙여 넣고 엔터:

```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**끝나면 창을 닫았다 다시 연다.** 안 닫으면 다음 단계에서 `uv 를 찾을 수 없습니다`
가 난다 — PATH 는 새 창에만 반영된다.

```powershell
uv --version
```

`uv 0.x.x` 가 나오면 됐다.

## 2. 코드

```powershell
cd C:\
git clone https://github.com/vanillapapaya/MangaLiveReader
cd MangaLiveReader
```

`git 을 찾을 수 없습니다` 가 나오면 git 이 없는 것이다. GitHub 페이지의 초록색
**Code** → **Download ZIP** → 압축을 풀어 `C:\MangaLiveReader` 로 옮긴 뒤
`cd C:\MangaLiveReader`.

## 3. 키

둘 중 **하나만** 있으면 된다.

**Anthropic** — 더 싸고 빠르다 (200쪽 $1.64 대 $2.44).
1. https://console.anthropic.com 가입
2. 결제 수단 등록. 선불이라 $5 만 넣어도 세 권쯤 본다
3. **API Keys → Create Key** → `sk-ant-` 로 시작하는 값 복사

**Google Gemini** — 무료 크레딧이 있다.
1. https://aistudio.google.com/apikey → **Create API key**
2. `AIza` 로 시작하는 값 복사

파일 하나에 넣는다. `여기에_키_붙여넣기` 자리에 복사한 값을 넣는다:

```powershell
mkdir "$env:USERPROFILE\.config\mangalivereader" -Force
"ANTHROPIC_API_KEY=여기에_키_붙여넣기" | Out-File -Encoding ascii "$env:USERPROFILE\.config\mangalivereader\env"
```

Gemini 면 `ANTHROPIC_API_KEY` 대신 `GEMINI_API_KEY` 라고 적는다.

**메모장으로 만들지 마라.** `env.txt` 로 저장되어 프로그램이 못 찾는다.

확인 — 키 값은 화면에 안 찍힌다. 찾았다는 표시만 나오면 된다:

```powershell
uv run python scripts\check_keys.py
```

## 4. 설치

```powershell
uv sync
uv run python scripts\fetch_models.py
```

첫 줄이 5-15분이다 (torch 만 2GB 넘는다). 둘째 줄은 글자를 찾는 모델과 읽는 모델을
받는다. 둘 다 **다시 돌려도 안전하다** — 이미 있으면 건너뛴다.

## 5. 켜기

```powershell
.\run-service.cmd
```

```
GPU: NVIDIA GeForce RTX 4070 · torch 2.11.0+cu128
mtl-service → http://127.0.0.1:8788
```

**이 창을 닫으면 꺼진다.** 만화를 보는 동안 켜 둔다. 다음부터는 이 한 줄이 전부다.

`GPU:` 줄 대신 `CUDA 를 못 잡았다` 가 나오면 그래픽카드를 못 쓰고 있는 것이다.
돌기는 하지만 10배 느리다 — 아래 「안 될 때」를 본다.

## 6. 확장

1. 주소창에 `chrome://extensions`
2. 오른쪽 위 **개발자 모드** 켜기
3. 왼쪽 위 **압축해제된 확장 프로그램을 로드**
4. `C:\MangaLiveReader\extension` 폴더를 고른다

목록에 **Manga Live Reader** 가 뜨면 끝이다. **옵션은 건드릴 것이 없다** — 기본값이
방금 켠 서비스를 가리킨다.

## 7. 써 보기

만화 뷰어 페이지에서 **`Alt+Shift+M`**.

몇 초 뒤 말풍선 위에 번역이 겹친다. 이런 모양이다:

![번역이 겹쳐 나온 화면](docs/demo.png)

(그림은 예시용으로 그린 것이다. 박스와 번역은 실제로 돌려서 나온 결과다.)

첫 장은 모델을 올리느라 10초쯤, 그다음부터 5초 안쪽이다. 같은 페이지를 다시 열면
캐시에서 나와 즉시 뜨고 **돈도 안 든다.**

읽고 나면 오른쪽 위에 버튼 패널이 뜬다:

| 버튼 | |
|---|---|
| **번역** | 지금 화면을 다시 읽는다 |
| **자동** | **켜 두면 페이지를 넘길 때마다 알아서 읽는다.** 이걸 켜는 것을 권한다 |
| **영역** | 읽을 곳을 마우스로 직접 고른다 (자동 탐지가 틀렸을 때) |
| **갱신** | 캐시를 버리고 다시 번역한다 |
| **음성** | 원문을 소리내어 읽는다 |
| `` ` `` (누르는 동안) | 오버레이를 감춰 원래 그림을 본다 |

늘 보는 사이트에서 「자동」이 스스로 켜지게 하려면 — 확장 아이콘 우클릭 → **옵션**
→ 「자동으로 켜질 사이트」에 한 줄에 하나씩 (`comic-walker.com` 같은 호스트만).
**기본은 비어 있다.** 적은 곳에서만 켜진다.

---

## 안 될 때

| 증상 | 원인 |
|---|---|
| `uv 를 찾을 수 없습니다` | PowerShell 창을 안 닫았다. 닫았다 다시 연다 |
| `CUDA 를 못 잡았다` | NVIDIA 드라이버를 최신으로. 카드가 없으면 `service.local.toml` 을 만들어 `[models]` 와 `device = "cpu"` 두 줄을 넣는다 (아주 느리다) |
| 번역 없이 원문만 나온다 | 키가 없거나 틀렸다. `uv run python scripts\check_keys.py` |
| `Alt+Shift+M` 이 아무 일도 안 한다 | 5번의 창이 꺼져 있다 |
| 「서비스에 붙지 못했다」 | 같은 것 — 서비스가 꺼져 있다 |
| 글자는 나오는데 자리가 어긋난다 | 브라우저 확대를 100%(`Ctrl+0`)로. 그래도 밀리면 「영역」으로 직접 고른다 |
| 번역이 이상하다 | 「갱신」. 효과음은 원래 자주 틀린다 |
| 목차 화면에서 헛돈다 | 정상이다. 뷰어가 아니면 자동은 안 켜진다 |

## 자주 묻는 것

**얼마나 드나** — 200쪽 한 권에 claude-sonnet-5 가 $1.64, gemini-3.6-flash 가 $2.44
로 실측했다. 단가는 Gemini 가 절반인데 사고 토큰이 3.3배라 실제로는 더 비싸다.
같은 페이지를 다시 볼 때는 캐시에서 나오므로 **0원**이다.

**내 키가 새지 않나** — 키는 **내 PC 안 파일 하나**에만 있다. 확장은 키를 모르고,
번역은 내 PC 에서 API 로 곧바로 나간다. 중간에 남의 서버가 없다.

**만화 그림이 어디로 가나** — 내 PC 의 서비스까지만 간다. 거기서 읽어 낸 **글자만**
번역 API 로 나간다. 그림은 나가지 않는다.

**아이패드나 다른 PC 에서도 되나** — 된다. 서비스는 그래픽카드 있는 PC 한 대에서만
돌리고 나머지는 확장만 깐다. 설정은 [`README.md` 의 「서비스 여는 법」](README.md#서비스-여는-법).

**목소리를 바꾸고 싶다** — 기본은 브라우저 음성이라 기계음이다. 학습된 목소리를
쓰려면 [gpt-sovits-server](https://github.com/vanillapapaya/gpt-sovits-server) 를
따로 띄운다. 그래픽카드가 하나 더 있는 편이 좋다.

**만화 말고 다른 일본어 페이지에서도 되나** — **잘 안 된다.** 청공문고 『羅生門』
페이지로 재보니 검출이 2개뿐이었다 — 제목은 정확히 읽었지만(「羅生門 芥川龍之介」)
본문은 900×719 한 덩어리로 잡혀 OCR 이 `．．．` 를 뱉었다. 말풍선 한 조각을 읽는
모델이지 산문 한 페이지를 읽는 모델이 아니다.

**끄고 싶다** — 창을 닫으면 서비스가 꺼진다. 확장은 `chrome://extensions` 에서
스위치를 내린다. 지울 때는 폴더째 지우면 끝이다 — 레지스트리를 건드리지 않는다.
