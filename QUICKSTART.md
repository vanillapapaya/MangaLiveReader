# 따라만 하면 되는 설치 안내

**컴퓨터를 잘 몰라도 30분이면 된다.** 위에서부터 순서대로, 명령은 복사해서 붙여
넣으면 된다. 막히면 맨 아래 「안 될 때」를 본다.

기술적인 설명은 [`README.md`](README.md) 에 있다 — 이 문서는 **되게 만드는 것**만
다룬다.

---

## 0. 시작하기 전에

**있어야 하는 것**

| | |
|---|---|
| 윈도우 PC | 맥·리눅스도 되지만 이 문서는 윈도우 기준이다 |
| **NVIDIA 그래픽카드** | 없으면 돌기는 하는데 한 장에 몇 분씩 걸려 못 쓴다 |
| 크롬 브라우저 | 또는 엣지·웨일 같은 크로미움 계열 |
| 번역 API 키 | 3번에서 만든다. **돈이 든다** — 만화 한 권(200쪽)에 약 2,000원 |

**시간**은 30분쯤 걸린다. 대부분 다운로드 기다리는 시간이다 (3GB 쯤 받는다).

> 이 도구는 화면을 그림으로 찍어 글자를 읽어 내는 방식이다. 정식 구독했거나 무료로
> 공개된 회차를 **자기가 읽을 때** 쓰는 것을 전제로 한다.

---

## 1. uv 설치하기

파이썬과 필요한 것들을 알아서 챙겨 주는 도구다. 이것 하나만 깔면 파이썬을 따로 깔
필요가 없다.

**시작 → `powershell` 검색 → 열기.** 아래를 붙여 넣고 엔터:

```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

끝나면 **PowerShell 창을 닫았다가 다시 연다** (안 닫으면 다음 단계에서
`uv 를 찾을 수 없습니다` 가 나온다).

확인:

```powershell
uv --version
```

`uv 0.x.x` 처럼 나오면 된 것이다.

## 2. 코드 받기

```powershell
cd C:\
git clone https://github.com/vanillapapaya/MangaLiveReader
cd MangaLiveReader
```

`git 을 찾을 수 없습니다` 가 나오면 — GitHub 페이지에서 초록색 **Code** 버튼 →
**Download ZIP** → 압축을 풀어 `C:\MangaLiveReader` 로 옮긴 뒤:

```powershell
cd C:\MangaLiveReader
```

## 3. 번역 키 만들기

둘 중 **하나만** 있으면 된다.

**Anthropic (권장 — 더 싸고 빠르다)**
1. https://console.anthropic.com 가입
2. 결제 수단 등록 (선불이다. $5 만 넣어도 만화 세 권쯤 본다)
3. **API Keys → Create Key** → `sk-ant-...` 로 시작하는 값을 복사

**Google Gemini**
1. https://aistudio.google.com/apikey → **Create API key**
2. `AIza...` 로 시작하는 값을 복사

복사한 키를 파일로 저장한다. PowerShell 에서 — **`여기에_키_붙여넣기` 자리에 복사한
키를 넣는다:**

```powershell
mkdir "$env:USERPROFILE\.config\mangalivereader" -Force
"ANTHROPIC_API_KEY=여기에_키_붙여넣기" | Out-File -Encoding ascii "$env:USERPROFILE\.config\mangalivereader\env"
```

Gemini 를 쓴다면 `ANTHROPIC_API_KEY` 대신 `GEMINI_API_KEY` 라고 적는다.

> **메모장으로 만들지 마라.** 메모장은 `env.txt` 로 저장해서 프로그램이 못 찾는다.

제대로 들어갔는지 보려면:

```powershell
uv run python scripts\check_keys.py
```

키 값은 화면에 찍히지 않는다. 「찾았다」는 표시만 나오면 된다.

## 4. 설치하기

```powershell
uv sync
uv run python scripts\fetch_models.py
```

첫 줄이 5-15분 걸린다 (3GB 쯤 받는다). 둘째 줄은 글자를 찾고 읽는 모델을 받는다.

## 5. 켜기

```powershell
.\run-service.cmd
```

이렇게 나오면 성공이다:

```
GPU: NVIDIA GeForce RTX 4070 · torch 2.11.0+cu128
mtl-service → http://127.0.0.1:8788
```

**이 창을 닫으면 꺼진다.** 만화를 보는 동안은 켜 둔다. 끌 때는 `Ctrl+C` 를 누르거나
창을 닫으면 된다. 다음부터는 이 명령 하나면 된다.

> `GPU:` 줄 대신 `CUDA 를 못 잡았다` 가 나오면 그래픽카드를 못 쓰고 있는 것이다.
> 돌기는 하지만 아주 느리다 — 「안 될 때」를 볼 것.

## 6. 크롬에 확장 설치

1. 주소창에 `chrome://extensions` 입력
2. 오른쪽 위 **개발자 모드** 켜기
3. 왼쪽 위 **압축해제된 확장 프로그램을 로드**
4. `C:\MangaLiveReader\extension` 폴더를 고른다

목록에 **Manga Live Reader** 가 나오면 끝이다. 설정은 건드릴 것이 없다 — 기본값이
방금 켠 서비스를 가리킨다.

## 7. 써 보기

만화 뷰어 페이지를 열고 **`Alt+Shift+M`**.

몇 초 뒤 말풍선 위에 번역이 겹쳐 나온다. 이런 모양이다:

![번역이 겹쳐 나온 화면](docs/demo.png)

(위 그림은 실제 만화가 아니라 예시용으로 그린 것이다.)
 처음 한 장은 모델을 올리느라 10초쯤
걸리고, 그다음부터는 5초 안쪽이다.

읽고 나면 오른쪽 위에 버튼 패널이 뜬다:

| 버튼 | 하는 일 |
|---|---|
| **번역** | 지금 화면을 다시 읽는다 |
| **자동** | 켜 두면 **페이지를 넘길 때마다 알아서** 번역한다 |
| **영역** | 번역할 자리를 마우스로 직접 고른다 (자동 탐지가 틀렸을 때) |
| **갱신** | 번역이 이상할 때 다시 시킨다 |
| **음성** | 원문을 소리내어 읽는다 |
| **`** 키 (누르는 동안) | 번역을 잠깐 감춰 원래 그림을 본다 |

**「자동」을 켜는 것을 권한다.** 페이지를 넘기기만 하면 알아서 따라온다.

늘 보는 사이트에서 자동이 스스로 켜지게 하려면 — 확장 아이콘 우클릭 → **옵션** →
「자동으로 켜질 사이트」에 주소를 한 줄에 하나씩 적는다 (예: `comic-walker.com`).

---

## 안 될 때

| 이런 증상 | 이렇게 한다 |
|---|---|
| `uv 를 찾을 수 없습니다` | PowerShell 창을 닫았다 다시 연다 |
| `CUDA 를 못 잡았다` | NVIDIA 드라이버를 최신으로. 그래픽카드가 없으면 `service.local.toml` 파일을 만들어 `[models]` 줄과 `device = "cpu"` 줄을 넣는다 (아주 느리다) |
| 번역 없이 원문만 나온다 | 키가 없거나 틀렸다. 3번을 다시. `uv run python scripts\check_keys.py` |
| `Alt+Shift+M` 을 눌러도 아무 일이 없다 | 5번의 검은 창이 켜져 있는지 확인 |
| 「서비스에 붙지 못했다」 | 같은 것 — 서비스가 꺼져 있다 |
| 글자는 나오는데 자리가 어긋난다 | 브라우저 확대(`Ctrl+0` 으로 100%)를 확인. 그래도 밀리면 「영역」으로 직접 고른다 |
| 번역이 이상하다 | 「갱신」을 누른다. 효과음은 원래 잘 틀린다 |
| 목차 화면에서 헛돈다 | 정상이다. 뷰어가 아니면 자동은 안 켜진다 |

## 자주 묻는 것

**돈이 얼마나 드나** — 200쪽 한 권에 claude-sonnet-5 가 약 $1.64 (2,000원쯤).
같은 페이지를 다시 볼 때는 저장해 둔 것을 쓰므로 돈이 들지 않는다.

**내 키가 어디로 새지 않나** — 키는 **내 PC 안 파일 하나**에만 있다. 확장은 키를
모르고, 번역은 내 PC 에서 직접 API 로 나간다. 중간에 서버가 없다.

**만화 이미지가 어디로 가나** — 내 PC 의 서비스로만 간다. 번역할 때 그 글자만
API 로 나가고 그림은 나가지 않는다.

**아이패드나 다른 PC 에서도 되나** — 된다. 서비스는 그래픽카드 있는 PC 한 대에서만
돌리고 다른 기기는 확장만 깔면 된다. 설정은 [`README.md` 의 「서비스 여는
법」](README.md#서비스-여는-법) 에 있다.

**목소리를 예쁘게 바꾸고 싶다** — 기본은 브라우저 음성이라 기계음이다. 학습된
목소리를 쓰려면 [gpt-sovits-server](https://github.com/vanillapapaya/gpt-sovits-server)
를 따로 띄운다. 그래픽카드가 하나 더 있는 편이 좋다.

**끄고 싶다** — 검은 창을 닫으면 서비스가 꺼진다. 확장은 `chrome://extensions` 에서
스위치를 내리면 된다. 지우려면 폴더째 지우면 끝이다 (레지스트리를 건드리지 않는다).
