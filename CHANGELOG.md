# 변경 이력

## v0.3.0 — 세로 스크롤 사이트는 화면이 아니라 이미지를 읽는다

### 새로 생긴 것

- **한 회차가 이미지 수십 장으로 이어진 사이트(harta 등)에서 캡처를 그만두고
  이미지 파일 자체를 보냅니다.** 캡처는 「지금 화면에 보이는 만큼」이라 스크롤할
  때마다 잘림이 달라졌고, 같은 그림이 매번 다른 해시와 좌표로 나갔습니다.
  v0.2.1~0.2.3 에서 고친 것들이 전부 그 뿌리에서 나온 증상이었습니다.
  이미지 단위로 읽으면 그 흔들림이 구조적으로 없어집니다:
  - 박스가 흔들리지 않습니다. 좌표가 그 이미지 안의 비율이라 스크롤·확대·창
    크기와 무관합니다. 화면에 두 장이 걸쳐 있으면 각자 제 이미지를 따라 움직입니다
  - 같은 이미지는 반드시 캐시에 맞습니다. 키가 이미지 주소라 어긋날 자리가 없습니다
  - 원본 해상도로 읽습니다. 화면 배율에 눌리지 않아 OCR 에 유리합니다
  - 화면이 깜빡이지 않습니다. 캡처하지 않으니 오버레이를 숨겼다 되살릴 일이 없습니다
  - 이미지 바이트를 못 받으면(CORS·핫링크 차단·CSP) 조용히 캡처로 내려갑니다.
    되던 사이트가 나빠지지는 않습니다
- **더블클릭 하나로 켜지는 런처.** `MangaLiveReader.cmd` 를 누르면 없는 것만
  채우고(uv·파이썬·의존성·모델 가중치·API 키) 서비스를 띄웁니다. 전부 멱등이라
  두 번째부터는 전부 건너뜁니다. `MangaLiveReader.vbs` 는 콘솔 창 없이 트레이
  아이콘으로 뜹니다 — 재시작·로그 열기·종료
- 런처가 **그래픽카드를 보고 갈래를 정합니다.** NVIDIA 가 아니면 쓰지도 못할
  CUDA 런타임을 몇 GB 씩 받지 않도록 CPU 판으로 갈아탑니다(느리다고 먼저 경고합니다).
  NVIDIA 라도 GTX 10 시리즈는 커널이 없어 설치 후 실제로 대조해 알려 줍니다

### 없어진 것

- **`⋯ → 진단` 버튼.** 세로/가로 판정 경계를 짐작으로 정하지 않으려고 값을
  모으던 발판이었습니다. 네 사이트에서 다 모았고 경계가 정해졌으니 걷어냅니다.
  서비스의 `POST /signals` 도 같이 지웠습니다

### 안쪽

- cu128 고정의 근거를 사실로 고쳤습니다. 「기본 PyPI 휠에는 sm_120 이 없다」는
  torch 2.7 시절 이야기고, 2.11 의 기본 휠은 CUDA 13 이라 RTX 50 이 그냥 돕니다.
  고정을 유지하는 이유는 재현성 하나입니다
- 기동 로그 예시에 박혀 있던 GPU 이름을 자리표시자로 바꿨습니다. 이 기계 사양이지
  요구사항이 아니었습니다
- WSL 셸에서 맨 `uv run` 을 치면 윈도우 venv 가 날아간다는 경고를 기동 지점으로
  올렸습니다. 복구 절차도 적었습니다

## v0.2.3 — 되돌아가면 번역이 안 보이던 것

### 고침

- **읽은 페이지로 되돌아가면 번역이 안 나오고, 「갱신」을 눌러야만 나오던 것.**
  캐시에서 온 좌표를 지금 화면에 맞게 옮기는 함수가 **정의되지 않은 채로 호출되고
  있었습니다.** v0.2.1 에서 파일을 여섯으로 나눌 때 그 함수가 들어갈 자리를 놓쳤고,
  그래서 캐시가 맞을 때마다(=되돌아갈 때마다) 박스가 통째로 그려지지 않았습니다.
  갱신하면 나오던 이유는 그 경로가 캐시를 안 쓰기 때문입니다
- 좌표 환산 결과가 화면 밖으로 나가면 이번 캡처 좌표로 떨어집니다. 조금 어긋나도
  보이는 편이 낫습니다
- 같은 순간 두 번 읽던 것 (상태 객체가 둘로 갈리는 경우)
- 다시 그릴 때 직전 화면의 번역을 물려받습니다. 읽기가 중간에 끊겨도 원문만 남지
  않습니다

### 안쪽

- 확장 연기 시험이 파일 간 호출을 더 넓게 태웁니다. 이번 버그를 못 잡은 자리를
  메웠습니다
- 서비스가 캐시를 왜 안 썼는지 계측에 남깁니다 (`no_cache`·`refresh`·`has_viewer`)
- 테스트가 실서비스 계측 로그에 쓰던 것 수정

## v0.2.2 — 번역이 끝나자마자 날아가던 것

### 고침

- **자동으로 읽은 직후 번역이 사라지던 것.** 읽는 동안 들어온 신호는 쌓아 두었다가
  끝나자마자 처리하는데(빨리 두 장 넘겼을 때를 위한 장치), 그 바람에 「읽고 나면
  기준 화면을 다시 찍는다」 쪽이 자리를 못 잡고 포기했습니다. 기준이 읽기 전
  화면으로 남아 방금 번역한 페이지를 "바뀌었다"고 보고 다시 읽었습니다.
  이제 자리가 날 때까지 다시 시도합니다

### 새로 생긴 것

- **`Alt` 를 누른 채로는 라벨을 끌지 않습니다** — 대신 글자를 긁을 수 있습니다.
  단어를 사전에 넣어 보고 싶을 때 씁니다. 복사·검색은 브라우저 우클릭이 그대로
  합니다. 커서가 바뀌어 지금 어느 쪽인지 보입니다

### 아직 남은 것

같은 화면을 다시 읽는 경우가 완전히 없어지지는 않았습니다 (실측 12건 중 3건).
다만 전부 캐시에서 나오므로 **번역 비용은 들지 않고** 화면도 곧 돌아옵니다.

## v0.2.1 — 자동 번역에서 박스가 어긋나던 것

### 고침

- **자동 번역에서 박스가 엉뚱한 자리에 그려지던 것.** 캐시는 화면 해시로 찾는데
  그건 근사 매칭이라 **크기가 다른 캡처도 같은 항목에 붙습니다.** 저장된 좌표는
  그때 캡처 기준이라 그대로 얹으면 어긋났습니다 (「갱신」을 누르면 맞던 이유가
  이것입니다). 이제 뷰어 사각형을 기준으로 지금 화면에 맞게 환산합니다 —
  **다시 번역하지 않습니다.** 창 크기를 바꿔 가며 확인했습니다
- **손으로 옮긴 라벨이 제자리로 돌아가던 것.** 원문 보기·새 번역 도착·스크롤·
  라벨 펼치기에서 모두 났습니다. 자동 배치가 「옮긴 것은 건드리지 않는다」 분기를
  타지 못하고 있었습니다
- 확장이 보낸 키로 만든 번역기를 요청마다 새로 만들던 것 (커넥션 풀이 쌓입니다)

### 새로 생긴 것

- **라벨을 끌면 원래 말풍선과 점선으로 이어집니다.** 멀리 옮기면 어느 말풍선의
  번역인지 알 수 없어서 넣었습니다. 옮겨 둔 라벨에 마우스를 올려도 보입니다

### 안쪽

- `content.js` 2,565줄을 여섯 파일로 나눴습니다 (진입점 · 영역 선택 · 패널 ·
  음성 · 박스/라벨 · 자동 감지)
- 확장 연기 시험을 넣었습니다 (`docs/smoke.html`). 콘텐츠 스크립트는 manifest
  순서대로 같은 전역에서 실행되어 순서 하나로 통째로 깨지는데, 브라우저에 올려
  보기 전에는 안 보입니다

**확장을 다시 읽어야 합니다** — 파일 구성이 바뀌었습니다.

## v0.2.0 — 로컬 모델, 프로바이더별 키

### 로컬 모델로 번역

같은 망에 띄운 LLM 으로 번역할 수 있습니다. OpenAI 호환이면 무엇이든 됩니다
(Ollama, LM Studio, vLLM). API 로 나가지 않으니 **돈이 들지 않고 원문도 밖으로
나가지 않습니다.**

```toml
[api]
local_model = "gemma4:e4b"
local_base_url = "http://127.0.0.1:11434/v1"
```

확장 옵션의 모델 목록에서 「로컬 모델」을 고르면 그쪽으로 갑니다.

**RTX 5080 + gemma4:e4b 실측** (실제 만화 페이지):

| | 말풍선 | 번역 |
|---|---|---|
| gemma4:e4b (로컬) | 15개 / 23개 | 24.6초 / 20.9초 |
| gemini-3.6-flash | 19개 | 15.3초 |

빠짐없이 번역되고 효과음 분류도 맞습니다. 다만 **문체 일관성과 오역에서 클라우드
모델보다 떨어집니다** — 같은 인물의 존댓말·반말이 섞이고, OCR 이 뭉갠 원문을
`ocr_noise` 로 표시하지 않고 그럴듯하게 지어내는 경우가 있습니다. 품질이 중요하면
claude-sonnet-5 를, 비용이 중요하면 로컬을 쓰십시오.

### 번역 키를 프로바이더별로

확장 옵션에 Claude / Gemini / ChatGPT 키 칸이 따로 생겼습니다. 고른 모델에 맞는
칸 하나만 서비스로 보내고, 서비스는 그 키로 번역합니다(저장하지 않습니다).
**서비스 쪽에 키를 넣어 두었다면 비워 두십시오** — 그게 기본입니다.

### Tailscale 이 필수가 아닙니다

바인딩 주소를 직접 적을 수 있습니다. 다른 VPN 이나 LAN 주소도 됩니다.

```toml
[server]
bind_host = "192.168.0.42"
```

막는 것은 `0.0.0.0` 하나뿐입니다 — 설정 실수로 모든 인터페이스에 열리는 것만
거부합니다.

### 그 밖에

- 로컬·ChatGPT 도 **완성된 말풍선부터** 흘립니다 (기존에는 다 받은 뒤 한꺼번에)
- 옵션 화면 문구 정리. 「번역 모델」의 비용표를 빼고 로컬 모델 설명을 제대로 적었습니다
- 작은 모델이 번역문 자리에 분류값을 넣던 문제 수정 (프롬프트에 출력 예시 추가)
- 개발자 개인 설정(`service.local.toml`)이 테스트에 새어 들어가던 것 수정

### 알아 두실 것

- 로컬 모델을 쓰려면 Ollama 의 컨텍스트를 늘리십시오. 기본 4096 으로는 말풍선이
  15개만 넘어도 프롬프트가 창을 넘겨 번역이 중간에 끊깁니다
  (`OLLAMA_CONTEXT_LENGTH=16384`)
- ChatGPT 경로는 **로컬(Ollama)로만 실측했습니다.** OpenAI 본가는 미검증입니다

## v0.1.0 — 첫 공개

일본 만화 뷰어 화면을 캡처해 **원래 말풍선 위에** 번역을 겹쳐 보여 줍니다.
번역본을 따로 만들지 않고, 보고 있는 화면에 그대로 얹습니다.

### 되는 것

- **화면 캡처 → 말풍선 검출 → OCR → 번역 → 겹쳐 표시**. `Alt+Shift+M` 한 번이면
  됩니다. 뷰어 영역은 자동으로 찾고, 못 찾으면 마우스로 직접 고를 수 있습니다.
- **자동 모드** — 켜 두면 페이지를 넘길 때마다 알아서 다시 읽습니다. 화면 해시가
  달라졌을 때만 읽으므로 같은 페이지를 두 번 번역하지 않습니다.
- **번역 스트리밍** — 말풍선이 완성되는 대로 하나씩 올라옵니다. 원문은 0.3초,
  첫 번역은 4.8초에 나갑니다 (전부 기다리면 9.8초였습니다).
- **페이지 캐시** — 한 번 읽은 페이지는 다시 읽지 않습니다 (7ms). 같은 회차를
  다시 넘겨 봐도 돈이 들지 않습니다.
- **음성 읽기** — 원문을 읽기 순서대로 소리내어 읽습니다. 기본은 브라우저 내장
  음성이고, 학습된 목소리를 쓰려면
  [gpt-sovits-server](https://github.com/vanillapapaya/gpt-sovits-server)
  를 따로 띄웁니다.
- **다른 기기에서 쓰기** — 서비스는 GPU 있는 PC 한 대에서만 돌리고, 맥북이나 다른
  PC 는 확장만 깔면 됩니다 (Tailscale).

### 비용

200쪽 한 권에 **claude-sonnet-5 가 약 $1.64**, gemini-3.6-flash 가 $2.44 입니다.
Gemini 는 단가가 절반인데 사고 토큰이 3.3배라 실제로는 더 비싸고 더 느렸습니다.

### 알아 두실 것

- **NVIDIA GPU 를 권합니다.** CPU 로도 돌지만 OCR 이 10배 느려 실사용이 어렵습니다.
- **API 키는 본인 것을 쓰십니다.** 서버는 아무도 호스팅하지 않습니다. 키는 본인 PC
  안 파일 하나에만 있고, 확장은 키를 모릅니다. 만화 이미지는 본인 PC 밖으로 나가지
  않고, 읽어 낸 글자만 번역 API 로 갑니다.
- **Retina(2배 화면)는 아직 검증하지 못했습니다.** 라벨이 말풍선에서 절반만큼
  밀린다면 그 문제입니다.
- 효과음 OCR 은 자주 틀립니다. 검출은 되지만 읽기 모델의 한계입니다.
- 읽기 순서는 52장 실측에서 위치 정확도 80%, 쌍 정확도 97% 입니다. 칸 경계를 보지
  않기 때문에 복잡한 배치에서 가끔 밀립니다.

### 설치

컴퓨터를 잘 모르셔도 30분이면 됩니다 — **[QUICKSTART.md](QUICKSTART.md)** 를
그대로 따라 하십시오. 기술적인 내용은 [README.md](README.md) 에 있습니다.

확장만 따로 받으시려면 아래 `manga-live-reader-extension-v0.1.0.zip` 을 풀어
`chrome://extensions` → 개발자 모드 → 압축해제된 확장 프로그램을 로드 로 넣으시면
됩니다. **서비스는 따로 띄워야 합니다** — 확장만으로는 동작하지 않습니다.
