# Manga Live Reader - 설계 명세 v5

일본 만화 뷰어 화면을 실시간 캡처해 OCR + 번역 결과를 **화면 위에 직접 겹쳐** 표시하는 개인용 읽기 도구.

대상 사이트: comic-walker.com, yanmaga.jp, shonenjumpplus.com

**v2 대비 변경**: 보조 패널 → 인플레이스 오버레이. 번역이 별도 창이 아니라 말풍선 옆/안에 나타난다. 표시 강도는 L1/L2/L3 세 단계 토글.

**v1 대비 변경**: 단일 프로세스 → 서비스/클라이언트 분리. 연산은 서비스 머신(RTX 5080) 한 곳에 고정하고, 맥북은 Tailscale로 붙는 원격 클라이언트가 된다.

---

## v3 → v4 변경 이력

v3는 구현 전에 쓴 문서다. M1/M2를 만들고 실물 52장으로 측정하면서 **전제 네 개가 틀린 것으로 드러났다.** 여기 모아 둔다 — 본문은 사실로 고쳤고, 왜 바뀌었는지는 이 절과 각 절의 각주에 남긴다.

| 절 | v3의 서술 | 실제 | 근거 |
|---|---|---|---|
| §3.1 | comic-text-detector가 `is_bubble`을 산출 | **주지 않는다.** 그 모델의 yolo 클래스는 말풍선 여부가 아니라 **언어**다 | 체크포인트 cfg `nc=2`, 원본 `utils/textblock.py`의 `LANG_LIST` |
| §8.2 | LLM 재배열로 읽기 순서를 교정 | **역효과.** XY-cut 36/45 → LLM 26~33/45. 이미 맞은 페이지를 망가뜨린다 | 실물 4장 45 region, 2개 모델 |
| §8.4 | `natural`→Haiku / `literal`→Sonnet 2계층 | **Haiku 4.5로는 성립하지 않는다.** 캐시 최소 길이 4096토큰이라 번역 프롬프트가 조용히 캐시되지 않고, `effort`/adaptive thinking을 400으로 거부한다 | 실측 |
| §10 | 번역 900ms, 완료 1.9초 | **번역 7-9초.** 출력 450토큰을 생성하는 시간이라 `effort`로 안 줄어든다 | 실물 6장 A/B |

## v4 → v5 변경 이력

| 절 | v4의 서술 | 실제 | 근거 |
|---|---|---|---|
| §3.1 §8.1 | `is_bubble` 로 효과음을 거른다 | **못 거른다.** 스크린톤 배경 위 말풍선을 효과음으로 오판하고, 배경 통계로는 원리적으로 고칠 수 없다 | 다섯 가지 통계 전부 실패 (`DEVLOG.md`) |
| §8.4 | 응답은 `{id, ko, note}` | **`kind` 추가.** 번역 모델이 원문을 보고 `dialogue`/`sfx`/`narration`/`extra` 로 분류한다. 필터는 이걸로 한다 | 실물 4장 52 region: `kind` 전부 정답, `is_bubble` 13개 오답 |

측정에서 확인된 나머지 사실은 `PROGRESS.md`에, 실험 자체는 `DEVLOG.md` 에 있다. 이 문서는 **무엇을 왜 만드는가**를, `PROGRESS.md`는 **지금 어디까지 와 있고 무엇이 남았는가**를, `DEVLOG.md` 는 **무엇을 재봤고 무엇이 나왔는가**를 적는다.

---

## 1. 목표와 성공 기준

### 목표

맥북이든 윈도우 PC든, 뷰어에서 페이지를 넘기면 **별도 조작 없이** 원문이 1초 이내에 말풍선 위에 뜨고, 번역이 뒤이어 채워진다.

### 성공 기준

| 지표 | 윈도우(로컬) | 맥북(원격) | 상태 |
|---|---|---|---|
| 전환 감지 → 원문 표시 | 0.8초 | 1.2초 | 서비스 구간 343ms 실측 |
| 전환 감지 → **첫 번역** 표시 | 5.5초 | 6.0초 | 첫 번역 4.8초 실측 (§10) |
| 전환 감지 → 번역 완료 | 8.5초 | 9.0초 | 전체 7.5초 실측 |
| 페이지당 사용자 조작 | 0회 | 0회 | |
| 말풍선 검출 재현율 | 90% 이상 | 동일 | 84% → 마스크 승격 후 재측정 필요 |
| 캐시 히트 시 표시 지연 | 0.1초 | 0.3초 | 7ms 실측 |
| **오버레이 위치 오차** | **±4px 이내** | **동일** | 미측정 (M3) |

맥북 예산이 더 큰 것은 네트워크 왕복과 이미지 업로드 때문이다.

**v3는 "번역 표시 2.0초"였다.** 실제 번역은 7-9초라 달성 불가능하다 — 근거는 §10. 대신 **원문과 번역을 분리**해 원문을 0.3초에 띄우고 번역을 스트리밍으로 채운다. 읽기 시작은 원문이 뜨는 시점이므로 체감은 원래 목표에 가깝다.

### 비목표

- **인페인팅 모델을 쓰지 않는다.** L3 모드의 말풍선 채우기는 검출 마스크를 단색으로 덮는 수준까지만 한다 *(v2에서 "합성하지 않는다"였던 항목을 축소)*
- 페이지 이미지를 디스크에 영구 저장하지 않는다
- 뷰어의 이미지 전송 방식을 리버스 엔지니어링하지 않는다
- iOS/Android 클라이언트를 만들지 않는다 (백그라운드 화면 감시가 OS 차원에서 불가)
- 브라우저 확장을 만들지 않는다 (§2.4 참조)

---

## 2. 설계 제약

### 2.1 캡처 방식 고정

화면에 이미 렌더링된 픽셀만 읽는다. 브라우저 DOM, 네트워크 요청, canvas 내부 버퍼에 접근하지 않는다.

**근거 (v2에서 수정)**: v2는 "사이트별 대응 코드가 0"을 근거로 삼았으나, 더 본질적인 근거는 따로 있다 — **타일 재구성을 하지 않는다.** 대상 사이트 중 최소 한 곳은 SpeedBinb 엔진을 쓰는데, 이 엔진은 이미지를 타일로 쪼개 뒤섞어 렌더링한다. 타일 순서를 역산해 원본을 재구성하는 것은 기술적 보호조치 무력화로 해석될 여지가 있다. 사용자가 정당하게 열람 중인 화면을 읽는 것은 성격이 다르다.

부수 효과로 사이트별 대응 코드가 0이 되는 것은 맞다.

### 2.2 연산 단일화

OCR/검출 모델은 서비스 머신(RTX 5080)에만 상주한다. 맥북에 중복 배치하지 않는다.

**근거**: 모델을 두 곳에 두면 버전 동기화, 모델 다운로드 관리, 결과 일관성 문제가 생긴다. 맥북 MPS는 5080 대비 2-3배 느려서 성능 이점도 없다. 네트워크 왕복 200-300ms를 지불하는 편이 낫다.

**예외**: 서버 접근 불가 시 맥북 로컬 폴백은 범위 밖. 필요해지면 추가한다.

### 2.3 데이터 수명

- 캡처 이미지: 전송 후 양쪽 모두 메모리에서 폐기
- 캐시: 텍스트 + 마스크만. 이미지는 지각 해시(64bit)만 키로 보관
- 로그: 사용자가 명시적으로 내보내기를 실행한 경우에만 파일 생성

### 2.4 표시 방식: OS 오버레이 창으로 고정

번역을 화면에 겹치는 방법은 두 가지였다.

| | 경로 A — OS 오버레이 창 | 경로 B — 브라우저 확장 |
|---|---|---|
| 정렬 정확도 | 좌표 역환산 필요 (±4px) | 완벽 (DOM 배치) |
| 사이트별 코드 | 0 | 셀렉터 + 프레임 배관 |
| 브라우저 종속 | 없음 | Chromium 계열만 |
| 맥북 포팅 비용 | 큼 (M6 전체) | 거의 없음 |

**경로 A로 확정했다.** 실증 조사 결과(부록 A)가 근거다:

1. 뷰어가 **SpeedBinb** 상용 엔진 위에 사이트별 `custom_*` 레이어를 얹은 구조다. 단일 canvas가 아니라 타일 요소 묶음이라 컨테이너 셀렉터를 따로 찾아야 하고, **서드파티 엔진이 업데이트되면 예고 없이 깨진다**
2. 뷰어가 **교차 출처 iframe** 안에 있다. `all_frames` + 도메인 권한 + 프레임 간 메시지 중계가 필요하다
3. 주력 브라우저(Vivaldi)에서 내장 페이지 캡처가 모든 페이지에서 실패한다. 확장 경로는 만화 읽을 때만 크롬을 강제할 위험이 있다

맥북 포팅은 **한 번 하면 끝나는 비용**이고, 셀렉터 유지보수는 **끝나지 않는 비용**이다. 전자를 택한다.

포기하는 것: 브라우저 사전 확장(Yomitan 등)과의 연동. v2 §5.3이 이를 요구사항으로 적었으나, 확장 사전은 브라우저 DOM 텍스트에만 걸리므로 **Qt 패널에서도 원래 성립하지 않던 요구사항**이다. 새로 잃는 것이 아니다. 대신 원문 클릭 → 클립보드 복사로 대체한다.

---

## 3. 아키텍처

```
┌──────────────────────────────┐        ┌──────────────────────────┐
│  MacBook (클라이언트)        │        │  서비스 머신  RTX 5080     │
│                              │        │                          │
│  ┌────────┐   ┌───────────┐  │        │  ┌────────────────────┐  │
│  │Watcher │──►│  Capture  │  │        │  │  mtl-service       │  │
│  └────────┘   │  + Encode │  │        │  │  (FastAPI, 상주)   │  │
│      ▲        └─────┬─────┘  │        │  │                    │  │
│      │              │        │        │  │  detect → ocr      │  │
│      │        POST /read ────┼────────┼─►│      → order       │  │
│      │              │        │Tailscale│  │      → translate   │  │
│      │        SSE ◄─┼────────┼────────┼──┤      → cache       │  │
│      │              ▼        │        │  └────────────────────┘  │
│      │       ┌───────────┐   │        │           ▲              │
│      └───────┤  Overlay  │   │        │           │ loopback     │
│   그린 rect  │ L1/L2/L3  │   │        │  ┌────────┴───────────┐  │
│   피드백     └───────────┘   │        │  │  Windows Client    │  │
└──────────────────────────────┘        │  │  (동일 코드베이스) │  │
                                        │  └────────────────────┘  │
                                        └──────────────────────────┘
                                                    │
                                                    ▼
                                             Anthropic API
```

**핵심 1**: 윈도우 클라이언트도 HTTP 루프백으로 서비스에 붙는다. 인프로세스 호출로 특수화하지 않는다. 코드베이스가 하나로 유지되고, 루프백 오버헤드는 1ms 미만이라 손해가 없다.

**핵심 2**: Overlay가 Watcher로 되돌아가는 화살표가 v3의 신규 요소다. 오버레이가 자기가 그린 사각형을 Watcher에 알려주고, Watcher는 그 영역을 해시 계산과 업로드에서 제외한다. §5.2 참조.

### 3.1 기술 스택

**서비스 (Windows)**

| 항목 | 선택 | 비고 |
|---|---|---|
| 웹 프레임워크 | FastAPI + uvicorn | SSE 응답 필요 |
| 말풍선 검출 | comic-text-detector | 마스크 + bbox. **`is_bubble`은 주지 않는다** (아래) |
| OCR | manga-ocr | 세로쓰기/손글씨체 대응 |
| 런타임 | PyTorch cu128 이상 | Blackwell(sm_120) 필수 |
| 번역 | Anthropic API | 배치 호출, 스트리밍 |
| 캐시 | SQLite | |

**클라이언트 (Windows / macOS 공용)**

| 항목 | 선택 | 비고 |
|---|---|---|
| 캡처 | `mss` | 양 OS 동작, 플랫폼별 보정 필요 |
| 이미지 처리 | Pillow | 리사이즈/인코딩 |
| **오버레이 창** | **PySide6 + `QPainter`** | **프레임리스 · 투명 · 항상위 · 입력통과** |
| HTTP | httpx + httpx-sse | SSE 클라이언트 |

PyTorch는 반드시 CUDA 12.8 이상 빌드로 설치할 것. RTX 5080은 Blackwell이라 이전 빌드에는 커널이 없다.

**`is_bubble`은 직접 판정한다.** v3는 검출기가 준다고 적었지만 사실이 아니다 — 그 모델의 yolo 클래스는 말풍선 여부가 아니라 **언어**다(체크포인트 cfg `nc=2`, 원본 `utils/textblock.py`의 `LANG_LIST`). `mask.py`가 글자 획 주위 링의 균일도·명도·채도로 판정한다. §7.4가 `fill_confidence`용으로 정의한 신호와 같은 것이다. 검은 말풍선에 흰 글자는 효과음으로 분류되며, 그 region은 L2로 그려지므로 안전한 실패다.

또 하나 — **DBNet 헤드(`text_det`)는 벤더링하지 않았다.** 텍스트 라인 폴리곤을 쓰지 않으므로 박스 병합(§8.1)과 읽기 순서(§8.2)는 직접 구현했다.

---

## 4. API 계약

### 4.1 POST /read

**요청**: `multipart/form-data`

| 필드 | 타입 | 설명 |
|---|---|---|
| `image` | bytes | JPEG q88, 짧은 변 1200-1600px로 정규화됨 |
| `meta` | JSON string | 아래 참조 |

```json
{
  "phash": "a3f2...",
  "profile": "jumpplus",
  "mode": "natural",
  "series_hint": null,
  "prev_page_phash": "b71c...",
  "include_sfx": true
}
```

`prev_page_phash`를 보내면 서버가 캐시에서 직전 페이지 원문을 꺼내 번역 문맥으로 사용한다. 클라이언트가 원문을 다시 올릴 필요가 없다.

**응답**: `text/event-stream` (SSE)

```
event: cached
data: {"hit": true}

event: ocr
data: {"regions":[
  {"id":1,"text":"なんだこれは…","bbox":[820,140,180,90],
   "is_bubble":true,"vertical":true,
   "mask_rle":"...","fill_rgb":[252,252,250],"fill_confidence":0.94},
  ...
]}

event: translation
data: {"id":1,"ko":"이게 대체 뭐야…","kind":"dialogue","note":null}

event: done
data: {"reordered":false,"timings":{"detect":142,"ocr":338,"translate":870}}
```

**region 필드 (v3 신규)**

| 필드 | 용도 |
|---|---|
| `is_bubble` | false면 효과음/배경 텍스트. **L3에서 절대 채우지 않는다** (그림 위에 있어서 채우면 그림이 사라진다) |
| `vertical` | 원문이 세로쓰기였는가. 번역 조판 방향과 L2 라벨 배치 방향을 결정 |
| `mask_rle` | 말풍선 마스크. L3 전용이지만 **항상 내려보낸다** (§4.5) |
| `fill_rgb` | 마스크 외곽 3px의 중앙값. 클라이언트는 원본 픽셀을 이미 버렸으므로 계산할 수단이 없다 |
| `fill_confidence` | L3 안전도. `< 0.8`이면 그 region만 L2로 강등 (§7.4) |

**그 밖의 규약**

- `cached` 이벤트가 `hit: true`면 곧바로 `ocr`과 전체 `translation`이 연달아 방출되고 종료된다
- `bbox`와 `mask_rle`는 **전송된 이미지 좌표계** 기준이다. 클라이언트가 자신의 다운스케일 비율을 알고 있으므로 화면 좌표 환산은 클라이언트 책임이다. 서버는 화면 해상도나 DPI를 알 필요가 없다
- 에러는 `event: error`로 방출하고 스트림을 닫는다. 클라이언트는 다음 페이지에서 재시도한다

### 4.2 GET /health

```json
{"status":"ok","models_loaded":true,"gpu":"NVIDIA GeForce RTX 5080","vram_used_mb":1420}
```

클라이언트가 시작 시 호출해 모델 상태를 확인하고, 미로드 상태면 워밍업을 유도한다.

### 4.3 POST /warmup

모델을 강제 로드한다. 응답까지 3-5초. 클라이언트 기동 시 비동기로 1회 호출.

### 4.4 인증

모든 요청에 `X-Auth-Token` 헤더 필요. 값은 양쪽 설정 파일에 둔 공유 시크릿.

서비스는 **Tailscale 인터페이스에만 바인딩**한다. `0.0.0.0`에 열지 않는다. Tailscale ACL로 이미 접근이 통제되므로 토큰은 이중 방어다. 실수로 다른 인터페이스에 바인딩됐을 때 마지막 방어선이 된다.

### 4.5 마스크는 조건부로 내려보내지 않는다

`include_mask` 같은 요청 플래그를 두지 않는다. 이유:

- 조건부 필드는 **캐시를 오염시킨다.** L2로 캐시된 페이지를 L3로 보려면 재실행해야 한다
- RLE 마스크는 페이지당 수 KB다. 대역폭 부담이 없다
- 항상 받아 캐시에 저장해두면 **L1↔L2↔L3 토글이 순수 클라이언트 리렌더가 된다.** 지연 0, API 호출 0

이는 §8.5가 OCR과 번역을 분리 저장하는 것과 같은 논리다.

---

## 5. 클라이언트 — 감지와 캡처

### 5.1 Watcher - 페이지 전환 감지

체감 속도를 결정하는 핵심 모듈. 양 OS 공통 로직.

```
매 poll_interval_ms:
    frame = capture(region, downscale_to=256)

    if is_blank(frame):            # ← v3 신규, §5.3
        report_blank(); continue

    frame = mask_overlay_rects(frame)   # ← v3 신규, §5.2
    h = dhash(frame, size=8)

    if hamming(h, last_hash) > THRESHOLD:
        stable_count = 0
    else:
        stable_count += 1

    if stable_count == stable_frames and h != processed_hash:
        overlay.clear()            # ← v3 신규, §7.2
        trigger_pipeline(h)
        processed_hash = h

    last_hash = h
```

- `THRESHOLD`: 해밍 거리 5 (설정 가능)
- 페이지 넘김 애니메이션 중에는 계속 변하므로 N프레임 연속 안정을 요구
- **`stable_frames`는 프로필별 설정이다** (v3 변경). SpeedBinb는 타일을 점진적으로 렌더링하므로 전역 2프레임으로는 로딩 중간에 트리거될 수 있다. 사이트마다 튜닝한다
- 되돌아가기 시에는 서버 캐시가 즉시 응답하므로 별도 처리 불필요
- 폴링용 캡처는 256px로 다운샘플하므로 CPU 비용은 무시할 수준

### 5.2 캡처 피드백 루프 차단

**v3에서 가장 중요한 신규 요구사항이다.**

오버레이를 화면에 그리면 다음 폴링 캡처에 그 한국어가 찍힌다 → 해시가 바뀐다 → 재트리거된다 → **우리가 쓴 한글을 OCR하게 된다.**

OS API(`WDA_EXCLUDEFROMCAPTURE`, `NSWindowSharingNone`)에 의존하지 않는다. 플랫폼별로 동작이 갈리고 조용히 깨진다. 대신:

```
클라이언트는 자기가 어디에 무엇을 그렸는지 정확히 안다.
→ Overlay가 그린 rect 목록을 Watcher에 등록한다.
→ dhash 계산 전, 그 rect를 균일 회색으로 덮는다.
→ 업로드용 JPEG도 동일하게 덮는다.
```

OS API 없음, 플랫폼 분기 없음, 100% 결정적.

### 5.3 검은 화면 자동 판정

캡처가 조용히 실패하는 경로가 여럿이다 — macOS 화면 기록 권한 미부여, 보호 콘텐츠, GPU 합성 문제. 전부 **예외 없이 검은 이미지**를 반환한다.

```
캡처 직후, 업로드 전:
    if 표준편차 < 임계 or 픽셀 고유값 < 8개:
        → 상태 표시에 "화면을 읽지 못했습니다"
        → 원인 후보 3개 안내 (권한 / 보호 콘텐츠 / GPU 가속)
        → 서버로 보내지 않는다   ← 검은 이미지로 OCR 돌려봐야 시간만 버린다
```

Watcher의 256px 썸네일 단계에서 이미 판정 가능하므로 폴링 비용이 추가되지 않는다.

### 5.4 Capture - 플랫폼별 보정

**여기가 크로스플랫폼에서 가장 자주 깨지는 지점이다.** 플랫폼 분기는 이 계층 한 곳에만 둔다.

**macOS**

- **화면 기록 권한(TCC)** 이 필요하다. 최초 실행 시 시스템 설정 - 개인정보 보호 및 보안 - 화면 기록에서 터미널 또는 앱 번들을 허용해야 한다. 권한이 없으면 `mss`가 예외 없이 검은 화면을 반환한다 → §5.3이 잡는다
- **Retina 스케일**: 논리 좌표 1200pt 영역을 캡처하면 2400px가 나온다. `mss`의 `monitors` 정보와 실제 반환 크기를 비교해 배율을 산출하고, 리전 좌표를 물리 픽셀로 환산한다
- 전역 단축키를 쓸 경우 **손쉬운 사용(Accessibility) 권한**도 별도로 필요하다

**Windows**

```python
import ctypes
ctypes.windll.shcore.SetProcessDpiAwareness(2)  # PROCESS_PER_MONITOR_DPI_AWARE
```

선언하지 않으면 배율 125%/150% 환경에서 좌표가 어긋난다.

**공통 - 전송 전 정규화**

캡처 원본을 그대로 올리면 안 된다. 맥북 Retina 캡처는 3-5MB에 달해 업로드만 300ms 이상 걸린다.

```
1. 짧은 변이 1600px 초과 → 1600px로 축소
2. 짧은 변이 1200px 미만 → 1200px로 확대 (Lanczos)
3. JPEG q88로 인코딩  → 목표 250-400KB
4. 적용한 스케일 비율을 로컬에 보관 (bbox 역환산용)
```

만화 텍스트 OCR에 1200-1600px면 충분하다. 그 이상은 대역폭만 낭비한다.

---

## 6. 좌표 역환산

오버레이 정확도를 결정하는 계산이다. §1의 ±4px 예산이 여기서 소진된다.

```
서버 bbox (전송 이미지 좌표)
  ÷ normalize_scale        → 캡처 리전 내 물리 픽셀
  + region_origin_physical → 화면 물리 픽셀
  ÷ display_scale          → 화면 논리 좌표 (Qt가 쓰는 좌표계)
```

- `normalize_scale`: §5.4의 4번에서 보관한 값
- `region_origin_physical`, `display_scale`: 캡처 계층이 제공. **캡처 대상 모니터를 특정하고 그 모니터의 배율을 쓴다.** 외부 모니터 연결 시 모니터마다 다를 수 있다
- 누적 오차는 세 번의 나눗셈에서 각 ±1px, 합 ±3px 수준. L1/L2에는 무해하고 L3에는 아슬아슬하다 → §7.4의 폴백이 필요한 이유

---

## 7. 오버레이 렌더링

### 7.1 창 속성

```python
flags = (Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint
         | Qt.Tool | Qt.WindowTransparentForInput)
self.setAttribute(Qt.WA_TranslucentBackground)
```

- **기본 클릭 통과.** 브라우저 페이지 넘김 클릭이 그대로 먹어야 한다
- `Alt`(mac `Option`) 홀드 시 입력 수용 → 원문 클릭으로 클립보드 복사
- macOS: `collectionBehavior`에 `canJoinAllSpaces` + `fullScreenAuxiliary` 필수. 안 넣으면 브라우저 전체화면에서 오버레이가 사라진다

부록 A의 테스트 3 결과에 따라, **오버레이가 포커스를 가져가도 뷰어가 블랭크되지 않는다.** 따라서 `WindowDoesNotAcceptFocus`는 블랭크 방지가 아니라 클릭 통과 목적으로만 쓴다. 리전 드래그 선택 UI(§9)도 안전하게 포커스를 가져갈 수 있다.

### 7.2 오버레이 수명 — 불변식 하나

```
오버레이 수명 = [결과 도착]  ~  [Watcher가 다음 변화 감지]
```

스크롤이든 창 이동이든 페이지 넘김이든 전부 해시 변화로 잡힌다. 따라서 **변화 감지 = 오버레이 즉시 소거**.

이 불변식 덕분에 창 위치 추적(WinEvent hook / AXObserver) 코드가 통째로 불필요하다. 이미 만든 Watcher를 재사용하는 것이라 공짜다.

### 7.3 표시 단계 L1/L2/L3

```
L1 핀            L2 라벨                     L3 말풍선 채우기
┌─────────┐     ┌─────────┐                ┌─────────┐
│  ①  ②   │     │ ┌──┐    │                │ ┌────┐  │
│         │     │ │그│ ┌──────────┐        │ │이게│  │
│ (호버 시│     │ │림│ │이게 뭐야…│        │ │ 뭐 │  │
│  툴팁)  │     │ └──┘ └──────────┘        │ │ 야…│  │
└─────────┘     └─────────┘                │ └────┘  │
그림 안 가림     그림 거의 안 가림          완전 대체
```

| 단축키 | 모드 | 좌표 정밀도 요구 | 마스크 |
|---|---|---|---|
| `Alt+0` | 끄기 — 원본 확인 | — | — |
| `Alt+1` | **L1 핀** — 번호 원형 + 호버 툴팁 | 낮음 (±20px 무관) | X |
| `Alt+2` | **L2 라벨** — 말풍선 옆 반투명 박스 *(기본값)* | 중간 | X |
| `Alt+3` | **L3 채우기** — 말풍선 내부 대체 | 높음 (±2px) | O |

순환(`Alt+O` 반복)이 아니라 직접 지정으로 간다. 순환은 원하는 모드까지 3번 눌러야 한다.

**모드는 프로필별로 기억한다.** 효과음이 서사에 개입하는 점프+는 L1이 낫고, 대사 위주 페이지는 L3가 낫다 — 사이트마다 답이 다르다.

### 7.4 L3 자동 폴백 — L3의 생존 조건

L3는 마스크가 어긋나면 그림을 뭉갠다. 전역 토글로 켜두고 방치하면 안 되고, **region 단위로 자동 강등**한다.

서버가 `fill_confidence`로 아래를 종합해 계산한다:

```
is_bubble == false           → 0.0  (효과음. 그림 위에 있어 채우면 그림이 사라진다)
mask면적 / bbox면적 > 0.9    → 감점 (마스크가 박스 전체를 먹음 = 검출 실패)
fill_rgb 채도 높음           → 감점 (흰 말풍선이 아니라 그림 위 텍스트)
```

클라이언트는 `fill_confidence < 0.8`이면 **그 region만** L2로 그린다. 페이지 전체가 아니라 말풍선 하나 단위로 강등하는 게 중요하다.

추가로 **L3에서 말풍선 클릭 → 그 풍선만 원본으로 되돌리기(peek)**. 자동 폴백이 놓친 경우의 탈출구다.

### 7.5 조판 규칙

`mtl_shared/layout.py`에 두어 어느 렌더러가 구현하든 동일하게 나오도록 한다.

- **`vertical: true`면 번역도 세로쓰기로 조판한다.** 세로로 좁고 긴 말풍선에 한국어를 가로로 넣으면 거의 100% 안 들어간다. 세로쓰기가 원본 레이아웃과 폰트 크기에 그대로 맞는다. L3에서는 선택이 아니라 필수 조건이다
- 폰트: 박스에 맞을 때까지 축소, 하한 11px. 하한에서도 넘치면 말줄임 + 호버 확장
- **L2 라벨 배치**: bbox의 우/하/좌 여백 중 다른 bbox와 겹치지 않는 최대 영역에 그리드 스냅. 자리가 없으면 bbox 위에 알파 0.85로 덮고 리더 라인을 긋는다
- **스트리밍 반영**: `ocr` 도착 시 박스 윤곽(L3는 채우기까지) 먼저, `translation` 도착 시 글자를 넣는다. 번역을 기다리지 않는다

---

## 8. 서비스 파이프라인

### 8.1 Detect

`comic-text-detector`로 텍스트 영역 마스크와 바운딩 박스를 얻는다.

- **`is_bubble` 로 필터하지 않는다 (v5).** 이 값은 스크린톤 배경 위 말풍선을 오판한다 — 실물에서 명백한 대사 4개가 한 페이지에서 잘못 걸러졌다. 배경 통계로는 고칠 수 없다는 것까지 확인했다 (`DEVLOG.md`). `include_sfx` 는 **켜 둔 채로** 두고, 효과음·잡문은 §8.4 의 `kind` 로 클라이언트에서 숨긴다
- **`is_bubble` 값은 응답에 그대로 실어야 한다** — L3 폴백이 이 값을 쓴다. 필터가 아니라 렌더링 힌트로만 쓴다
- 면적이 이미지의 0.05% 미만인 박스는 노이즈로 폐기
- 같은 말풍선 마스크 안에서 줄 단위로 쪼개진 박스는 **병합 후** OCR에 투입한다. 쪼갠 채로 넣으면 세로쓰기 줄 순서가 뒤집힌다
- 마스크를 RLE로 인코딩하고, 마스크 외곽 3px 링의 중앙값 색을 `fill_rgb`로 계산한다

**병합 허용 간격에는 절대 상한이 필요하다 (v4).** 벌어진 방향의 박스 크기에 비례시키기만 하면 세로쓰기 박스가 길쭉해서 가로로 멀리까지 손을 뻗는다 — 실물 52장에서 간격 병합 30건 중 24건이 30px를 넘었고 전부 오류였다. 한 말풍선 안 줄·열 간격은 박스 크기가 아니라 **페이지 해상도**에 묶이는 값이다. 자세한 것은 `PROGRESS.md` §7.

**yolo 헤드가 놓친 것은 마스크에서 줍는다 (v4).** 효과음(「ダッ!!」「ジャキ」…)은 yolo 블록 헤드가 박스를 아예 제안하지 않는다 — `detect_threshold`를 낮춰도 안 나온다. 반면 세그멘테이션 마스크에는 선명히 찍혀 있으므로, 팽창으로 획을 이어 덩어리를 만들고 yolo 박스가 덮지 않은 것을 region으로 올린다. 노이즈를 가르는 신호는 **이진화 전 마스크 원본값**이다 (진짜 글자 193-221, 그림 오검출 85-142).

### 8.2 Order - 읽기 순서 정렬

일본 만화는 우상단에서 좌하단으로 읽는다. 이 모듈 품질이 번역 품질을 좌우한다.

**재귀 XY-cut**

```
1. 펼침면이면 페이지부터 가른다 — 가운데 1/3 구간의 세로 빈 띠에서 자르고
   오른쪽 페이지 전부를 먼저
2. 페이지 안에서는 가로 빈 띠(= 칸 행)를 우선한다. 위 → 아래
3. 가로로 못 자르면 세로 빈 띠로 자르고 오른쪽 → 왼쪽
4. 둘 다 없으면 x 내림차순
```

**방향은 간격 크기가 아니라 구조의 위계로 정한다.** "가장 넓은 틈"으로 고르면 한 행 안
좌우 말풍선 간격(900px)이 행 사이 간격(230px)보다 넓어서 행을 가로질러 버린다.

**v3의 컬럼 밴드 휴리스틱은 폐기했다.** x 중심으로만 묶어서 칸 행 개념이 없었고, 실물에서
51개 중 26개(51%) 실패했다 — "복잡한 페이지에서 10-15%"라는 예상보다 훨씬 나빴다. 쉬운
페이지에서도 틀렸다.

**한계**: 칸 테두리를 모르고 글자 배치의 빈 띠만 본다. 한 칸 안에서 말풍선과 캡션이 세로로
떨어져 있으면 그 사이를 칸 행 경계로 착각해, 같은 칸의 아래쪽 것이 왼쪽 칸 뒤로 밀린다.
검증 페이지 기준 위치 정확도 80%, 쌍 정확도 97%.

**v3의 완화책(LLM 재배열 교정)은 폐기했다.** 실측에서 **역효과**였다 — XY-cut 36/45인데
LLM을 태우면 26~33/45로 내려가고, 이미 맞은 페이지를 망가뜨린다. 두 방향 모두 실패한다:
좌표를 안 주면 판단 근거가 없어 입력 순서를 그대로 베끼고, 좌표와 기하 규칙을 주면 XY-cut을
LLM더러 다시 구현하라는 요구가 되어 결정론적 알고리즘보다 못한다. LLM의 강점은 기하가 아니라
대사 흐름인데, 그걸 쓰려면 좌표가 필요하고 좌표를 주면 기하로 풀려 든다.

남은 실패는 **칸 검출**로 풀 문제지 번역기로 덮을 문제가 아니다. 응답의 `reordered` 플래그는
항상 `false`다.

### 8.3 OCR

정렬된 박스를 잘라 **배치로** 투입한다. 하나씩 순차 호출하면 모델 오버헤드가 박스 수만큼 곱해진다.

- 크롭 시 상하좌우 4px 패딩
- 결과가 빈 문자열이거나 1글자면 오검출로 보고 제외
- **이 시점에 `ocr` 이벤트를 즉시 방출한다.** 번역을 기다리지 않는다

### 8.4 Translate

**페이지 전체를 1회 호출로 처리한다.** 말풍선당 호출은 지연이 선형으로 늘고 문맥이 끊긴다.

```json
{
  "series_hint": null,
  "previous_page": ["직전 페이지 원문 배열 (최대 10개)"],
  "current_page": [{"id": 1, "text": "..."}],
  "mode": "natural"
}
```

`previous_page`가 중요하다. 만화 대사는 페이지를 넘어 이어지므로 직전 문맥 없이는 주어 생략 문장의 화자를 놓친다. 서버가 `prev_page_phash`로 캐시에서 자동 조회한다.

스트리밍으로 받아 `translations` 배열 요소가 완성되는 대로 SSE `translation` 이벤트로 중계한다.

**`kind` 분류 (v5).** 응답의 region 마다 `dialogue` / `sfx` / `narration` / `extra` 를 받는다.
클라이언트는 이걸로 효과음·잡문을 숨긴다.

| 값 | 뜻 | 기본 표시 |
|---|---|---|
| `dialogue` | 인물이 말하거나 생각하는 것 | 보임 |
| `narration` | 이야기의 일부인 지문 — 나레이션 상자, 화 제목, 간판 | 보임 |
| `sfx` | 효과음 | 숨김 |
| `extra` | 이야기 밖의 글 — 사이트 UI, 광고, 판권 | 숨김 |

**§3.1 의 `is_bubble` 로 거르지 않는다.** 그 값은 스크린톤 배경 위 말풍선을 오판하고,
배경 통계로는 고칠 수 없다는 것을 측정으로 확인했다 (`DEVLOG.md` — 다섯 가지 시도
전부 실패). 번역 모델이 **원문 텍스트를 보고** 내리는 판단이 훨씬 정확하다.

실측(4장 52 region): `kind` 는 전부 맞고 `is_bubble` 은 13개 틀렸다. 양방향 모두 —
스크린톤 말풍선을 효과음으로(4건), 나레이션 상자를 말풍선으로(5건).

`is_bubble` 을 **모델 입력에도 넣지 않는다.** 넣었더니 명백한 대사 4개가 그 힌트
하나 때문에 전부 잘못 분류됐다. "참고만 하라" 는 프롬프트로는 안 막힌다.

**번역 모드**

| 모드 | 용도 | 지시 |
|---|---|---|
| `natural` | 빠른 내용 파악 | 한국어 만화 대사로 자연스럽게. 주석 없음 |
| `literal` | 원문 분석 | 어순과 문장 구조 보존. 1인칭 선택, 어미(だぜ/わよ/でございます), 경어 등급, 방언이 화자 특성을 드러내면 `note`에 명시 |

`literal`은 자연스러운 의역이 가장 먼저 버리는 정보 — 말투, 관계 설정, 시대극 어투 — 를 살리기 위한 모드다.

**모델은 두 모드 모두 Sonnet 5다 (v4).** v3는 `natural`→Haiku / `literal`→Sonnet 2계층을 적었지만 Haiku 4.5로는 성립하지 않는다:

- **캐시 최소 길이가 4096토큰**이라 번역 시스템 프롬프트(1-1.5K토큰)가 **조용히 캐시되지 않는다.** 에러도 안 나고 매번 정가로 청구된다. Opus 5는 512, Sonnet 5는 1024토큰
- `effort`/adaptive thinking을 **400으로 거부**한다

2계층을 정당화할 측정도 아직 없다. 나누려면 먼저 재 볼 것.

**프로바이더 중립으로 구현했다 (v4).** 일본어→한국어 만화 번역 품질은 공개 벤치마크로 답이 안 나온다. 같은 페이지를 여러 모델에 태워 직접 비교했다 (`scripts/ab_translate.py`, 실물 6장):

| | 지연 중앙값 | 출력 토큰 | 200쪽 1권 |
|---|---|---|---|
| **claude-sonnet-5** | **8.7초** | **444** | **$1.64** |
| gemini-3.6-flash | 11.0초 | 1,477 | $2.44 |

Gemini는 단가가 절반인데 사고 토큰이 3.3배라 **실제로 더 비싸고 더 느리다.**

**응답은 JSON 스키마로 강제하고, 스트리밍으로 받으며 완성된 원소부터 중계한다 (v4).** 스트리밍 JSON에서 중괄호를 세어 완성된 배열 원소만 뽑으면(`jsonstream.py`) 스키마 강제를 유지한 채 증분 처리가 된다 — JSONL로 바꿔 스키마를 포기할 필요가 없다.

`note`는 L1 툴팁과 L2 라벨에만 표시한다. L3는 말풍선 안에 들어갈 공간이 없다.

### 8.5 Cache

```sql
CREATE TABLE page_cache (
    phash       TEXT PRIMARY KEY,
    profile     TEXT,
    ocr_json    TEXT,        -- 마스크·fill_rgb·is_bubble 포함
    trans_json  TEXT,
    mode        TEXT,
    created_at  INTEGER
);
CREATE INDEX idx_created ON page_cache(created_at);
```

- 이미지는 저장하지 않는다. 해시만 키로 사용
- 정확 일치 외에 해밍 거리 3 이내 근사 매칭 허용 (스크롤 미세 차이 흡수)
- OCR과 번역을 분리 저장한다. `natural`로 읽은 페이지를 나중에 `literal`로 다시 볼 때 OCR을 재실행하지 않는다
- 90일 초과 항목 정리 태스크

---

## 9. 서비스 운영 (Windows)

### 9.1 상시 가동

맥북에서 읽으려면 서비스 머신가 켜져 있어야 한다. 전원 설정에서 절전을 끈다. 디스플레이만 끄기로 설정.

*(선택)* Arch 서버(3080)가 상시 가동 중이므로, 맥북 클라이언트가 서버 무응답을 감지하면 Arch에 WoL 매직 패킷 발송을 요청하는 경로를 둘 수 있다. 다만 부팅 + 모델 로드에 1분 이상 걸려 즉시성이 없다. 우선순위 낮음.

### 9.2 자동 시작

Windows 작업 스케줄러에 등록한다. 서비스로 만들지 않는다.

- 트리거: 로그온 시
- 동작: `pythonw.exe -m mtl_service`
- 서비스(SCM)로 등록하면 세션 0에서 실행되어 GPU 접근이 까다로워진다. 사용자 세션 프로세스가 단순하고 확실하다

### 9.3 VRAM 공존

ComfyUI를 같은 GPU에서 돌리는 경우를 고려한다.

- 상주 모델은 검출 + OCR 합쳐 1.5GB 미만. 16GB 중 10% 미만이므로 기본값은 **상주 유지**
- 대형 워크플로 실행 시 충돌하면 `ttl_seconds` 설정으로 유휴 N분 후 해제하도록 전환. 단 재로드에 3-5초가 붙으므로 필요할 때만 켠다
- `/health`에 VRAM 사용량을 노출해 판단 근거를 제공한다

**실제 비용은 VRAM이 아니라 SM 경합이다 (v4 정정).** v3는 이 절을 VRAM 문제로 다뤘는데
측정 결과 그렇지 않다 — 그림 생성이 유휴일 때 VRAM은 7.8/16.3GB가 잡혀 있는데도 우리
지연은 정상이었다. 반대로 실제로 연산이 돌면 같은 입력의 OCR 지연이 **53ms ↔ 680ms로
13배** 흔들린다. `ttl_seconds`로 모델을 내려도 이 문제는 해결되지 않는다.

성능을 판단하기 전에 반드시 확인할 것:

```
nvidia-smi --query-gpu=utilization.gpu,power.draw --format=csv
```

WSL에서는 `--query-compute-apps`가 항상 비어 나온다 — Windows 프로세스가 안 보이는
것뿐이니 이걸로 판단하지 말고 utilization/power를 볼 것.

### 9.4 관측

각 단계 소요 시간을 구조화 로그(JSONL)로 남긴다. 예산 초과 시 어느 구간인지 즉시 특정할 수 있어야 한다.

```json
{"ts":"...","phash":"a3f2","client":"macbook","detect":142,"ocr":338,
 "translate":870,"upload_bytes":312044,"cached":false}
```

---

## 10. 성능 예산

**v4에서 전면 재작성했다.** v3는 번역을 900ms로 잡아 완료 1.9초를 목표했지만, 실제 번역은
**7-9초**다. 페이지당 출력 450토큰을 생성하는 시간이라 `effort`를 낮춰도 줄지 않는다 —
thinking 때문이 아니다.

그래서 **원문과 번역을 분리한 예산**으로 바꿨다. 읽기 시작은 원문이 뜨는 시점이므로,
원문 구간은 v3 목표를 그대로 지키고 번역은 스트리밍으로 채운다.

### 윈도우 (로컬, 루프백)

| 단계 | 예산 | 실측 |
|---|---|---|
| 변화 감지 지연 | 300-600ms | 미측정 (M4) |
| 캡처 + 정규화 + 인코딩 | 90ms | 미측정 (M3) |
| 전송 (루프백) | 5ms | |
| 검출 (+ 마스크 RLE) | 165ms | 28ms |
| OCR (배치 10개) | 350ms | 228ms |
| **오버레이 원문 표시** | **~1.0s** | 서비스 구간 343ms |
| 첫 번역 도착 | 5s | 4.8s |
| 번역 완료 | 8s | 7.5s |

### 맥북 (원격, Tailscale)

| 단계 | 예산 |
|---|---|
| 변화 감지 지연 | 300-600ms |
| 캡처 (Retina) + 정규화 + 인코딩 | 130ms |
| 업로드 (300KB, WiFi) | 200ms |
| 검출 (+ 마스크 RLE) | 165ms |
| OCR (배치 10개) | 350ms |
| SSE 왕복 | 30ms |
| **오버레이 원문 표시** | **~1.3s** |
| 첫 번역 도착 | 5.4s |
| 번역 완료 | 8.4s |

캐시 적중은 7ms다 (실측). 같은 페이지를 다시 보거나 모드를 바꿀 때 이 경로를 탄다.

오버레이 렌더링 자체는 예산에 넣지 않는다. QPainter 드로잉은 1ms 미만이다.

**전제 조건**

- 모델 상주. 매 페이지 로드 시 3-5초 추가
- 맥북이 유선/5GHz WiFi. 2.4GHz면 업로드가 500ms 이상으로 늘어난다
- Tailscale이 DERP 릴레이가 아닌 직접 연결(P2P)로 붙어 있어야 한다. `tailscale status`로 확인 가능
- **GPU가 다른 워크로드와 공유되지 않아야 한다.** 그림 생성이 돌고 있으면 같은 입력의 OCR
  지연이 13배까지 흔들린다. 실제 비용은 VRAM이 아니라 **SM 경합**이다 (§9.3 정정)
- 번역 지연은 출력 토큰 수에 비례한다. 말풍선이 많은 페이지는 더 걸린다

---

## 11. 구현 순서

각 단계마다 동작하는 산출물이 나오도록 쪼갰다.

### M1 - 서비스 코어 (윈도우 로컬만)

이미지 파일을 POST하면 검출-OCR-정렬 결과를 JSON으로 돌려주는 FastAPI. 번역 없음, SSE 없음, 클라이언트 없음. curl로 테스트.

**검증**: 세 사이트 각각 10페이지 스크린샷을 수동으로 준비해 투입. 검출률, 읽기 순서 정확도, `is_bubble` 분류 정확도를 눈으로 확인.

### M2 - 번역 + 캐시 ✅

배치 번역과 SQLite 캐시 추가. SSE 응답으로 전환. 여전히 curl 테스트.

**검증**: 캐시 히트 시 API 호출이 발생하지 않는지 확인. 같은 페이지 재요청 시 응답 시간 0.1초 미만.

**결과 (2026-07-27)**: 캐시 적중 7ms. 1회차 원문 343ms / 첫 번역 4.8초 / 완료 7.5초.
번역은 스트리밍으로 흘리며 말풍선이 300-700ms 간격으로 채워진다. OCR과 번역을 분리
저장하므로 모드를 바꿔도 검출·OCR을 재실행하지 않는다.

### M3 - 윈도우 클라이언트 + L1 오버레이

단축키를 누르면 캡처 → 정규화 → POST → **L1 핀 오버레이**. 폴링 없음.

L1부터 하는 이유: 클라이언트 최대 리스크인 **좌표 역환산(§6)** 을 최소 코드로 검증한다. 핀은 ±20px 어긋나도 어느 말풍선인지 알 수 있으므로, 정확도를 단계적으로 조여갈 수 있다.

**검증**: 원문이 1초 이내에 뜨는지 계측. 배율 100%/125%/150% 각각에서 핀이 말풍선을 가리키는지.

### M4 - Watcher + 피드백 차단

폴링 자동 감지로 전환. §5.2 오버레이 rect 마스킹과 §5.3 검은 화면 판정을 함께 넣는다. 단축키는 수동 재처리용으로만 남긴다.

**검증**: 20페이지 연속 넘기며 오탐(중복 처리)과 미탐 횟수 기록. **오버레이가 떠 있는 상태에서 재트리거가 0회여야 한다.** 사이트별 `stable_frames` 값을 여기서 확정한다.

### M5 - L2/L3 렌더러

`mtl_shared/layout.py`의 배치·조판 규칙, 세로쓰기 조판, `fill_confidence` 폴백, 모드 토글.

**검증**: L3에서 그림이 뭉개진 말풍선이 몇 개인지 세기. 폴백이 그 중 몇 개를 잡았는지.

### M6 - 맥북 클라이언트

**여기가 진짜 작업이 몰리는 구간이다.** TCC 권한 처리, Retina 배율 산출, Tailscale 연결 관리, 업로드 최적화, macOS 오버레이 창 속성(`fullScreenAuxiliary`).

**검증**: 권한 미부여 상태에서 §5.3이 명확한 안내를 내는지. 외부 모니터 연결 상태에서 좌표가 맞는지. 네트워크 단절 시 오버레이가 죽지 않는지.

### M7 - 설정과 다듬기

드래그 리전 지정 UI, 프로필 전환, 모드 토글 UI, 세션 내보내기(마크다운), 성능 로그 요약.

---

## 12. 설정

**서비스** `service.toml`

```toml
[server]
bind_tailscale_only = true
port = 8788
auth_token = "<공유 시크릿>"

[models]
resident = true
ttl_seconds = 0        # 0 = 무한 상주

[api]
model_fast = "claude-haiku-4-5-20251001"
model_quality = "claude-sonnet-5"

[cache]
retention_days = 90
fuzzy_hamming = 3
```

**클라이언트** `client.toml`

```toml
[service]
url = "http://100.x.y.z:8788"
auth_token = "<공유 시크릿>"
timeout_seconds = 15

[watcher]
poll_interval_ms = 300
hash_threshold = 5
stable_frames = 2          # 프로필에서 덮어쓸 수 있는 기본값

[capture]
target_short_side = 1400
jpeg_quality = 88
blank_stddev_threshold = 2.0

[overlay]
default_mode = "label"     # pin | label | fill | off
fill_confidence_min = 0.8
font_min_px = 11

# v5: include_sfx 는 전부 true 로 둔다. 서버의 is_bubble 필터는 신뢰할 수 없고
# (§8.1), 효과음·잡문은 §8.4 의 kind 로 클라이언트가 숨긴다.
[profiles.comicwalker]
capture_region = { x = 480, y = 120, w = 1200, h = 1400 }
include_sfx = true
stable_frames = 3          # SpeedBinb 타일 점진 렌더링 대응
default_mode = "label"

[profiles.yanmaga]
capture_region = { x = 520, y = 100, w = 1100, h = 1500 }
include_sfx = true
stable_frames = 3
default_mode = "label"

[profiles.jumpplus]
capture_region = { x = 400, y = 140, w = 1400, h = 1350 }
include_sfx = true
stable_frames = 2
default_mode = "pin"       # 효과음이 많아 화면이 복잡하다
```

리전 좌표는 기기마다 다르므로 `client.toml`은 맥북과 윈도우에서 별개로 관리한다. 동기화하지 말 것.

**리전 설정 UX**: 초기 실행 시 화면을 어둡게 덮고 드래그로 영역을 지정하는 오버레이를 제공한다. 좌표를 손으로 입력하게 만들면 아무도 안 쓴다.

---

## 13. 알려진 난제

1. **읽기 순서**는 완전히 풀리지 않는다. 휴리스틱 + LLM 교정으로 85-90%를 목표하고, 나머지는 사용자가 원문을 보고 판단하게 둔다.

2. **캡처 피드백 루프**(§5.2)를 빼먹으면 무한 재트리거에 빠지고, 우리가 그린 한글을 OCR한다. M4의 필수 검증 항목이다.

3. **macOS 화면 기록 권한**은 조용히 실패한다. §5.3의 검은 화면 판정이 이걸 잡는다. 판정 로직을 빼먹으면 원인 모를 "아무것도 안 뜸" 버그로 몇 시간을 쓴다.

4. **Retina 배율 산출**은 외부 모니터 연결 시 모니터마다 다를 수 있다. 캡처 대상 모니터를 특정하고 그 모니터의 배율을 쓸 것.

5. **효과음(擬音語)** 은 OCR 정확도가 낮고 번역 가치도 애매하다. 점프+처럼 효과음이 서사에 개입하는 경우가 있어 프로필별 토글을 뒀다. L3에서는 절대 채우지 않는다.

6. **L3 조판**은 세로쓰기가 전제다. 가로쓰기로 구현하면 좁고 긴 말풍선에서 대부분 넘친다.

7. **폴링 오탐**: 광고 배너나 애니메이션 요소가 리전에 걸리면 계속 트리거된다. 캡처 리전을 좁게 잡는 것이 1차 방어. 그래도 문제면 해시 계산에서 리전 가장자리 10%를 제외한다. *(광고 차단기가 이미 동작 중이라 위험이 낮다 — 부록 A)*

8. **Tailscale DERP 폴백**: 직접 연결이 안 되면 릴레이를 타면서 업로드가 1초 이상으로 늘어난다. 클라이언트가 응답 지연을 감지하면 경고를 표시할 것.

---

## 14. 작업 지시

- **M1부터 순서대로.** 전체를 한 번에 쓰지 말 것. 각 마일스톤 끝에서 실제로 돌려보고 넘어간다
- **성능 계측을 처음부터 넣는다.** 나중에 붙이면 어디가 느린지 추측하게 된다
- 모델 로딩은 싱글톤. 요청 핸들러 안에서 로드하는 코드가 들어가면 설계 목표가 무너진다
- OCR은 배치 추론. 루프 안에서 하나씩 호출 금지
- 번역 API 호출은 페이지당 1회. 말풍선당 호출 금지
- 캡처 이미지를 디스크에 쓰지 않는다. 디버그 모드에서만 예외 허용, 종료 시 삭제
- 클라이언트는 **플랫폼 분기를 캡처 계층 한 곳에 격리**한다. Watcher, 오버레이, 네트워크 코드에 `if platform == "darwin"`이 흩어지면 유지보수가 무너진다
- **배치·조판 규칙은 `mtl_shared/layout.py`에 둔다.** 렌더러가 늘어나도 결과가 갈리지 않게
- 예외 처리: 파이프라인 어느 단계가 실패해도 Watcher는 살아 있어야 한다. 실패는 표시하고 다음 페이지에서 재시도

### 프로젝트 구조

```
MangaLiveReader/
├── pyproject.toml
├── DESIGN.md
├── service.toml
├── client.toml
├── src/
│   ├── mtl_service/
│   │   ├── __main__.py
│   │   ├── app.py            # FastAPI, SSE 엔드포인트
│   │   ├── config.py
│   │   ├── detect.py         # comic-text-detector 래퍼 (싱글톤)
│   │   ├── ocr.py            # manga-ocr 배치 래퍼 (싱글톤)
│   │   ├── order.py          # 읽기 순서 정렬
│   │   ├── mask.py           # RLE 인코딩, fill_rgb, fill_confidence
│   │   ├── translate.py      # 배치 번역, 프롬프트, 스트리밍
│   │   ├── cache.py          # SQLite
│   │   └── metrics.py
│   ├── mtl_client/
│   │   ├── __main__.py
│   │   ├── watcher.py        # 폴링, 해시, 오버레이 rect 마스킹, 블랭크 판정
│   │   ├── capture/
│   │   │   ├── base.py       # 공통 인터페이스, 정규화
│   │   │   ├── win.py        # DPI awareness
│   │   │   └── mac.py        # TCC 검증, Retina 배율
│   │   ├── transport.py      # httpx, SSE 수신
│   │   └── overlay/
│   │       ├── window.py     # 프레임리스 · 투명 · 항상위 · 입력통과
│   │       ├── pin.py        # L1
│   │       ├── label.py      # L2
│   │       ├── fill.py       # L3
│   │       └── region.py     # 드래그 리전 선택
│   └── mtl_shared/
│       ├── models.py         # 요청/응답 dataclass (양쪽 공유)
│       ├── layout.py         # 배치·조판 규칙 (렌더러 공용)
│       └── coords.py         # 좌표 역환산 (§6)
└── tests/
```

---

## 15. 이용 범위

- 정식 구독 또는 사이트가 무료로 공개한 회차를 열람하는 중에만 사용한다
- 번역 결과와 캡처 이미지를 외부에 배포하지 않는다
- 이 도구는 언어 장벽 때문에 읽지 못하는 콘텐츠를 개인이 읽기 위한 보조 수단이며, 정식 번역판이 있는 작품은 그쪽을 이용하는 것이 낫다

---

## 부록 A. 실증 조사 결과 (2026-07-26)

설계 결정의 근거가 된 실제 측정값. 재확인하려면 유료 구독 뷰어를 열어야 하므로 기록해 둔다.

| 항목 | 결과 | 영향 |
|---|---|---|
| OS 화면 캡처 (`Win+Shift+S`) | **정상** — 만화가 그대로 찍힘 | 경로 A 실증. §2.4 |
| 포커스 상실 시 블랭크 | **없음** — 다른 창 클릭해도 유지 | 오버레이·리전선택 UI가 포커스를 가져가도 안전. §7.1 |
| 뷰어 엔진 | **SpeedBinb** (`speedbinb-*.js`) | 타일 스크램블 → §2.1 근거. 점진 렌더링 → §5.1 `stable_frames` |
| 프레임 구조 | **교차 출처 iframe** (`viewer_embed` ↔ `parent_main` postMessage) | 경로 B 감점. §2.4 |
| 사이트 커스터마이즈 | `custom_speedbinb-*.js`, `custom_parent_main-*.js` 존재 | 셀렉터가 사이트별로 갈림. 경로 B 감점 |
| 광고 | `ERR_BLOCKED_BY_CLIENT` (차단기 동작) | §13-7 오탐 위험 낮음 |
| 브라우저 확장 캡처 | **미검증** | 경로 A 확정으로 무의미 |
| Vivaldi 내장 캡처 | **모든 페이지에서 실패** (원인 미상) | 경로 A는 브라우저 무관하므로 무영향 |
